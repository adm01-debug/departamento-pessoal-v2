# Docker Compose Prod - Configuration
version: '1.0'
name: Docker Compose Prod
enabled: true

settings:
  environment: production
  logging: true
  monitoring: true

config:
  timeout: 300
  retries: 3
  cache: true
