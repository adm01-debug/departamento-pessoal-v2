# Helm Values Prod - Configuration
version: '1.0'
name: Helm Values Prod
enabled: true

settings:
  environment: production
  logging: true
  monitoring: true

config:
  timeout: 300
  retries: 3
  cache: true
