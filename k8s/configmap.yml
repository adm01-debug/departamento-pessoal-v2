# K8s ConfigMap - Configuration
version: '1.0'
name: K8s ConfigMap
enabled: true

settings:
  environment: production
  logging: true
  monitoring: true

config:
  timeout: 300
  retries: 3
  cache: true
