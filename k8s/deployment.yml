# K8s Deployment - Configuration
version: '1.0'
name: K8s Deployment
enabled: true

settings:
  environment: production
  logging: true
  monitoring: true

config:
  timeout: 300
  retries: 3
  cache: true
