# Ansible Inventory - Configuration
version: '1.0'
name: Ansible Inventory
enabled: true

settings:
  environment: production
  logging: true
  monitoring: true

config:
  timeout: 300
  retries: 3
  cache: true
