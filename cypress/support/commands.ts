Cypress.Commands.add('login',(email='admin@sistema.com',senha='senha123')=>{cy.session([email,senha],()=>{cy.visit('/login');cy.get('input[name="email"]').type(email);cy.get('input[name="senha"]').type(senha);cy.get('button[type="submit"]').click();cy.url().should('eq','http://localhost:3000/');});});declare global{namespace Cypress{interface Chainable{login(email?:string,senha?:string):Chainable<void>;}}}export{};
