import{differenceInMonths}from'date-fns';interface DadosDecimo{salario:number;mesesTrabalhados:number;adicionais:number;mediaComissoes:number;}export function calcular13Primeira(dados:DadosDecimo):number{const base=dados.salario+dados.adicionais+dados.mediaComissoes;return(base/12)*Math.min(dados.mesesTrabalhados,12)/2;}export function calcular13Segunda(dados:DadosDecimo,valorPrimeira:number,inss:number,irrf:number):number{const base=dados.salario+dados.adicionais+dados.mediaComissoes;const total13=(base/12)*Math.min(dados.mesesTrabalhados,12);return total13-valorPrimeira-inss-irrf;}export function calcularMesesTrabalhados(dataAdmissao:string,competencia:string):number{const admissao=new Date(dataAdmissao);const comp=new Date(competencia+'-01');const meses=differenceInMonths(comp,admissao)+1;return Math.min(12,Math.max(0,meses));}
