export interface DadosRAIS{anoBase:string;empresa:{cnpj:string;razaoSocial:string};vinculos:{cpf:string;nome:string;dataAdmissao:string;dataDemissao?:string;remuneracoes:number[]}[];}export function gerarArquivoRAIS(dados:DadosRAIS):string{const linhas:string[]=[];linhas.push(`RAIS${dados.anoBase}`);linhas.push(`E${dados.empresa.cnpj}${dados.empresa.razaoSocial}`);dados.vinculos.forEach(v=>{linhas.push(`V${v.cpf}${v.nome}${v.dataAdmissao}${v.dataDemissao||''}`);v.remuneracoes.forEach((r,i)=>{linhas.push(`R${(i+1).toString().padStart(2,'0')}${r.toFixed(2)}`);});});return linhas.join('
');}export function validarRAIS(dados:DadosRAIS):{valido:boolean;erros:string[]}{const erros:string[]=[];if(!dados.anoBase)erros.push('Ano base obrigatório');return{valido:erros.length===0,erros};}
