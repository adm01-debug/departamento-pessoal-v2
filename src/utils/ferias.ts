import{differenceInMonths,differenceInDays,addYears}from'date-fns';interface SaldoFerias{periodoAquisitivo:{inicio:Date;fim:Date};diasDisponiveis:number;diasGozados:number;diasVendidos:number;vencimento:Date;vencido:boolean;}export function calcularSaldoFerias(dataAdmissao:string,feriasGozadas:{inicio:string;dias:number;abono:number}[]):SaldoFerias[]{const admissao=new Date(dataAdmissao);const hoje=new Date();const periodos:SaldoFerias[]=[];let periodoInicio=admissao;while(periodoInicio<hoje){const periodoFim=addYears(periodoInicio,1);const vencimento=addYears(periodoFim,1);const mesesTrabalhados=Math.min(12,differenceInMonths(hoje,periodoInicio));const diasDisponiveis=Math.floor(mesesTrabalhados*2.5);const feriasNoPeriodo=feriasGozadas.filter(f=>{const d=new Date(f.inicio);return d>=periodoInicio&&d<periodoFim;});const diasGozados=feriasNoPeriodo.reduce((s,f)=>s+f.dias,0);const diasVendidos=feriasNoPeriodo.reduce((s,f)=>s+f.abono,0);periodos.push({periodoAquisitivo:{inicio:periodoInicio,fim:periodoFim},diasDisponiveis:Math.max(0,30-diasGozados-diasVendidos),diasGozados,diasVendidos,vencimento,vencido:hoje>vencimento&&diasDisponiveis>0});periodoInicio=periodoFim;}return periodos;}export function calcularValorFerias(salario:number,dias:number,abono:number):number{const valorDiario=salario/30;const valorFerias=valorDiario*dias;const terco=valorFerias/3;const valorAbono=valorDiario*abono;const tercoAbono=valorAbono/3;return valorFerias+terco+valorAbono+tercoAbono;}
