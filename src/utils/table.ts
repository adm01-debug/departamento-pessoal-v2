import{Column}from'@tanstack/react-table';export function sortByDate(a:string,b:string):number{return new Date(a).getTime()-new Date(b).getTime();}export function sortByNumber(a:number,b:number):number{return a-b;}export function sortByString(a:string,b:string):number{return a.localeCompare(b,'pt-BR');}export function filterBySearch<T>(data:T[],search:string,keys:string[]):T[]{if(!search.trim())return data;const searchLower=search.toLowerCase();return data.filter(item=>keys.some(key=>{const value=(item as any)[key];return String(value).toLowerCase().includes(searchLower);}));}export function paginate<T>(data:T[],page:number,pageSize:number):T[]{const start=(page-1)*pageSize;return data.slice(start,start+pageSize);}export function getTotalPages(total:number,pageSize:number):number{return Math.ceil(total/pageSize);}
