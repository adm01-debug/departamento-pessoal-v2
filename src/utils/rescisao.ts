export interface DadosRescisao{salario:number;dataAdmissao:string;dataDemissao:string;tipo:'sem_justa_causa'|'justa_causa'|'pedido_demissao'|'acordo';avisoPrevio:'trabalhado'|'indenizado'|'dispensado';saldoFGTS:number;feriasVencidas:number;diasFerias:number;mediaHorasExtras:number;}export function calcularRescisao(dados:DadosRescisao){const diasTrabalhados=calcularDiasTrabalhados(dados.dataDemissao);const saldoSalario=(dados.salario/30)*diasTrabalhados;const feriasProporcionais=calcularFeriasProporcionais(dados.salario,dados.dataAdmissao,dados.dataDemissao);const decimoProporc=calcularDecimoProporc(dados.salario,dados.dataDemissao);const avisoPrevio=dados.tipo==='sem_justa_causa'&&dados.avisoPrevio==='indenizado'?dados.salario:0;const multaFGTS=dados.tipo==='sem_justa_causa'?dados.saldoFGTS*0.4:dados.tipo==='acordo'?dados.saldoFGTS*0.2:0;const total=saldoSalario+feriasProporcionais.total+decimoProporc+avisoPrevio+multaFGTS;return{saldoSalario,feriasProporcionais,decimoProporc,avisoPrevio,multaFGTS,total};}function calcularDiasTrabalhados(data:string):number{const d=new Date(data);return d.getDate();}function calcularFeriasProporcionais(sal:number,adm:string,dem:string):{valor:number;terco:number;total:number}{const meses=Math.floor((new Date(dem).getTime()-new Date(adm).getTime())/(30*24*60*60*1000))%12;const valor=(sal/12)*meses;const terco=valor/3;return{valor,terco,total:valor+terco};}function calcularDecimoProporc(sal:number,dem:string):number{const mes=new Date(dem).getMonth()+1;return(sal/12)*mes;}
