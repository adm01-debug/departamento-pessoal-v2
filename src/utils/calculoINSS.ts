export const calcularINSS = (salario: number): number => { const faixas = [{ ate: 1518.00, aliq: 0.075 }, { ate: 2793.88, aliq: 0.09 }, { ate: 4190.83, aliq: 0.12 }, { ate: 8157.41, aliq: 0.14 }]; let inss = 0; let anterior = 0; for (const faixa of faixas) { if (salario > anterior) { const base = Math.min(salario, faixa.ate) - anterior; inss += base * faixa.aliq; anterior = faixa.ate; } } return inss; };
