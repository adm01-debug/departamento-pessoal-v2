import{formatCurrency,formatDate}from'./formatters';export interface ReportConfig{title:string;subtitle?:string;headers:string[];data:any[][];footer?:string;}export function generateReport(config:ReportConfig):string{let html=`<h1>${config.title}</h1>`;if(config.subtitle)html+=`<p>${config.subtitle}</p>`;html+=`<table><thead><tr>${config.headers.map(h=>`<th>${h}</th>`).join('')}</tr></thead><tbody>`;config.data.forEach(row=>{html+=`<tr>${row.map(cell=>`<td>${cell}</td>`).join('')}</tr>`;});html+=`</tbody></table>`;if(config.footer)html+=`<p style="margin-top:20px;font-size:12px;">${config.footer}</p>`;return html;}export function generateColaboradoresReport(colaboradores:any[]):ReportConfig{return{title:'Relatório de Colaboradores',subtitle:`Gerado em ${formatDate(new Date().toISOString())}`,headers:['Nome','CPF','Cargo','Departamento','Admissão','Salário'],data:colaboradores.map(c=>[c.nome,c.cpf,c.cargo,c.departamento,formatDate(c.dataAdmissao),formatCurrency(c.salario)]),footer:`Total: ${colaboradores.length} colaboradores`};}
