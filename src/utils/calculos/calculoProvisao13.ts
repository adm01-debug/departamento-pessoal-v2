import { calcularProvisao13 } from '@/calculators/rescisao';

export const calculoProvisao13 = (salarioBase: number, mesesTrabalhados: number) => {
  return calcularProvisao13(salarioBase, mesesTrabalhados);
};
