export const importUtils = { parseCSV: (csv: string, separator: string = ','): string[][] => { return csv.split('\n').map(line => line.split(separator).map(cell => cell.trim())); }, csvToObjects: <T>(csv: string, separator: string = ','): T[] => { const [headers, ...rows] = importUtils.parseCSV(csv, separator); return rows.map(row => headers.reduce((obj, header, i) => ({ ...obj, [header]: row[i] }), {} as T)); } };
