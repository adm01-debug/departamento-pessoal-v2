export const validators = {
  cpf: (cpf: string): boolean => { cpf = cpf.replace(/\D/g, ""); if (cpf.length !== 11 || /^(\d)\1+$/.test(cpf)) return false; let soma = 0; for (let i = 0; i < 9; i++) soma += parseInt(cpf[i]) * (10 - i); let resto = (soma * 10) % 11; if (resto === 10 || resto === 11) resto = 0; if (resto !== parseInt(cpf[9])) return false; soma = 0; for (let i = 0; i < 10; i++) soma += parseInt(cpf[i]) * (11 - i); resto = (soma * 10) % 11; if (resto === 10 || resto === 11) resto = 0; return resto === parseInt(cpf[10]); },
  cnpj: (cnpj: string): boolean => { cnpj = cnpj.replace(/\D/g, ""); if (cnpj.length !== 14 || /^(\d)\1+$/.test(cnpj)) return false; let tamanho = cnpj.length - 2; let numeros = cnpj.substring(0, tamanho); let digitos = cnpj.substring(tamanho); let soma = 0; let pos = tamanho - 7; for (let i = tamanho; i >= 1; i--) { soma += parseInt(numeros.charAt(tamanho - i)) * pos--; if (pos < 2) pos = 9; } let resultado = soma % 11 < 2 ? 0 : 11 - (soma % 11); if (resultado !== parseInt(digitos.charAt(0))) return false; tamanho++; numeros = cnpj.substring(0, tamanho); soma = 0; pos = tamanho - 7; for (let i = tamanho; i >= 1; i--) { soma += parseInt(numeros.charAt(tamanho - i)) * pos--; if (pos < 2) pos = 9; } resultado = soma % 11 < 2 ? 0 : 11 - (soma % 11); return resultado === parseInt(digitos.charAt(1)); },
  email: (email: string): boolean => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email),
  pis: (pis: string): boolean => { pis = pis.replace(/\D/g, ""); if (pis.length !== 11) return false; const multiplicadores = [3, 2, 9, 8, 7, 6, 5, 4, 3, 2]; let soma = 0; for (let i = 0; i < 10; i++) soma += parseInt(pis[i]) * multiplicadores[i]; const resto = soma % 11; const digito = resto < 2 ? 0 : 11 - resto; return digito === parseInt(pis[10]); },
  telefone: (tel: string): boolean => /^\d{10,11}$/.test(tel.replace(/\D/g, "")),
};
export default validators;
