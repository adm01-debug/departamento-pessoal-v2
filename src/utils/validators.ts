export function isValidCPF(cpf:string):boolean{cpf=cpf.replace(/\D/g,'');if(cpf.length!==11||/^(\d)+$/.test(cpf))return false;let sum=0;for(let i=0;i<9;i++)sum+=(10-i)*parseInt(cpf[i]);let rest=(sum*10)%11;if(rest===10||rest===11)rest=0;if(rest!==parseInt(cpf[9]))return false;sum=0;for(let i=0;i<10;i++)sum+=(11-i)*parseInt(cpf[i]);rest=(sum*10)%11;if(rest===10||rest===11)rest=0;return rest===parseInt(cpf[10]);}export function isValidCNPJ(cnpj:string):boolean{cnpj=cnpj.replace(/\D/g,'');if(cnpj.length!==14||/^(\d)+$/.test(cnpj))return false;const calc=(x:number)=>{let sum=0;let pos=x-7;for(let i=x;i>=1;i--){sum+=parseInt(cnpj[x-i])*pos--;if(pos<2)pos=9;}const result=sum%11<2?0:11-sum%11;return result===parseInt(cnpj[x]);};return calc(12)&&calc(13);}export function isValidEmail(email:string):boolean{return/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);}export function isValidPhone(phone:string):boolean{return/^\d{10,11}$/.test(phone.replace(/\D/g,''));}export function isValidPIS(pis:string):boolean{pis=pis.replace(/\D/g,'');if(pis.length!==11)return false;const weights=[3,2,9,8,7,6,5,4,3,2];let sum=0;for(let i=0;i<10;i++)sum+=parseInt(pis[i])*weights[i];const rest=11-sum%11;const digit=rest===10||rest===11?0:rest;return digit===parseInt(pis[10]);}
