import{describe,it,expect}from'vitest';import{validarCPF,validarCNPJ,validarEmail,validarTelefone}from'../validators';describe('validators',()=>{describe('validarCPF',()=>{it('validates correct CPF',()=>{expect(validarCPF('529.982.247-25')).toBe(true);expect(validarCPF('52998224725')).toBe(true);});it('invalidates wrong CPF',()=>{expect(validarCPF('111.111.111-11')).toBe(false);expect(validarCPF('123.456.789-00')).toBe(false);});});describe('validarEmail',()=>{it('validates correct email',()=>{expect(validarEmail('test@email.com')).toBe(true);});it('invalidates wrong email',()=>{expect(validarEmail('invalid')).toBe(false);});});});
