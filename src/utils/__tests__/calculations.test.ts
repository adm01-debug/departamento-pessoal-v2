import{describe,it,expect}from'vitest';import{calcularINSS,calcularIRRF}from'../calculations';describe('calculations',()=>{describe('calcularINSS',()=>{it('calculates first range',()=>{expect(calcularINSS(1412)).toBeCloseTo(105.90,2);});it('calculates higher salary',()=>{expect(calcularINSS(5000)).toBeCloseTo(523.55,2);});it('respects ceiling',()=>{expect(calcularINSS(10000)).toBeCloseTo(908.85,2);});});describe('calcularIRRF',()=>{it('returns 0 for exempt range',()=>{expect(calcularIRRF(2000,0)).toBe(0);});it('calculates with deductions',()=>{expect(calcularIRRF(5000,2)).toBeGreaterThan(0);});});});
