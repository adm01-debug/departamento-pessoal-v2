export const calcularRescisao = (salario: number, mesesTrabalhados: number, tipoRescisao: string, avisoPrevio: boolean): any => { const saldoSalario = salario; const ferias = (salario / 12) * mesesTrabalhados; const decimoTerceiro = (salario / 12) * mesesTrabalhados; const fgts = salario * 0.08 * mesesTrabalhados; const multa = tipoRescisao === 'semJustaCausa' ? fgts * 0.4 : 0; const aviso = avisoPrevio ? salario : 0; return { saldoSalario, ferias, decimoTerceiro, fgts, multa, avisoPrevio: aviso, total: saldoSalario + ferias + decimoTerceiro + multa + aviso }; };
