const BASE_URL = import.meta.env.VITE_API_URL || '/api';
export const httpClient = { get: async <T>(url: string): Promise<T> => { const res = await fetch(`${BASE_URL}${url}`); if (!res.ok) throw new Error(res.statusText); return res.json(); }, post: async <T>(url: string, data: any): Promise<T> => { const res = await fetch(`${BASE_URL}${url}`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) }); if (!res.ok) throw new Error(res.statusText); return res.json(); }, put: async <T>(url: string, data: any): Promise<T> => { const res = await fetch(`${BASE_URL}${url}`, { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) }); if (!res.ok) throw new Error(res.statusText); return res.json(); }, delete: async (url: string): Promise<void> => { const res = await fetch(`${BASE_URL}${url}`, { method: 'DELETE' }); if (!res.ok) throw new Error(res.statusText); } };
