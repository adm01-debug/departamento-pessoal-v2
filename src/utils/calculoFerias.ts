export const calcularFerias = (salario: number, diasFerias: number = 30, abono: boolean = false): { ferias: number; terco: number; abono: number; total: number } => { const valorDia = salario / 30; const ferias = valorDia * diasFerias; const terco = ferias / 3; const valorAbono = abono ? (valorDia * 10) + ((valorDia * 10) / 3) : 0; return { ferias, terco, abono: valorAbono, total: ferias + terco + valorAbono }; };
