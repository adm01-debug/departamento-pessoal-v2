export interface DadosBoleto{valor:number;vencimento:Date;beneficiario:{cnpj:string;nome:string;banco:string;agencia:string;conta:string;};pagador:{cpf:string;nome:string;endereco:string;};nossoNumero:string;}export function gerarCodigoBarras(dados:DadosBoleto):string{const banco=dados.beneficiario.banco.padStart(3,'0');const moeda='9';const fatorVenc=calcularFatorVencimento(dados.vencimento);const valor=Math.round(dados.valor*100).toString().padStart(10,'0');const campoLivre=[dados.beneficiario.agencia.padStart(4,'0'),dados.beneficiario.conta.padStart(10,'0'),dados.nossoNumero.padStart(11,'0')].join('');const codigoSemDV=banco+moeda+fatorVenc+valor+campoLivre;const dv=calcularDV11(codigoSemDV);return banco+moeda+dv+fatorVenc+valor+campoLivre;}function calcularFatorVencimento(vencimento:Date):string{const base=new Date(1997,9,7);const diff=Math.floor((vencimento.getTime()-base.getTime())/(1000*60*60*24));return diff.toString().padStart(4,'0');}function calcularDV11(codigo:string):string{const pesos=[4,3,2,9,8,7,6,5,4,3,2,9,8,7,6,5,4,3,2,9,8,7,6,5,4,3,2,9,8,7,6,5,4,3,2,9,8,7,6,5,4,3,2];let soma=0;for(let i=0;i<codigo.length;i++){soma+=parseInt(codigo[i])*pesos[i];}const resto=soma%11;if(resto===0||resto===1||resto===10)return'1';return(11-resto).toString();}export function gerarLinhaDigitavel(codigoBarras:string):string{const campo1=codigoBarras.slice(0,4)+codigoBarras.slice(19,24);const campo2=codigoBarras.slice(24,34);const campo3=codigoBarras.slice(34,44);const campo4=codigoBarras[4];const campo5=codigoBarras.slice(5,19);return`${campo1}.${campo2} ${campo3} ${campo4} ${campo5}`;}
