export const dateUtils = { toISO: (date: Date) => date.toISOString(), fromISO: (str: string) => new Date(str), addDays: (date: Date, days: number) => new Date(date.getTime() + days * 86400000), diffDays: (a: Date, b: Date) => Math.floor((a.getTime() - b.getTime()) / 86400000), isWeekend: (date: Date) => [0, 6].includes(date.getDay()), startOfMonth: (date: Date) => new Date(date.getFullYear(), date.getMonth(), 1), endOfMonth: (date: Date) => new Date(date.getFullYear(), date.getMonth() + 1, 0) };
