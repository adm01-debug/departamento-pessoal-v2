export const arrayUtils = { unique: <T>(arr: T[]) => [...new Set(arr)], chunk: <T>(arr: T[], size: number) => Array.from({ length: Math.ceil(arr.length / size) }, (_, i) => arr.slice(i * size, i * size + size)), shuffle: <T>(arr: T[]) => [...arr].sort(() => Math.random() - 0.5), groupBy: <T>(arr: T[], key: keyof T) => arr.reduce((acc, item) => { const k = String(item[key]); (acc[k] = acc[k] || []).push(item); return acc; }, {} as Record<string, T[]>) };
