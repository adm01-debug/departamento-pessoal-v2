import{format,parseISO,differenceInDays,differenceInMonths,differenceInYears,addDays,addMonths,startOfMonth,endOfMonth,isWeekend,isSameMonth}from'date-fns';import{ptBR}from'date-fns/locale';export function formatDate(date:string|Date):string{const d=typeof date==='string'?parseISO(date):date;return format(d,'dd/MM/yyyy',{locale:ptBR});}export function formatDateTime(date:string|Date):string{const d=typeof date==='string'?parseISO(date):date;return format(d,'dd/MM/yyyy HH:mm',{locale:ptBR});}export function formatMonth(date:string|Date):string{const d=typeof date==='string'?parseISO(date):date;return format(d,'MMMM/yyyy',{locale:ptBR});}export function calcularIdade(dataNascimento:string):number{return differenceInYears(new Date(),parseISO(dataNascimento));}export function calcularTempoEmpresa(dataAdmissao:string):{anos:number;meses:number;dias:number}{const inicio=parseISO(dataAdmissao);const hoje=new Date();return{anos:differenceInYears(hoje,inicio),meses:differenceInMonths(hoje,inicio)%12,dias:differenceInDays(hoje,addMonths(inicio,differenceInMonths(hoje,inicio)))};}export function getDiasUteis(inicio:Date,fim:Date):number{let dias=0;let atual=inicio;while(atual<=fim){if(!isWeekend(atual))dias++;atual=addDays(atual,1);}return dias;}export function getCompetencia(date:Date=new Date()):string{return format(date,'yyyy-MM');}
