export const validateCNPJ = (cnpj: string): boolean => { const cleaned = cnpj.replace(/\D/g, ''); if (cleaned.length !== 14 || /^(\d)\1+$/.test(cleaned)) return false; const calc = (x: number) => { let sum = 0; let pos = x - 7; for (let i = x; i >= 1; i--) { sum += parseInt(cleaned[x - i]) * pos--; if (pos < 2) pos = 9; } return sum % 11 < 2 ? 0 : 11 - (sum % 11); }; return calc(12) === parseInt(cleaned[12]) && calc(13) === parseInt(cleaned[13]); };
