export function formatDuration(minutes:number):string{const hours=Math.floor(minutes/60);const mins=minutes%60;if(hours===0)return`${mins}min`;if(mins===0)return`${hours}h`;return`${hours}h ${mins}min`;}export function parseTimeString(time:string):{hours:number;minutes:number}{const[hours,minutes]=time.split(':').map(Number);return{hours:hours||0,minutes:minutes||0};}export function timeStringToMinutes(time:string):number{const{hours,minutes}=parseTimeString(time);return hours*60+minutes;}export function minutesToTimeString(totalMinutes:number):string{const hours=Math.floor(totalMinutes/60);const minutes=totalMinutes%60;return`${String(hours).padStart(2,'0')}:${String(minutes).padStart(2,'0')}`;}export function calculateWorkHours(entrada:string,saidaAlmoco:string,retornoAlmoco:string,saida:string):string{const manha=timeStringToMinutes(saidaAlmoco)-timeStringToMinutes(entrada);const tarde=timeStringToMinutes(saida)-timeStringToMinutes(retornoAlmoco);return minutesToTimeString(manha+tarde);}
