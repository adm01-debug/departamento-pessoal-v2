export const KEYS={ENTER:'Enter',ESCAPE:'Escape',TAB:'Tab',ARROW_UP:'ArrowUp',ARROW_DOWN:'ArrowDown',ARROW_LEFT:'ArrowLeft',ARROW_RIGHT:'ArrowRight',SPACE:' ',BACKSPACE:'Backspace',DELETE:'Delete'};export function isKey(e:KeyboardEvent,key:string):boolean{return e.key===key;}export function isEnter(e:KeyboardEvent):boolean{return isKey(e,KEYS.ENTER);}export function isEscape(e:KeyboardEvent):boolean{return isKey(e,KEYS.ESCAPE);}export function isTab(e:KeyboardEvent):boolean{return isKey(e,KEYS.TAB);}export function withCtrl(e:KeyboardEvent,key:string):boolean{return e.ctrlKey&&isKey(e,key);}export function withShift(e:KeyboardEvent,key:string):boolean{return e.shiftKey&&isKey(e,key);}export function preventDefaultOnEnter(e:KeyboardEvent):void{if(isEnter(e))e.preventDefault();}
