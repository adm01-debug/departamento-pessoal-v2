export const numberUtils = { clamp: (num: number, min: number, max: number) => Math.min(Math.max(num, min), max), round: (num: number, decimals: number) => Math.round(num * 10 ** decimals) / 10 ** decimals, random: (min: number, max: number) => Math.random() * (max - min) + min, isEven: (num: number) => num % 2 === 0, sum: (arr: number[]) => arr.reduce((a, b) => a + b, 0), average: (arr: number[]) => arr.length ? arr.reduce((a, b) => a + b, 0) / arr.length : 0 };
