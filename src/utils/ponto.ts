import{differenceInMinutes,parse}from'date-fns';interface RegistroPonto{entrada:string;saidaAlmoco?:string;retornoAlmoco?:string;saida?:string;}interface ResultadoPonto{horasTrabalhadas:number;minutosExtras:number;minutosAtraso:number;intervalo:number;status:'regular'|'incompleto'|'atraso'|'falta';}export function calcularPonto(registro:RegistroPonto,jornadaMinutos:number=480,tolerancia:number=10):ResultadoPonto{if(!registro.entrada)return{horasTrabalhadas:0,minutosExtras:0,minutosAtraso:0,intervalo:0,status:'falta'};const entrada=parse(registro.entrada,'HH:mm',new Date());const esperadaEntrada=parse('08:00','HH:mm',new Date());const minutosAtraso=Math.max(0,differenceInMinutes(entrada,esperadaEntrada)-tolerancia);if(!registro.saida)return{horasTrabalhadas:0,minutosExtras:0,minutosAtraso,intervalo:0,status:'incompleto'};const saida=parse(registro.saida,'HH:mm',new Date());let totalMinutos=differenceInMinutes(saida,entrada);let intervalo=0;if(registro.saidaAlmoco&&registro.retornoAlmoco){const saidaAlm=parse(registro.saidaAlmoco,'HH:mm',new Date());const retornoAlm=parse(registro.retornoAlmoco,'HH:mm',new Date());intervalo=differenceInMinutes(retornoAlm,saidaAlm);totalMinutos-=intervalo;}const horasTrabalhadas=totalMinutos/60;const minutosExtras=Math.max(0,totalMinutos-jornadaMinutos);return{horasTrabalhadas,minutosExtras,minutosAtraso,intervalo,status:minutosAtraso>0?'atraso':'regular'};}
