export function validarPIS(pis:string):boolean{const pisLimpo=pis.replace(/\D/g,'');if(pisLimpo.length!==11)return false;const pesos=[3,2,9,8,7,6,5,4,3,2];let soma=0;for(let i=0;i<10;i++){soma+=parseInt(pisLimpo[i])*pesos[i];}const resto=soma%11;const dv=resto<2?0:11-resto;return dv===parseInt(pisLimpo[10]);}export function formatarPIS(pis:string):string{const pisLimpo=pis.replace(/\D/g,'');return pisLimpo.replace(/(\d{3})(\d{5})(\d{2})(\d{1})/,'$1.$2.$3-$4');}export function gerarPIS():string{const gerar=()=>Math.floor(Math.random()*10);let pis='';for(let i=0;i<10;i++)pis+=gerar();const pesos=[3,2,9,8,7,6,5,4,3,2];let soma=0;for(let i=0;i<10;i++)soma+=parseInt(pis[i])*pesos[i];const resto=soma%11;const dv=resto<2?0:11-resto;return pis+dv;}
