export const fileUtils = { getExtension: (filename: string) => filename.split('.').pop()?.toLowerCase() || '', getMimeType: (ext: string) => ({ pdf: 'application/pdf', xlsx: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet', docx: 'application/vnd.openxmlformats-officedocument.wordprocessingml.document', csv: 'text/csv', json: 'application/json', png: 'image/png', jpg: 'image/jpeg' }[ext] || 'application/octet-stream'), formatSize: (bytes: number) => { const units = ['B', 'KB', 'MB', 'GB']; let i = 0; while (bytes >= 1024 && i < units.length - 1) { bytes /= 1024; i++; } return `${bytes.toFixed(2)} ${units[i]}`; } };
