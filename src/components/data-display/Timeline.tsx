import React from'react';import{cn}from'@/lib/utils';interface TimelineItem{id:string;title:string;description?:string;date:string;icon?:React.ReactNode;}interface Props{items:TimelineItem[];}export function Timeline({items}:Props){return(<ol className="relative border-l border-gray-200">{items.map((item,i)=>(<li key={item.id}className="mb-10 ml-6"><span className={cn('absolute flex items-center justify-center w-6 h-6 bg-primary rounded-full -left-3 ring-8 ring-white')}>{item.icon||<div className="w-2 h-2 bg-white rounded-full"/>}</span><h3 className="font-medium">{item.title}</h3>{item.description&&<p className="text-sm text-muted-foreground">{item.description}</p>}<time className="text-xs text-muted-foreground">{item.date}</time></li>))}</ol>);}
