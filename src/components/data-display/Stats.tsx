import React from'react';import{Card,CardContent}from'@/components/ui/card';import{cn}from'@/lib/utils';interface StatItem{label:string;value:string|number;change?:string;trend?:'up'|'down'|'neutral';icon?:React.ReactNode;}interface Props{stats:StatItem[];columns?:2|3|4;}export function Stats({stats,columns=4}:Props){return(<div className={cn('grid gap-4',`md:grid-cols-${columns}`)}>{stats.map((stat,i)=>(<Card key={i}><CardContent className="pt-6"><div className="flex items-center justify-between"><div><p className="text-sm text-muted-foreground">{stat.label}</p><p className="text-2xl font-bold">{stat.value}</p>{stat.change&&(<p className={cn('text-xs',stat.trend==='up'&&'text-green-600',stat.trend==='down'&&'text-red-600')}>{stat.change}</p>)}</div>{stat.icon&&<div className="text-muted-foreground">{stat.icon}</div>}</div></CardContent></Card>))}</div>);}
