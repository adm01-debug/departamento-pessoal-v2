import React from'react';interface InfoItem{label:string;value:React.ReactNode;}interface Props{items:InfoItem[];columns?:1|2|3;}export function InfoList({items,columns=2}:Props){return(<dl className={cn('grid gap-4',columns===1&&'grid-cols-1',columns===2&&'md:grid-cols-2',columns===3&&'md:grid-cols-3')}>{items.map((item,i)=>(<div key={i}className="space-y-1"><dt className="text-sm text-muted-foreground">{item.label}</dt><dd className="font-medium">{item.value}</dd></div>))}</dl>);}import{cn}from'@/lib/utils';
