import { describe, it, expect } from 'vitest';
import { AdmissaoProgress } from '../AdmissaoProgress';
describe('AdmissaoProgress', () => { it('defined', () => { expect(AdmissaoProgress).toBeDefined(); }); });
