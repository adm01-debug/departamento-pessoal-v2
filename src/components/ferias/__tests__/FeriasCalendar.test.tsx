import { describe, it, expect } from 'vitest';
import { FeriasCalendar } from '../FeriasCalendar';
describe('FeriasCalendar', () => { it('should be defined', () => { expect(FeriasCalendar).toBeDefined(); }); });
