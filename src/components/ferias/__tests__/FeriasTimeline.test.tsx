import { describe, it, expect } from 'vitest';
import { FeriasTimeline } from '../FeriasTimeline';
describe('FeriasTimeline', () => { it('defined', () => { expect(FeriasTimeline).toBeDefined(); }); });
