import React from'react';import{Timeline}from'@/components/data-display/Timeline';import{formatDate}from'@/utils/formatters';import{Calendar,CheckCircle,XCircle,Clock}from'lucide-react';interface Historico{id:string;acao:string;data:string;usuario:string;}interface Props{historico:Historico[];}const icons:Record<string,React.ReactNode>={solicitada:<Clock className="w-3 h-3 text-white"/>,aprovada:<CheckCircle className="w-3 h-3 text-white"/>,rejeitada:<XCircle className="w-3 h-3 text-white"/>,alterada:<Calendar className="w-3 h-3 text-white"/>};export function FeriasTimeline({historico}:Props){const items=historico.map(h=>({id:h.id,title:h.acao,description:`Por ${h.usuario}`,date:formatDate(h.data),icon:icons[h.acao.toLowerCase()]||<Clock className="w-3 h-3 text-white"/>}));return<Timeline items={items}/>;}
