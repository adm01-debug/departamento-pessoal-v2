import React from'react';import{cn}from'@/lib/utils';interface TimelineItem{id:string;title:string;description?:string;date:string;icon?React.ReactNode;color?:'primary'|'success'|'warning'|'error';}interface Props{items:TimelineItem[];className?:string;}const colors={primary:'bg-primary',success:'bg-green-500',warning:'bg-yellow-500',error:'bg-red-500'};export function Timeline({items,className}:Props){return(<div className={cn('space-y-4',className)}>{items.map((item,i)=>(<div key={item.id}className="flex gap-4"><div className="flex flex-col items-center"><div className={cn('w-3 h-3 rounded-full',colors[item.color||'primary'])}/>{i<items.length-1&&<div className="w-px h-full bg-border"/>}</div><div className="pb-4"><p className="text-sm text-muted-foreground">{item.date}</p><h4 className="font-medium">{item.title}</h4>{item.description&&<p className="text-sm text-muted-foreground">{item.description}</p>}</div></div>))}</div>);}
