import React from'react';interface Props{value:number;max?:number;showLabel?:boolean;size?:'sm'|'md'|'lg';color?:'default'|'success'|'warning'|'danger';}const sizeClasses={sm:'h-1',md:'h-2',lg:'h-4'};const colorClasses={default:'bg-primary',success:'bg-green-500',warning:'bg-yellow-500',danger:'bg-red-500'};export function ProgressBar({value,max=100,showLabel=false,size='md',color='default'}:Props){const percentage=Math.min(100,Math.max(0,(value/max)*100));return(<div className="w-full"><div className={`w-full bg-muted rounded-full overflow-hidden ${sizeClasses[size]}`}><div className={`${colorClasses[color]} ${sizeClasses[size]} transition-all duration-300`}style={{width:`${percentage}%`}}/></div>{showLabel&&<span className="text-xs text-muted-foreground mt-1">{Math.round(percentage)}%</span>}</div>);}
