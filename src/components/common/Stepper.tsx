import React from'react';import{cn}from'@/lib/utils';import{Check}from'lucide-react';interface Step{id:string;label:string;description?:string;}interface Props{steps:Step[];currentStep:number;onChange?:(step:number)=>void;className?:string;}export function Stepper({steps,currentStep,onChange,className}:Props){return(<div className={cn('flex',className)}>{steps.map((step,i)=>(<div key={step.id}className="flex-1 flex items-center"><button type="button"onClick={()=>onChange?.(i)}className={cn('flex items-center gap-2',i<=currentStep?'text-primary':'text-muted-foreground')}disabled={!onChange}><div className={cn('w-8 h-8 rounded-full flex items-center justify-center border-2',i<currentStep?'bg-primary border-primary text-white':i===currentStep?'border-primary':'border-muted')}>{i<currentStep?<Check className="w-4 h-4"/>:<span>{i+1}</span>}</div><div className="text-left hidden sm:block"><p className="text-sm font-medium">{step.label}</p>{step.description&&<p className="text-xs text-muted-foreground">{step.description}</p>}</div></button>{i<steps.length-1&&<div className={cn('flex-1 h-px mx-4',i<currentStep?'bg-primary':'bg-border')}/>}</div>))}</div>);}
