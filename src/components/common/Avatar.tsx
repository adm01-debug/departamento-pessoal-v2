import React from'react';import{cn}from'@/lib/utils';interface Props{name:string;image?:string;size?:'sm'|'md'|'lg'|'xl';className?:string;}const sizeClasses={sm:'w-8 h-8 text-xs',md:'w-10 h-10 text-sm',lg:'w-12 h-12 text-base',xl:'w-20 h-20 text-2xl'};export function Avatar({name,image,size='md',className}:Props){const initials=name.split(' ').map(n=>n[0]).slice(0,2).join('').toUpperCase();const colors=['bg-red-500','bg-blue-500','bg-green-500','bg-yellow-500','bg-purple-500','bg-pink-500','bg-indigo-500'];const colorIndex=name.split('').reduce((acc,char)=>acc+char.charCodeAt(0),0)%colors.length;if(image){return<img src={image}alt={name}className={cn('rounded-full object-cover',sizeClasses[size],className)}/>;}return(<div className={cn('rounded-full flex items-center justify-center text-white font-medium',sizeClasses[size],colors[colorIndex],className)}>{initials}</div>);}
