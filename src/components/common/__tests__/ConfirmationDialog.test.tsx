import { describe, it, expect } from 'vitest';
import { ConfirmationDialog } from '../ConfirmationDialog';
describe('ConfirmationDialog', () => { it('defined', () => { expect(ConfirmationDialog).toBeDefined(); }); });
