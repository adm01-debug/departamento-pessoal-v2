import { describe, it, expect } from 'vitest';
import { VisuallyHidden } from '../VisuallyHidden';
describe('VisuallyHidden', () => { it('defined', () => { expect(VisuallyHidden).toBeDefined(); }); });
