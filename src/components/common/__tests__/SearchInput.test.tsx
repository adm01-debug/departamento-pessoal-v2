import { describe, it, expect } from 'vitest';
import { SearchInput } from '../SearchInput';
describe('SearchInput', () => { it('should be defined', () => { expect(SearchInput).toBeDefined(); }); });
