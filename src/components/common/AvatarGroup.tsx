import React from'react';import{Avatar}from'./Avatar';interface Props{avatars:{name:string;image?:string}[];max?:number;size?:'sm'|'md'|'lg';}export function AvatarGroup({avatars,max=4,size='md'}:Props){const visible=avatars.slice(0,max);const remaining=avatars.length-max;return(<div className="flex -space-x-2">{visible.map((avatar,i)=>(<div key={i}className="ring-2 ring-background rounded-full"><Avatar name={avatar.name}image={avatar.image}size={size}/></div>))}{remaining>0&&<div className={`flex items-center justify-center rounded-full bg-muted text-muted-foreground ring-2 ring-background ${size==='sm'?'w-8 h-8 text-xs':size==='md'?'w-10 h-10 text-sm':'w-12 h-12 text-base'}`}>+{remaining}</div>}</div>);}
