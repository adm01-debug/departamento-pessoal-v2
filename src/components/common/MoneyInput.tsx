import React,{useState,useEffect}from'react';import{Input}from'@/components/ui/input';import{cn}from'@/lib/utils';interface Props{value?:number;onChange:(value:number)=>void;placeholder?:string;disabled?:boolean;className?:string;}export function MoneyInput({value,onChange,placeholder='R$ 0,00',disabled,className}:Props){const[display,setDisplay]=useState('');useEffect(()=>{if(value!==undefined){setDisplay(formatCurrency(value));}},[value]);const formatCurrency=(v:number)=>{return new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(v);};const handleChange=(e:React.ChangeEvent<HTMLInputElement>)=>{const raw=e.target.value.replace(/\D/g,'');const num=parseInt(raw||'0',10)/100;setDisplay(formatCurrency(num));onChange(num);};return(<Input value={display}onChange={handleChange}placeholder={placeholder}disabled={disabled}className={cn('text-right',className)}/>);}
