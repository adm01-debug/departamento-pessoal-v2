import React from'react';import{Input}from'@/components/ui/input';import{Clock}from'lucide-react';interface Props{value:string;onChange:(value:string)=>void;disabled?:boolean;placeholder?:string;}export function TimePicker({value,onChange,disabled,placeholder}:Props){return(<div className="relative"><Input type="time"value={value}onChange={e=>onChange(e.target.value)}disabled={disabled}placeholder={placeholder}className="pr-10"/><Clock className="absolute right-3 top-1/2 -translate-y-1/2 w-4 h-4 text-muted-foreground pointer-events-none"/></div>);}
