import React from'react';import{Button}from'@/components/ui/button';import{Minus,Plus}from'lucide-react';interface Props{value:number;onChange:(value:number)=>void;min?:number;max?:number;step?:number;disabled?:boolean;}export function Counter({value,onChange,min=0,max=Infinity,step=1,disabled}:Props){const decrease=()=>{const newValue=value-step;if(newValue>=min)onChange(newValue);};const increase=()=>{const newValue=value+step;if(newValue<=max)onChange(newValue);};return(<div className="flex items-center gap-2"><Button type="button"variant="outline"size="icon"onClick={decrease}disabled={disabled||value<=min}><Minus className="w-4 h-4"/></Button><span className="w-12 text-center font-medium">{value}</span><Button type="button"variant="outline"size="icon"onClick={increase}disabled={disabled||value>=max}><Plus className="w-4 h-4"/></Button></div>);}
