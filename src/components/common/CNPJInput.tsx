import React,{useState}from'react';import{Input}from'@/components/ui/input';import{maskCNPJ}from'@/utils/masks';interface Props{value?:string;onChange:(value:string)=>void;placeholder?:string;disabled?:boolean;className?:string;}export function CNPJInput({value='',onChange,placeholder='00.000.000/0000-00',disabled,className}:Props){const[display,setDisplay]=useState(maskCNPJ(value));const handleChange=(e:React.ChangeEvent<HTMLInputElement>)=>{const masked=maskCNPJ(e.target.value);setDisplay(masked);onChange(masked.replace(/\D/g,''));};return(<Input value={display}onChange={handleChange}placeholder={placeholder}disabled={disabled}className={className}maxLength={18}/>);}
