import { describe, it, expect } from 'vitest';
import { DocumentoCard } from '../DocumentoCard';
describe('DocumentoCard', () => { it('should be defined', () => { expect(DocumentoCard).toBeDefined(); }); });
