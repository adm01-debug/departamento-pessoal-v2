import { describe, it, expect } from 'vitest';
import { DocumentoFilters } from '../DocumentoFilters';
describe('DocumentoFilters', () => { it('defined', () => { expect(DocumentoFilters).toBeDefined(); }); });
