import { describe, it, expect } from 'vitest';
import { AssinaturaCanvas } from '../AssinaturaCanvas';
describe('AssinaturaCanvas', () => { it('defined', () => { expect(AssinaturaCanvas).toBeDefined(); }); });
