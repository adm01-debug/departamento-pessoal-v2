import { describe, it, expect } from 'vitest';
import { AssinaturaStatus } from '../AssinaturaStatus';
describe('AssinaturaStatus', () => { it('defined', () => { expect(AssinaturaStatus).toBeDefined(); }); });
