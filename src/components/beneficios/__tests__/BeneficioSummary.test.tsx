import { describe, it, expect } from 'vitest';
import { BeneficioSummary } from '../BeneficioSummary';
describe('BeneficioSummary', () => { it('defined', () => { expect(BeneficioSummary).toBeDefined(); }); });
