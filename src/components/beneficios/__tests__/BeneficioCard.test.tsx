import { describe, it, expect } from 'vitest';
import { BeneficioCard } from '../BeneficioCard';
describe('BeneficioCard', () => { it('should be defined', () => { expect(BeneficioCard).toBeDefined(); }); });
