import { describe, it, expect } from 'vitest';
import { BeneficioDetails } from '../BeneficioDetails';
describe('BeneficioDetails', () => { it('defined', () => { expect(BeneficioDetails).toBeDefined(); }); });
