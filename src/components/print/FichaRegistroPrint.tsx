import React from "react";
interface FichaRegistroData { empresa: { nome: string; cnpj: string; endereco: string }; colaborador: { nome: string; cpf: string; rg: string; dataNascimento: string; naturalidade: string; estadoCivil: string; escolaridade: string; endereco: string; telefone: string; email: string; pis: string; ctps: string; tituloEleitor: string; reservista: string }; contrato: { dataAdmissao: string; cargo: string; departamento: string; salario: number; cargaHoraria: string; tipoContrato: string }; dependentes: { nome: string; parentesco: string; dataNascimento: string }[]; }
export function FichaRegistroPrint({ data }: { data: FichaRegistroData }) {
  return (<div className="p-8 max-w-4xl mx-auto bg-white text-sm print:p-4"><div className="text-center border-b pb-4 mb-4"><h1 className="text-xl font-bold">{data.empresa.nome}</h1><p>CNPJ: {data.empresa.cnpj}</p><h2 className="text-lg mt-2">FICHA DE REGISTRO DE EMPREGADO</h2></div><section className="mb-4"><h3 className="font-bold bg-gray-100 p-2">DADOS PESSOAIS</h3><div className="grid grid-cols-3 gap-2 p-2">{Object.entries(data.colaborador).map(([k, v]) => <div key={k}><span className="text-gray-500 text-xs">{k.toUpperCase()}:</span><br/>{v}</div>)}</div></section><section className="mb-4"><h3 className="font-bold bg-gray-100 p-2">DADOS DO CONTRATO</h3><div className="grid grid-cols-3 gap-2 p-2">{Object.entries(data.contrato).map(([k, v]) => <div key={k}><span className="text-gray-500 text-xs">{k.toUpperCase()}:</span><br/>{typeof v === "number" ? `R$ ${v.toFixed(2)}` : v}</div>)}</div></section>{data.dependentes.length > 0 && <section className="mb-4"><h3 className="font-bold bg-gray-100 p-2">DEPENDENTES</h3><table className="w-full"><thead><tr className="border-b"><th className="text-left p-2">Nome</th><th className="text-left p-2">Parentesco</th><th className="text-left p-2">Data Nasc.</th></tr></thead><tbody>{data.dependentes.map((d, i) => <tr key={i} className="border-b"><td className="p-2">{d.nome}</td><td className="p-2">{d.parentesco}</td><td className="p-2">{d.dataNascimento}</td></tr>)}</tbody></table></section>}<div className="mt-8 grid grid-cols-2 gap-8"><div className="text-center border-t border-black pt-2">Assinatura do Empregador</div><div className="text-center border-t border-black pt-2">Assinatura do Empregado</div></div></div>);
}
export default FichaRegistroPrint;
