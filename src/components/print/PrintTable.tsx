import React from'react';interface Column{key:string;header:string;align?:'left'|'center'|'right';}interface Props{columns:Column[];data:any[];}export function PrintTable({columns,data}:Props){return(<table className="w-full border-collapse"><thead><tr className="border-b-2 border-black">{columns.map(col=>(<th key={col.key}className={`p-2 text-${col.align||'left'}`}>{col.header}</th>))}</tr></thead><tbody>{data.map((row,i)=>(<tr key={i}className="border-b">{columns.map(col=>(<td key={col.key}className={`p-2 text-${col.align||'left'}`}>{row[col.key]}</td>))}</tr>))}</tbody></table>);}
