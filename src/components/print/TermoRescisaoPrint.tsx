import React from "react";
interface TermoRescisaoData { empresa: { nome: string; cnpj: string }; colaborador: { nome: string; cpf: string; ctps: string; pis: string; cargo: string; admissao: string; demissao: string }; verbas: { descricao: string; valor: number; tipo: "P" | "D" }[]; totais: { proventos: number; descontos: number; liquido: number }; tipoRescisao: string; motivoRescisao: string; }
export function TermoRescisaoPrint({ data }: { data: TermoRescisaoData }) {
  return (<div className="p-8 max-w-4xl mx-auto bg-white text-sm print:p-4"><div className="text-center border-b pb-4 mb-4"><h1 className="text-xl font-bold">TERMO DE RESCISÃO DO CONTRATO DE TRABALHO</h1><p className="text-xs mt-2">Lei nº 13.467/2017 - CLT</p></div><section className="mb-4 p-3 bg-gray-50 rounded"><h3 className="font-bold mb-2">IDENTIFICAÇÃO DO EMPREGADOR</h3><div className="grid grid-cols-2 gap-2"><div>Razão Social: <strong>{data.empresa.nome}</strong></div><div>CNPJ: {data.empresa.cnpj}</div></div></section><section className="mb-4 p-3 bg-gray-50 rounded"><h3 className="font-bold mb-2">IDENTIFICAÇÃO DO TRABALHADOR</h3><div className="grid grid-cols-3 gap-2"><div>Nome: <strong>{data.colaborador.nome}</strong></div><div>CPF: {data.colaborador.cpf}</div><div>PIS: {data.colaborador.pis}</div><div>CTPS: {data.colaborador.ctps}</div><div>Cargo: {data.colaborador.cargo}</div><div>Admissão: {data.colaborador.admissao}</div></div></section><section className="mb-4 p-3 bg-blue-50 rounded"><div className="grid grid-cols-3 gap-2"><div>Data Demissão: <strong>{data.colaborador.demissao}</strong></div><div>Tipo: <strong>{data.tipoRescisao}</strong></div><div>Motivo: {data.motivoRescisao}</div></div></section><table className="w-full mb-4 border"><thead className="bg-gray-100"><tr><th className="text-left p-2 border">Descrição</th><th className="text-right p-2 border">Proventos</th><th className="text-right p-2 border">Descontos</th></tr></thead><tbody>{data.verbas.map((v, i) => <tr key={i}><td className="p-2 border">{v.descricao}</td><td className="text-right p-2 border">{v.tipo === "P" ? v.valor.toFixed(2) : ""}</td><td className="text-right p-2 border">{v.tipo === "D" ? v.valor.toFixed(2) : ""}</td></tr>)}<tr className="font-bold bg-gray-100"><td className="p-2 border">TOTAIS</td><td className="text-right p-2 border">{data.totais.proventos.toFixed(2)}</td><td className="text-right p-2 border">{data.totais.descontos.toFixed(2)}</td></tr><tr className="font-bold bg-gray-900 text-white"><td className="p-2 border" colSpan={2}>VALOR LÍQUIDO</td><td className="text-right p-2 border">R$ {data.totais.liquido.toFixed(2)}</td></tr></tbody></table><div className="mt-8 text-xs"><p>Recebi a importância líquida acima especificada, dando plena e irrevogável quitação.</p><div className="mt-8 grid grid-cols-3 gap-4"><div className="text-center border-t border-black pt-2">Empregador</div><div className="text-center border-t border-black pt-2">Empregado</div><div className="text-center border-t border-black pt-2">Homologador</div></div></div></div>);
}
export default TermoRescisaoPrint;
