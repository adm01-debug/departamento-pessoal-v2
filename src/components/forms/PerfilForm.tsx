import React from'react';import{useForm}from'react-hook-form';import{zodResolver}from'@hookform/resolvers/zod';import{z}from'zod';import{Button}from'@/components/ui/button';import{Input}from'@/components/ui/input';import{Label}from'@/components/ui/label';import{Checkbox}from'@/components/ui/checkbox';const schema=z.object({nome:z.string().min(1),descricao:z.string().optional(),permissoes:z.array(z.string())});type FormData=z.infer<typeof schema>;const modulos=[{id:'colaboradores',label:'Colaboradores'},{id:'folha',label:'Folha de Pagamento'},{id:'ferias',label:'Férias'},{id:'ponto',label:'Ponto'},{id:'beneficios',label:'Benefícios'},{id:'esocial',label:'eSocial'},{id:'relatorios',label:'Relatórios'},{id:'configuracoes',label:'Configurações'}];interface Props{defaultValues?:Partial<FormData>;onSubmit:(data:FormData)=>void;onCancel?:()=>void;}export function PerfilForm({defaultValues,onSubmit,onCancel}:Props){const{register,handleSubmit,formState:{errors},setValue,watch}=useForm<FormData>({resolver:zodResolver(schema),defaultValues:{permissoes:[],...defaultValues}});const permissoes=watch('permissoes')||[];const togglePerm=(id:string)=>{const novo=permissoes.includes(id)?permissoes.filter(p=>p!==id):[...permissoes,id];setValue('permissoes',novo);};return(<form onSubmit={handleSubmit(onSubmit)}className="space-y-4"><div><Label>Nome do Perfil</Label><Input{...register('nome')}/></div><div><Label>Descrição</Label><Input{...register('descricao')}/></div><div><Label>Permissões</Label><div className="grid grid-cols-2 gap-2 mt-2">{modulos.map(m=>(<div key={m.id}className="flex items-center gap-2"><Checkbox checked={permissoes.includes(m.id)}onCheckedChange={()=>togglePerm(m.id)}/><span>{m.label}</span></div>))}</div></div><div className="flex gap-2 justify-end">{onCancel&&<Button type="button"variant="outline"onClick={onCancel}>Cancelar</Button>}<Button type="submit">Salvar</Button></div></form>);}
