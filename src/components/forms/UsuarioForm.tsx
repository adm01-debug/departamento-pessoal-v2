import React from'react';import{useForm}from'react-hook-form';import{zodResolver}from'@hookform/resolvers/zod';import{usuarioSchema}from'@/schemas/usuario';import{Button}from'@/components/ui/button';import{Input}from'@/components/ui/input';import{Label}from'@/components/ui/label';import{Select,SelectContent,SelectItem,SelectTrigger,SelectValue}from'@/components/ui/select';import{Switch}from'@/components/ui/switch';interface Props{defaultValues?:any;perfis:{id:string;nome:string}[];isEdit?:boolean;onSubmit:(data:any)=>void;onCancel:()=>void;}export function UsuarioForm({defaultValues,perfis,isEdit,onSubmit,onCancel}:Props){const{register,handleSubmit,setValue,watch,formState:{errors}}=useForm({resolver:zodResolver(usuarioSchema),defaultValues:{ativo:true,...defaultValues}});const ativo=watch('ativo');return(<form onSubmit={handleSubmit(onSubmit)}className="space-y-4"><div><Label>Nome*</Label><Input{...register('nome')}/></div><div><Label>Email*</Label><Input type="email"{...register('email')}/></div><div><Label>Perfil*</Label><Select onValueChange={v=>setValue('perfilId',v)}defaultValue={defaultValues?.perfilId}><SelectTrigger><SelectValue placeholder="Selecione"/></SelectTrigger><SelectContent>{perfis.map(p=><SelectItem key={p.id}value={p.id}>{p.nome}</SelectItem>)}</SelectContent></Select></div>{!isEdit&&<div><Label>Senha*</Label><Input type="password"{...register('senha')}/></div>}<div className="flex items-center gap-2"><Switch checked={ativo}onCheckedChange={v=>setValue('ativo',v)}/><Label>Ativo</Label></div><div className="flex justify-end gap-2"><Button type="button"variant="outline"onClick={onCancel}>Cancelar</Button><Button type="submit">Salvar</Button></div></form>);}
