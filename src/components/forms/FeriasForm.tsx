import React from'react';import{useForm}from'react-hook-form';import{zodResolver}from'@hookform/resolvers/zod';import{feriasSchema,FeriasFormData}from'@/schemas/ferias';import{Button}from'@/components/ui/button';import{Input}from'@/components/ui/input';import{Label}from'@/components/ui/label';import{Select,SelectContent,SelectItem,SelectTrigger,SelectValue}from'@/components/ui/select';import{Checkbox}from'@/components/ui/checkbox';interface Props{defaultValues?:Partial<FeriasFormData>;colaboradores:{id:string;nome:string}[];onSubmit:(data:FeriasFormData)=>void;onCancel:()=>void;}export function FeriasForm({defaultValues,colaboradores,onSubmit,onCancel}:Props){const{register,handleSubmit,setValue,watch,formState:{errors}}=useForm<FeriasFormData>({resolver:zodResolver(feriasSchema),defaultValues:{abonoPecuniario:false,...defaultValues}});const abono=watch('abonoPecuniario');return(<form onSubmit={handleSubmit(onSubmit)}className="space-y-4"><div><Label>Colaborador*</Label><Select onValueChange={v=>setValue('colaboradorId',v)}defaultValue={defaultValues?.colaboradorId}><SelectTrigger><SelectValue placeholder="Selecione"/></SelectTrigger><SelectContent>{colaboradores.map(c=><SelectItem key={c.id}value={c.id}>{c.nome}</SelectItem>)}</SelectContent></Select>{errors.colaboradorId&&<span className="text-sm text-red-500">{errors.colaboradorId.message}</span>}</div><div className="grid grid-cols-2 gap-4"><div><Label>Data Início*</Label><Input type="date"{...register('dataInicio')}/></div><div><Label>Data Fim*</Label><Input type="date"{...register('dataFim')}/></div></div><div><Label>Dias*</Label><Input type="number"{...register('dias',{valueAsNumber:true})}/>{errors.dias&&<span className="text-sm text-red-500">{errors.dias.message}</span>}</div><div className="flex items-center gap-2"><Checkbox id="abono"checked={abono}onCheckedChange={v=>setValue('abonoPecuniario',!!v)}/><Label htmlFor="abono">Abono pecuniário (venda de dias)</Label></div>{abono&&<div><Label>Dias de abono</Label><Input type="number"{...register('diasAbono',{valueAsNumber:true})}/></div>}<div className="flex justify-end gap-2"><Button type="button"variant="outline"onClick={onCancel}>Cancelar</Button><Button type="submit">Salvar</Button></div></form>);}
