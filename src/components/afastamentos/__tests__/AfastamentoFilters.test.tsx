import { describe, it, expect } from 'vitest';
import { AfastamentoFilters } from '../AfastamentoFilters';
describe('AfastamentoFilters', () => { it('defined', () => { expect(AfastamentoFilters).toBeDefined(); }); });
