import { describe, it, expect } from 'vitest';
import { AfastamentoList } from '../AfastamentoList';
describe('AfastamentoList', () => { it('defined', () => { expect(AfastamentoList).toBeDefined(); }); });
