import React from'react';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{Separator}from'@/components/ui/separator';import{formatCurrency}from'@/utils/formatters';interface Item{descricao:string;referencia?:string;valor:number;}interface Props{colaborador:{nome:string;cargo:string;cpf:string;};competencia:string;proventos:Item[];descontos:Item[];}export function HoleritePreview({colaborador,competencia,proventos,descontos}:Props){const totalProventos=proventos.reduce((s,p)=>s+p.valor,0);const totalDescontos=descontos.reduce((s,d)=>s+d.valor,0);const liquido=totalProventos-totalDescontos;return(<Card><CardHeader><CardTitle>Holerite - {competencia}</CardTitle><div className="text-sm text-muted-foreground"><p>{colaborador.nome}</p><p>{colaborador.cargo} | CPF: {colaborador.cpf}</p></div></CardHeader><CardContent><div className="space-y-4"><div><h4 className="font-medium mb-2">Proventos</h4>{proventos.map((p,i)=>(<div key={i}className="flex justify-between text-sm"><span>{p.descricao}</span><span className="text-green-600">{formatCurrency(p.valor)}</span></div>))}<div className="flex justify-between font-medium mt-2"><span>Total Proventos</span><span className="text-green-600">{formatCurrency(totalProventos)}</span></div></div><Separator/><div><h4 className="font-medium mb-2">Descontos</h4>{descontos.map((d,i)=>(<div key={i}className="flex justify-between text-sm"><span>{d.descricao}</span><span className="text-red-600">-{formatCurrency(d.valor)}</span></div>))}<div className="flex justify-between font-medium mt-2"><span>Total Descontos</span><span className="text-red-600">-{formatCurrency(totalDescontos)}</span></div></div><Separator/><div className="flex justify-between text-lg font-bold"><span>Líquido a Receber</span><span className="text-blue-600">{formatCurrency(liquido)}</span></div></div></CardContent></Card>);}
