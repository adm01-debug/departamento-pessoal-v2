import React from'react';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{BarChart}from'@/components/charts/BarChart';interface Props{data:{competencia:string;bruto:number;liquido:number;}[];}export function FolhaChart({data}:Props){const chartData=data.map(d=>({name:d.competencia,Bruto:d.bruto,Líquido:d.liquido}));return(<Card><CardHeader><CardTitle>Evolução da Folha</CardTitle></CardHeader><CardContent><BarChart data={chartData}dataKey="Bruto"xAxisKey="name"/></CardContent></Card>);}
