import React from'react';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{formatCurrency}from'@/utils/formatters';interface Props{resumo:{totalColaboradores:number;totalProventos:number;totalDescontos:number;totalLiquido:number;totalFGTS:number;totalINSS:number;totalIRRF:number};}export function ResumoFolha({resumo}:Props){const items=[{label:'Colaboradores',value:resumo.totalColaboradores,format:'number'},{label:'Total Proventos',value:resumo.totalProventos,color:'text-green-600'},{label:'Total Descontos',value:resumo.totalDescontos,color:'text-red-600'},{label:'Total Líquido',value:resumo.totalLiquido,color:'text-blue-600 font-bold'},{label:'FGTS',value:resumo.totalFGTS},{label:'INSS',value:resumo.totalINSS},{label:'IRRF',value:resumo.totalIRRF}];return(<Card><CardHeader><CardTitle>Resumo da Folha</CardTitle></CardHeader><CardContent><div className="grid grid-cols-2 md:grid-cols-4 gap-4">{items.map(item=>(<div key={item.label}className="text-center p-3 bg-muted rounded-lg"><p className="text-sm text-muted-foreground">{item.label}</p><p className={`text-lg font-semibold ${item.color||''}`}>{item.format==='number'?item.value:formatCurrency(item.value)}</p></div>))}</div></CardContent></Card>);}
