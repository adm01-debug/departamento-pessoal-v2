import React from'react';import{Card,CardContent}from'@/components/ui/card';import{Badge}from'@/components/ui/badge';import{Button}from'@/components/ui/button';import{Avatar}from'@/components/common/Avatar';import{formatDate}from'@/utils/formatters';import{Edit,Trash2,Key}from'lucide-react';interface Props{usuario:{id:string;nome:string;email:string;perfil:string;ultimoAcesso?:string;ativo:boolean};onEdit?:(id:string)=>void;onDelete?:(id:string)=>void;onResetSenha?:(id:string)=>void;}export function UsuarioCard({usuario,onEdit,onDelete,onResetSenha}:Props){return(<Card><CardContent className="pt-4"><div className="flex items-center gap-3"><Avatar name={usuario.nome}size="md"/><div className="flex-1"><div className="flex justify-between items-start"><div><h4 className="font-medium">{usuario.nome}</h4><p className="text-sm text-muted-foreground">{usuario.email}</p></div><Badge variant={usuario.ativo?'default':'secondary'}>{usuario.ativo?'Ativo':'Inativo'}</Badge></div><p className="text-xs text-muted-foreground mt-1">Perfil: {usuario.perfil}</p>{usuario.ultimoAcesso&&<p className="text-xs text-muted-foreground">Último acesso: {formatDate(usuario.ultimoAcesso)}</p>}</div></div><div className="flex gap-2 mt-4 justify-end">{onResetSenha&&<Button size="sm"variant="outline"onClick={()=>onResetSenha(usuario.id)}><Key className="w-4 h-4 mr-1"/>Reset Senha</Button>}{onEdit&&<Button size="sm"variant="outline"onClick={()=>onEdit(usuario.id)}><Edit className="w-4 h-4"/></Button>}{onDelete&&<Button size="sm"variant="ghost"onClick={()=>onDelete(usuario.id)}><Trash2 className="w-4 h-4 text-red-500"/></Button>}</div></CardContent></Card>);}
