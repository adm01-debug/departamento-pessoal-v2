import { describe, it, expect } from 'vitest';
import { NovoUsuarioModal } from '../NovoUsuarioModal';
describe('NovoUsuarioModal', () => { it('should be defined', () => { expect(NovoUsuarioModal).toBeDefined(); }); });
