import React from "react";import { Button } from "@/components/ui/button";import { LayoutGrid, List } from "lucide-react";interface ToggleViewButtonProps { view: "grid" | "list"; onClick: () => void; className?: string; }export function ToggleViewButton({ view, onClick, className }: ToggleViewButtonProps) { return (<Button variant="ghost" size="icon" onClick={onClick} className={className}>{view === "grid" ? <List className="h-4 w-4" /> : <LayoutGrid className="h-4 w-4" />}</Button>); } export default ToggleViewButton;
