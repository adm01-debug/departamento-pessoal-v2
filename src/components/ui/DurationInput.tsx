import React from "react";import { cn } from "@/lib/utils";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";interface DurationInputProps { value: { hours: number; minutes: number }; onChange: (value: { hours: number; minutes: number }) => void; className?: string; }export function DurationInput({ value, onChange, className }: DurationInputProps) { return (<div className={cn("flex items-center gap-2", className)}><div className="flex items-center gap-1"><Input type="number" value={value.hours} onChange={(e) => onChange({ ...value, hours: Number(e.target.value) })} className="w-16" min={0} /><span className="text-sm text-muted-foreground">h</span></div><div className="flex items-center gap-1"><Input type="number" value={value.minutes} onChange={(e) => onChange({ ...value, minutes: Number(e.target.value) })} className="w-16" min={0} max={59} /><span className="text-sm text-muted-foreground">min</span></div></div>); } export default DurationInput;
