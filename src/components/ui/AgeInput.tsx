import React from "react";import { cn } from "@/lib/utils";import { Input } from "@/components/ui/input";interface AgeInputProps { value: number; onChange: (value: number) => void; className?: string; }export function AgeInput({ value, onChange, className }: AgeInputProps) { return (<div className={cn("relative", className)}><Input type="number" value={value} onChange={(e) => onChange(Number(e.target.value))} min={0} max={150} className="pr-12" /><span className="absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground text-sm">anos</span></div>); } export default AgeInput;
