import React from "react";import { Button } from "@/components/ui/button";import { Check } from "lucide-react";interface ConfirmButtonProps { onClick: () => void; label?: string; className?: string; }export function ConfirmButton({ onClick, label = "Confirmar", className }: ConfirmButtonProps) { return (<Button onClick={onClick} className={className}><Check className="h-4 w-4 mr-2" />{label}</Button>); } export default ConfirmButton;
