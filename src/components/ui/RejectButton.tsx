import React from "react";import { Button } from "@/components/ui/button";import { X } from "lucide-react";interface RejectButtonProps { onClick: () => void; label?: string; className?: string; }export function RejectButton({ onClick, label = "Rejeitar", className }: RejectButtonProps) { return (<Button variant="destructive" onClick={onClick} className={className}><X className="h-4 w-4 mr-2" />{label}</Button>); } export default RejectButton;
