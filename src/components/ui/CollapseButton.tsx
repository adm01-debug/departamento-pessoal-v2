import React from "react";import { Button } from "@/components/ui/button";import { ChevronDown, ChevronUp } from "lucide-react";interface CollapseButtonProps { collapsed: boolean; onClick: () => void; className?: string; }export function CollapseButton({ collapsed, onClick, className }: CollapseButtonProps) { return (<Button variant="ghost" size="icon" onClick={onClick} className={className}>{collapsed ? <ChevronDown className="h-4 w-4" /> : <ChevronUp className="h-4 w-4" />}</Button>); } export default CollapseButton;
