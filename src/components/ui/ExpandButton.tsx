import React from "react";import { Button } from "@/components/ui/button";import { Maximize2, Minimize2 } from "lucide-react";interface ExpandButtonProps { expanded: boolean; onClick: () => void; className?: string; }export function ExpandButton({ expanded, onClick, className }: ExpandButtonProps) { return (<Button variant="ghost" size="icon" onClick={onClick} className={className}>{expanded ? <Minimize2 className="h-4 w-4" /> : <Maximize2 className="h-4 w-4" />}</Button>); } export default ExpandButton;
