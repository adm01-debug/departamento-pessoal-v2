import React from "react";import { cn } from "@/lib/utils";import { Input } from "@/components/ui/input";interface WeightInputProps { value: number; onChange: (value: number) => void; unit?: "kg" | "g"; className?: string; }export function WeightInput({ value, onChange, unit = "kg", className }: WeightInputProps) { return (<div className={cn("relative", className)}><Input type="number" value={value} onChange={(e) => onChange(Number(e.target.value))} min={0} step={0.1} className="pr-12" /><span className="absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground text-sm">{unit}</span></div>); } export default WeightInput;
