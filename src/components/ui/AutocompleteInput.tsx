import React from "react";import { cn } from "@/lib/utils";import { Input } from "@/components/ui/input";interface AutocompleteInputProps { value: string; onChange: (value: string) => void; suggestions: string[]; onSelect: (value: string) => void; placeholder?: string; className?: string; }export function AutocompleteInput({ value, onChange, suggestions, onSelect, placeholder, className }: AutocompleteInputProps) { const [show, setShow] = React.useState(false); const filtered = suggestions.filter((s) => s.toLowerCase().includes(value.toLowerCase())); return (<div className={cn("relative", className)}><Input value={value} onChange={(e) => { onChange(e.target.value); setShow(true); }} onBlur={() => setTimeout(() => setShow(false), 200)} placeholder={placeholder} />{show && filtered.length > 0 && (<div className="absolute z-10 w-full mt-1 bg-background border rounded-md shadow-lg max-h-48 overflow-y-auto">{filtered.map((s, i) => <button key={i} className="w-full p-2 text-left hover:bg-muted" onClick={() => { onSelect(s); setShow(false); }}>{s}</button>)}</div>)}</div>); } export default AutocompleteInput;
