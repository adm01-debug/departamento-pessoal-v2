import React from "react";import { Button } from "@/components/ui/button";import { Loader2 } from "lucide-react";interface SubmitButtonProps { loading?: boolean; className?: string; }export function SubmitButton({ loading, className }: SubmitButtonProps) { return (<Button type="submit" disabled={loading} className={className}>{loading && <Loader2 className="h-4 w-4 mr-2 animate-spin" />}Enviar</Button>); } export default SubmitButton;
