import React from "react";import { Button } from "@/components/ui/button";import { Filter } from "lucide-react";interface FilterButtonProps { onClick: () => void; active?: boolean; className?: string; }export function FilterButton({ onClick, active, className }: FilterButtonProps) { return (<Button variant={active ? "secondary" : "outline"} onClick={onClick} className={className}><Filter className="h-4 w-4 mr-2" />Filtros</Button>); } export default FilterButton;
