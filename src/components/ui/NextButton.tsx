import React from "react";import { Button } from "@/components/ui/button";import { ArrowRight } from "lucide-react";interface NextButtonProps { onClick: () => void; label?: string; disabled?: boolean; className?: string; }export function NextButton({ onClick, label = "Próximo", disabled, className }: NextButtonProps) { return (<Button onClick={onClick} disabled={disabled} className={className}>{label}<ArrowRight className="h-4 w-4 ml-2" /></Button>); } export default NextButton;
