import React from "react";import { cn } from "@/lib/utils";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";interface DocumentsFormProps { value: { rg: string; cpf: string; ctps: string; pis: string }; onChange: (v: any) => void; className?: string; }export function DocumentsForm({ value, onChange, className }: DocumentsFormProps) { const set = (k: string, v: string) => onChange({ ...value, [k]: v }); return (<div className={cn("grid grid-cols-2 gap-4", className)}><div><Label>RG</Label><Input value={value.rg} onChange={(e) => set("rg", e.target.value)} /></div><div><Label>CPF</Label><Input value={value.cpf} onChange={(e) => set("cpf", e.target.value)} /></div><div><Label>CTPS</Label><Input value={value.ctps} onChange={(e) => set("ctps", e.target.value)} /></div><div><Label>PIS</Label><Input value={value.pis} onChange={(e) => set("pis", e.target.value)} /></div></div>); } export default DocumentsForm;
