import React from "react";import { Button } from "@/components/ui/button";import { ArrowLeft } from "lucide-react";interface BackButtonProps { onClick: () => void; label?: string; className?: string; }export function BackButton({ onClick, label = "Voltar", className }: BackButtonProps) { return (<Button variant="ghost" onClick={onClick} className={className}><ArrowLeft className="h-4 w-4 mr-2" />{label}</Button>); } export default BackButton;
