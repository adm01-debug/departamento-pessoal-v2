import React from "react";import { cn } from "@/lib/utils";import { Calendar } from "@/components/ui/calendar";import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";import { ptBR } from "date-fns/locale";interface CalendarWidgetProps { selected?: Date; onSelect?: (date?: Date) => void; className?: string; }export function CalendarWidget({ selected, onSelect, className }: CalendarWidgetProps) { return (<Card className={className}><CardHeader><CardTitle className="text-lg">Calendário</CardTitle></CardHeader><CardContent><Calendar mode="single" selected={selected} onSelect={onSelect} locale={ptBR} /></CardContent></Card>); } export default CalendarWidget;
