import React from "react";import { cn } from "@/lib/utils";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";interface BankDataFormProps { value: { bank: string; agency: string; account: string; type: string }; onChange: (v: any) => void; className?: string; }export function BankDataForm({ value, onChange, className }: BankDataFormProps) { const set = (k: string, v: string) => onChange({ ...value, [k]: v }); return (<div className={cn("grid grid-cols-2 gap-4", className)}><div><Label>Banco</Label><Input value={value.bank} onChange={(e) => set("bank", e.target.value)} /></div><div><Label>Agência</Label><Input value={value.agency} onChange={(e) => set("agency", e.target.value)} /></div><div><Label>Conta</Label><Input value={value.account} onChange={(e) => set("account", e.target.value)} /></div><div><Label>Tipo</Label><Select value={value.type} onValueChange={(v) => set("type", v)}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="corrente">Corrente</SelectItem><SelectItem value="poupanca">Poupança</SelectItem></SelectContent></Select></div></div>); } export default BankDataForm;
