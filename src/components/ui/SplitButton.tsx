import React from "react";import { Button } from "@/components/ui/button";import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from "@/components/ui/dropdown-menu";import { ChevronDown } from "lucide-react";interface SplitButtonProps { label: string; onClick: () => void; options: { label: string; onClick: () => void }[]; className?: string; }export function SplitButton({ label, onClick, options, className }: SplitButtonProps) { return (<div className={`flex ${className}`}><Button onClick={onClick} className="rounded-r-none">{label}</Button><DropdownMenu><DropdownMenuTrigger asChild><Button className="rounded-l-none px-2 border-l"><ChevronDown className="h-4 w-4" /></Button></DropdownMenuTrigger><DropdownMenuContent>{options.map((opt, i) => <DropdownMenuItem key={i} onClick={opt.onClick}>{opt.label}</DropdownMenuItem>)}</DropdownMenuContent></DropdownMenu></div>); } export default SplitButton;
