import React from "react";import { Button } from "@/components/ui/button";import { RotateCcw } from "lucide-react";interface ResetButtonProps { onClick: () => void; label?: string; className?: string; }export function ResetButton({ onClick, label = "Resetar", className }: ResetButtonProps) { return (<Button variant="outline" onClick={onClick} className={className}><RotateCcw className="h-4 w-4 mr-2" />{label}</Button>); } export default ResetButton;
