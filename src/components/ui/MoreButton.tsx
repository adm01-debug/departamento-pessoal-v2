import React from "react";import { Button } from "@/components/ui/button";import { MoreHorizontal } from "lucide-react";interface MoreButtonProps { onClick: () => void; className?: string; }export function MoreButton({ onClick, className }: MoreButtonProps) { return (<Button variant="ghost" size="icon" onClick={onClick} className={className}><MoreHorizontal className="h-4 w-4" /></Button>); } export default MoreButton;
