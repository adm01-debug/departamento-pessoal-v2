import React from "react";import { cn } from "@/lib/utils";import { Input } from "@/components/ui/input";interface HourlyRateInputProps { value: number; onChange: (value: number) => void; className?: string; }export function HourlyRateInput({ value, onChange, className }: HourlyRateInputProps) { return (<div className={cn("relative", className)}><span className="absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground">R$</span><Input type="number" value={value} onChange={(e) => onChange(Number(e.target.value))} min={0} step={0.01} className="pl-10 pr-12" /><span className="absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground text-sm">/hora</span></div>); } export default HourlyRateInput;
