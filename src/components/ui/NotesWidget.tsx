import React, { useState } from "react";import { cn } from "@/lib/utils";import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";import { Textarea } from "@/components/ui/textarea";interface NotesWidgetProps { defaultValue?: string; onSave?: (value: string) => void; className?: string; }export function NotesWidget({ defaultValue = "", onSave, className }: NotesWidgetProps) { const [value, setValue] = useState(defaultValue); return (<Card className={className}><CardHeader><CardTitle className="text-lg">Notas</CardTitle></CardHeader><CardContent><Textarea value={value} onChange={(e) => setValue(e.target.value)} onBlur={() => onSave?.(value)} placeholder="Escreva suas notas..." rows={4} /></CardContent></Card>); } export default NotesWidget;
