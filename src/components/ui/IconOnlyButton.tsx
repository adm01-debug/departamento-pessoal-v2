import React from "react";import { Button } from "@/components/ui/button";import { LucideIcon } from "lucide-react";interface IconOnlyButtonProps { icon: LucideIcon; onClick?: () => void; variant?: "default" | "outline" | "ghost"; className?: string; }export function IconOnlyButton({ icon: Icon, onClick, variant = "ghost", className }: IconOnlyButtonProps) { return (<Button variant={variant} size="icon" onClick={onClick} className={className}><Icon className="h-4 w-4" /></Button>); } export default IconOnlyButton;
