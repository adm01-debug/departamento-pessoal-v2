import React from "react";import { cn } from "@/lib/utils";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";interface PersonalDataFormProps { value: { name: string; birthDate: string; gender: string; maritalStatus: string }; onChange: (v: any) => void; className?: string; }export function PersonalDataForm({ value, onChange, className }: PersonalDataFormProps) { const set = (k: string, v: string) => onChange({ ...value, [k]: v }); return (<div className={cn("grid grid-cols-2 gap-4", className)}><div className="col-span-2"><Label>Nome Completo</Label><Input value={value.name} onChange={(e) => set("name", e.target.value)} /></div><div><Label>Data Nascimento</Label><Input type="date" value={value.birthDate} onChange={(e) => set("birthDate", e.target.value)} /></div><div><Label>Sexo</Label><Input value={value.gender} onChange={(e) => set("gender", e.target.value)} /></div></div>); } export default PersonalDataForm;
