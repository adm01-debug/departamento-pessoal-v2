import React from "react";import { cn } from "@/lib/utils";import { Input } from "@/components/ui/input";interface HeightInputProps { value: number; onChange: (value: number) => void; unit?: "cm" | "m"; className?: string; }export function HeightInput({ value, onChange, unit = "cm", className }: HeightInputProps) { return (<div className={cn("relative", className)}><Input type="number" value={value} onChange={(e) => onChange(Number(e.target.value))} min={0} className="pr-12" /><span className="absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground text-sm">{unit}</span></div>); } export default HeightInput;
