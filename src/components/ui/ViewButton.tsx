import React from "react";import { Button } from "@/components/ui/button";import { Eye } from "lucide-react";interface ViewButtonProps { onClick: () => void; className?: string; }export function ViewButton({ onClick, className }: ViewButtonProps) { return (<Button variant="ghost" size="icon" onClick={onClick} className={className}><Eye className="h-4 w-4" /></Button>); } export default ViewButton;
