import React from "react";import { Button } from "@/components/ui/button";import { Maximize, Minimize } from "lucide-react";interface FullscreenButtonProps { fullscreen: boolean; onClick: () => void; className?: string; }export function FullscreenButton({ fullscreen, onClick, className }: FullscreenButtonProps) { return (<Button variant="ghost" size="icon" onClick={onClick} className={className}>{fullscreen ? <Minimize className="h-4 w-4" /> : <Maximize className="h-4 w-4" />}</Button>); } export default FullscreenButton;
