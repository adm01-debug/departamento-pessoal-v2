import React from "react";import { cn } from "@/lib/utils";import { Input } from "@/components/ui/input";import { Mail, CheckCircle, XCircle } from "lucide-react";interface EmailInputProps { value: string; onChange: (value: string) => void; showValidation?: boolean; className?: string; }export function EmailInput({ value, onChange, showValidation = true, className }: EmailInputProps) { const isValid = /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value); return (<div className={cn("relative", className)}><Mail className="absolute left-3 top-1/2 -translate-y-1/2 h-4 w-4 text-muted-foreground" /><Input type="email" value={value} onChange={(e) => onChange(e.target.value)} placeholder="email@exemplo.com" className="pl-10 pr-10" />{showValidation && value && (<span className="absolute right-3 top-1/2 -translate-y-1/2">{isValid ? <CheckCircle className="h-4 w-4 text-green-500" /> : <XCircle className="h-4 w-4 text-red-500" />}</span>)}</div>); } export default EmailInput;
