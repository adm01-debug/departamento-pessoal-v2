import React from "react";import { Button } from "@/components/ui/button";import { ArrowUpDown } from "lucide-react";interface SortButtonProps { onClick: () => void; className?: string; }export function SortButton({ onClick, className }: SortButtonProps) { return (<Button variant="ghost" size="icon" onClick={onClick} className={className}><ArrowUpDown className="h-4 w-4" /></Button>); } export default SortButton;
