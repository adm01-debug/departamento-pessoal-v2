import React from "react";import { Button } from "@/components/ui/button";import { Save, Loader2 } from "lucide-react";interface SaveButtonProps { onClick: () => void; loading?: boolean; className?: string; }export function SaveButton({ onClick, loading, className }: SaveButtonProps) { return (<Button onClick={onClick} disabled={loading} className={className}>{loading ? <Loader2 className="h-4 w-4 mr-2 animate-spin" /> : <Save className="h-4 w-4 mr-2" />}Salvar</Button>); } export default SaveButton;
