import React from "react";import { Button } from "@/components/ui/button";import { FileUp } from "lucide-react";interface ImportButtonProps { onClick: () => void; label?: string; className?: string; }export function ImportButton({ onClick, label = "Importar", className }: ImportButtonProps) { return (<Button variant="outline" onClick={onClick} className={className}><FileUp className="h-4 w-4 mr-2" />{label}</Button>); } export default ImportButton;
