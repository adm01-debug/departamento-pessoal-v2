import React from "react";import { cn } from "@/lib/utils";import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";import { LucideIcon } from "lucide-react";interface QuickLink { icon: LucideIcon; label: string; href: string; }interface QuickLinksWidgetProps { title?: string; links: QuickLink[]; onNavigate?: (href: string) => void; className?: string; }export function QuickLinksWidget({ title = "Links Rápidos", links, onNavigate, className }: QuickLinksWidgetProps) { return (<Card className={className}><CardHeader><CardTitle className="text-lg">{title}</CardTitle></CardHeader><CardContent className="grid grid-cols-2 gap-2">{links.map((link, i) => { const Icon = link.icon; return (<button key={i} className="flex items-center gap-2 p-2 rounded hover:bg-muted" onClick={() => onNavigate?.(link.href)}><Icon className="h-4 w-4" /><span className="text-sm">{link.label}</span></button>); })}</CardContent></Card>); } export default QuickLinksWidget;
