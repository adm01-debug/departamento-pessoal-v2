import React from "react";import { cn } from "@/lib/utils";import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";import { Progress } from "@/components/ui/progress";interface ProgressWidgetProps { title: string; current: number; total: number; className?: string; }export function ProgressWidget({ title, current, total, className }: ProgressWidgetProps) { const percent = Math.round((current / total) * 100); return (<Card className={className}><CardHeader><CardTitle className="text-lg">{title}</CardTitle></CardHeader><CardContent className="space-y-2"><Progress value={percent} /><div className="flex justify-between text-sm text-muted-foreground"><span>{current} de {total}</span><span>{percent}%</span></div></CardContent></Card>); } export default ProgressWidget;
