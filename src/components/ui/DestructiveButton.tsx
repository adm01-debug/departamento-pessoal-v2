import React from "react";import { Button } from "@/components/ui/button";interface DestructiveButtonProps { children: React.ReactNode; onClick?: () => void; className?: string; }export function DestructiveButton({ children, onClick, className }: DestructiveButtonProps) { return (<Button variant="destructive" onClick={onClick} className={className}>{children}</Button>); } export default DestructiveButton;
