import React from "react";import { cn } from "@/lib/utils";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";interface AddressFormProps { value: { cep: string; street: string; number: string; city: string; state: string }; onChange: (v: any) => void; className?: string; }export function AddressForm({ value, onChange, className }: AddressFormProps) { const set = (k: string, v: string) => onChange({ ...value, [k]: v }); return (<div className={cn("grid grid-cols-2 gap-4", className)}><div><Label>CEP</Label><Input value={value.cep} onChange={(e) => set("cep", e.target.value)} /></div><div><Label>Rua</Label><Input value={value.street} onChange={(e) => set("street", e.target.value)} /></div><div><Label>Número</Label><Input value={value.number} onChange={(e) => set("number", e.target.value)} /></div><div><Label>Cidade</Label><Input value={value.city} onChange={(e) => set("city", e.target.value)} /></div><div><Label>Estado</Label><Input value={value.state} onChange={(e) => set("state", e.target.value)} /></div></div>); } export default AddressForm;
