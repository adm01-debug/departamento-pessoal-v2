import React from "react";import { Button } from "@/components/ui/button";import { Plus } from "lucide-react";interface AddButtonProps { onClick: () => void; label?: string; className?: string; }export function AddButton({ onClick, label, className }: AddButtonProps) { return (<Button onClick={onClick} className={className}><Plus className="h-4 w-4" />{label && <span className="ml-2">{label}</span>}</Button>); } export default AddButton;
