import React from "react";import { Button } from "@/components/ui/button";import { ThumbsUp } from "lucide-react";interface ApproveButtonProps { onClick: () => void; label?: string; className?: string; }export function ApproveButton({ onClick, label = "Aprovar", className }: ApproveButtonProps) { return (<Button onClick={onClick} className={className}><ThumbsUp className="h-4 w-4 mr-2" />{label}</Button>); } export default ApproveButton;
