import React from "react";import { cn } from "@/lib/utils";import { Button } from "@/components/ui/button";import { ArrowRight } from "lucide-react";interface CTAButtonProps { children: React.ReactNode; onClick?: () => void; size?: "default" | "lg"; className?: string; }export function CTAButton({ children, onClick, size = "default", className }: CTAButtonProps) { return (<Button size={size} onClick={onClick} className={cn("gap-2", className)}>{children}<ArrowRight className="h-4 w-4" /></Button>); } export default CTAButton;
