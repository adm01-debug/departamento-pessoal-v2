import React from "react";import { cn } from "@/lib/utils";import { Card, CardContent } from "@/components/ui/card";import { Cloud, Sun, CloudRain } from "lucide-react";interface WeatherWidgetProps { temp: number; condition: "sunny" | "cloudy" | "rainy"; location: string; className?: string; }const icons = { sunny: Sun, cloudy: Cloud, rainy: CloudRain };export function WeatherWidget({ temp, condition, location, className }: WeatherWidgetProps) { const Icon = icons[condition]; return (<Card className={className}><CardContent className="pt-6 flex items-center gap-4"><Icon className="h-12 w-12 text-primary" /><div><p className="text-3xl font-bold">{temp}°C</p><p className="text-muted-foreground">{location}</p></div></CardContent></Card>); } export default WeatherWidget;
