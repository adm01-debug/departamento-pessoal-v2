import React from "react";import { Button } from "@/components/ui/button";import { CheckSquare } from "lucide-react";interface SelectAllButtonProps { onClick: () => void; selected?: boolean; className?: string; }export function SelectAllButton({ onClick, selected, className }: SelectAllButtonProps) { return (<Button variant="ghost" size="sm" onClick={onClick} className={className}><CheckSquare className="h-4 w-4 mr-2" />{selected ? "Desmarcar todos" : "Selecionar todos"}</Button>); } export default SelectAllButton;
