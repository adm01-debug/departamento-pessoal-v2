import React, { useState } from "react";import { cn } from "@/lib/utils";import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";import { Checkbox } from "@/components/ui/checkbox";import { Input } from "@/components/ui/input";import { Plus } from "lucide-react";interface TodoItem { id: string; text: string; done: boolean; }interface TodoWidgetProps { items?: TodoItem[]; onChange?: (items: TodoItem[]) => void; className?: string; }export function TodoWidget({ items: initial = [], onChange, className }: TodoWidgetProps) { const [items, setItems] = useState(initial); const [input, setInput] = useState(""); const add = () => { if (input.trim()) { const newItems = [...items, { id: Date.now().toString(), text: input, done: false }]; setItems(newItems); onChange?.(newItems); setInput(""); } }; const toggle = (id: string) => { const newItems = items.map((i) => i.id === id ? { ...i, done: !i.done } : i); setItems(newItems); onChange?.(newItems); }; return (<Card className={className}><CardHeader><CardTitle className="text-lg">Tarefas</CardTitle></CardHeader><CardContent className="space-y-3"><div className="flex gap-2"><Input value={input} onChange={(e) => setInput(e.target.value)} placeholder="Nova tarefa..." onKeyDown={(e) => e.key === "Enter" && add()} /><button onClick={add} className="p-2 hover:bg-muted rounded"><Plus className="h-4 w-4" /></button></div>{items.map((item) => (<div key={item.id} className="flex items-center gap-2"><Checkbox checked={item.done} onCheckedChange={() => toggle(item.id)} /><span className={cn("text-sm", item.done && "line-through text-muted-foreground")}>{item.text}</span></div>))}</CardContent></Card>); } export default TodoWidget;
