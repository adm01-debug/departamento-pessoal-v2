import React from "react";import { Button } from "@/components/ui/button";import { FileDown } from "lucide-react";interface ExportButtonProps { onClick: () => void; label?: string; className?: string; }export function ExportButton({ onClick, label = "Exportar", className }: ExportButtonProps) { return (<Button variant="outline" onClick={onClick} className={className}><FileDown className="h-4 w-4 mr-2" />{label}</Button>); } export default ExportButton;
