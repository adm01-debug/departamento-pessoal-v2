import React from "react";import { cn } from "@/lib/utils";import { Button } from "@/components/ui/button";interface GhostButtonProps { children: React.ReactNode; onClick?: () => void; className?: string; }export function GhostButton({ children, onClick, className }: GhostButtonProps) { return (<Button variant="ghost" onClick={onClick} className={className}>{children}</Button>); } export default GhostButton;
