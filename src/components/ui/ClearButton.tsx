import React from "react";import { Button } from "@/components/ui/button";import { XCircle } from "lucide-react";interface ClearButtonProps { onClick: () => void; label?: string; className?: string; }export function ClearButton({ onClick, label = "Limpar", className }: ClearButtonProps) { return (<Button variant="ghost" onClick={onClick} className={className}><XCircle className="h-4 w-4 mr-2" />{label}</Button>); } export default ClearButton;
