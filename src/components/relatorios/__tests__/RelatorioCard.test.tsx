import { describe, it, expect } from 'vitest';
import { RelatorioCard } from '../RelatorioCard';
describe('RelatorioCard', () => { it('should be defined', () => { expect(RelatorioCard).toBeDefined(); }); });
