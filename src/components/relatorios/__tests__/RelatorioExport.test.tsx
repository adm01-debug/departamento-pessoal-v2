import { describe, it, expect } from 'vitest';
import { RelatorioExport } from '../RelatorioExport';
describe('RelatorioExport', () => { it('should be defined', () => { expect(RelatorioExport).toBeDefined(); }); });
