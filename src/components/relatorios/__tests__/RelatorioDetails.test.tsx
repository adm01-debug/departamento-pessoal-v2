import { describe, it, expect } from 'vitest';
import { RelatorioDetails } from '../RelatorioDetails';
describe('RelatorioDetails', () => { it('defined', () => { expect(RelatorioDetails).toBeDefined(); }); });
