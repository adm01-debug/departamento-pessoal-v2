import { describe, it, expect } from 'vitest';
import { RelatorioSummary } from '../RelatorioSummary';
describe('RelatorioSummary', () => { it('defined', () => { expect(RelatorioSummary).toBeDefined(); }); });
