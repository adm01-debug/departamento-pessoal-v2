import { describe, it, expect } from 'vitest';
import { DesligamentoChecklist } from '../DesligamentoChecklist';
describe('DesligamentoChecklist', () => { it('should be defined', () => { expect(DesligamentoChecklist).toBeDefined(); }); });
