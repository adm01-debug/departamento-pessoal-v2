import { describe, it, expect } from 'vitest';
import { AssinaturaModal } from '../AssinaturaModal';
describe('AssinaturaModal', () => { it('should be defined', () => { expect(AssinaturaModal).toBeDefined(); }); });
