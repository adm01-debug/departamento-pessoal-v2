import { describe, it, expect } from 'vitest';
import { AssinaturaCanvas } from '../AssinaturaCanvas';
describe('AssinaturaCanvas', () => { it('should be defined', () => { expect(AssinaturaCanvas).toBeDefined(); }); });
