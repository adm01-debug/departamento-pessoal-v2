import React from'react';import{useQuery}from'@tanstack/react-query';import{Dialog,DialogContent,DialogHeader,DialogTitle}from'@/components/ui/dialog';import{UsuarioForm}from'@/components/forms/UsuarioForm';import{Usuario}from'@/types/usuario';import{api}from'@/lib/api';interface Props{open:boolean;onOpenChange:(open:boolean)=>void;usuario?:Usuario;onSubmit:(data:any)=>void;}export function UsuarioModal({open,onOpenChange,usuario,onSubmit}:Props){const{data:perfis=[]}=useQuery({queryKey:['perfis'],queryFn:async()=>(await api.get('/perfis')).data});const handleSubmit=(data:any)=>{onSubmit(data);onOpenChange(false);};return(<Dialog open={open}onOpenChange={onOpenChange}><DialogContent className="max-w-lg"><DialogHeader><DialogTitle>{usuario?'Editar Usuário':'Novo Usuário'}</DialogTitle></DialogHeader><UsuarioForm defaultValues={usuario}perfis={perfis}isEdit={!!usuario}onSubmit={handleSubmit}onCancel={()=>onOpenChange(false)}/></DialogContent></Dialog>);}
