import React from'react';import{Dialog,DialogContent,DialogHeader,DialogTitle}from'@/components/ui/dialog';import{BeneficioForm}from'@/components/forms/BeneficioForm';import{useCreateBeneficio,useUpdateBeneficio}from'@/hooks/useBeneficio';interface Props{open:boolean;onClose:()=>void;beneficio?:any;}export function BeneficioModal({open,onClose,beneficio}:Props){const createMutation=useCreateBeneficio();const updateMutation=useUpdateBeneficio();const handleSubmit=async(data:any)=>{try{if(beneficio){await updateMutation.mutateAsync({id:beneficio.id,data});}else{await createMutation.mutateAsync(data);}onClose();}catch(e){console.error(e);}};return(<Dialog open={open}onOpenChange={onClose}><DialogContent><DialogHeader><DialogTitle>{beneficio?'Editar Benefício':'Novo Benefício'}</DialogTitle></DialogHeader><BeneficioForm defaultValues={beneficio}onSubmit={handleSubmit}onCancel={onClose}/></DialogContent></Dialog>);}
