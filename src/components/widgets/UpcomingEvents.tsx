import React from'react';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{Badge}from'@/components/ui/badge';import{Calendar,Gift,Sun,AlertTriangle}from'lucide-react';import{formatDate}from'@/utils/formatters';interface Event{id:string;tipo:'aniversario'|'ferias'|'feriado'|'vencimento';titulo:string;data:string;descricao?:string;}const tipoConfig={aniversario:{color:'bg-pink-100 text-pink-800',icon:Gift},ferias:{color:'bg-blue-100 text-blue-800',icon:Sun},feriado:{color:'bg-green-100 text-green-800',icon:Calendar},vencimento:{color:'bg-red-100 text-red-800',icon:AlertTriangle}};interface Props{events:Event[];title?:string;}export function UpcomingEvents({events,title='Próximos Eventos'}:Props){return(<Card><CardHeader><CardTitle className="text-lg">{title}</CardTitle></CardHeader><CardContent><div className="space-y-3">{events.length===0?<p className="text-sm text-muted-foreground text-center py-4">Nenhum evento próximo</p>:events.map(e=>{const config=tipoConfig[e.tipo];const Icon=config.icon;return(<div key={e.id}className="flex items-center gap-3 p-2"><div className={cn('p-2 rounded-full',config.color)}><Icon className="w-4 h-4"/></div><div className="flex-1"><p className="text-sm font-medium">{e.titulo}</p>{e.descricao&&<p className="text-xs text-muted-foreground">{e.descricao}</p>}</div><Badge variant="outline">{formatDate(e.data)}</Badge></div>);})}</div></CardContent></Card>);}function cn(...classes:string[]){return classes.filter(Boolean).join(' ');}
