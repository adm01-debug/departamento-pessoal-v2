import React from'react';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{Badge}from'@/components/ui/badge';import{useDashboardEvents}from'@/hooks/useDashboard';import{formatDate}from'@/utils/formatters';import{Calendar}from'lucide-react';const tipoColors={ferias:'bg-blue-100 text-blue-800',aniversario:'bg-pink-100 text-pink-800',vencimento:'bg-red-100 text-red-800',feriado:'bg-green-100 text-green-800'};export function UpcomingEvents(){const{data:events}=useDashboardEvents();return(<Card><CardHeader><CardTitle className="flex items-center gap-2"><Calendar className="w-5 h-5"/>Próximos Eventos</CardTitle></CardHeader><CardContent className="space-y-3">{events?.slice(0,5).map((event:any,i:number)=>(<div key={i}className="flex items-center justify-between"><div><p className="font-medium">{event.titulo}</p><p className="text-xs text-muted-foreground">{formatDate(event.data)}</p></div><Badge className={tipoColors[event.tipo as keyof typeof tipoColors]}>{event.tipo}</Badge></div>))}</CardContent></Card>);}
