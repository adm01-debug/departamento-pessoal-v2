import React from'react';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{Badge}from'@/components/ui/badge';import{Button}from'@/components/ui/button';import{AlertCircle,Clock,CheckCircle}from'lucide-react';interface Task{id:string;titulo:string;descricao:string;prioridade:'alta'|'media'|'baixa';vencimento:string;tipo:string;}const prioridadeConfig={alta:{color:'bg-red-100 text-red-800',icon:AlertCircle},media:{color:'bg-yellow-100 text-yellow-800',icon:Clock},baixa:{color:'bg-green-100 text-green-800',icon:CheckCircle}};interface Props{tasks:Task[];onComplete?:(id:string)=>void;title?:string;}export function PendingTasks({tasks,onComplete,title='Tarefas Pendentes'}:Props){return(<Card><CardHeader><CardTitle className="text-lg">{title}</CardTitle></CardHeader><CardContent><div className="space-y-3">{tasks.length===0?<p className="text-sm text-muted-foreground text-center py-4">Nenhuma tarefa pendente</p>:tasks.map(t=>{const config=prioridadeConfig[t.prioridade];const Icon=config.icon;return(<div key={t.id}className="flex items-center justify-between p-3 border rounded-lg"><div className="flex items-center gap-3"><Icon className="w-5 h-5"/><div><p className="text-sm font-medium">{t.titulo}</p><p className="text-xs text-muted-foreground">{t.descricao}</p></div></div><div className="flex items-center gap-2"><Badge className={config.color}>{t.prioridade}</Badge>{onComplete&&<Button size="sm"variant="ghost"onClick={()=>onComplete(t.id)}><CheckCircle className="w-4 h-4"/></Button>}</div></div>);})}</div></CardContent></Card>);}
