import React from'react';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{Button}from'@/components/ui/button';import{Badge}from'@/components/ui/badge';import{useNavigate}from'react-router-dom';import{ClipboardList,ArrowRight}from'lucide-react';const prioridadeColors={alta:'bg-red-100 text-red-800',media:'bg-yellow-100 text-yellow-800',baixa:'bg-green-100 text-green-800'};interface Task{id:string;titulo:string;modulo:string;prioridade:'alta'|'media'|'baixa';path:string;}interface Props{tasks:Task[];}export function PendingTasks({tasks}:Props){const navigate=useNavigate();return(<Card><CardHeader><CardTitle className="flex items-center gap-2"><ClipboardList className="w-5 h-5"/>Tarefas Pendentes</CardTitle></CardHeader><CardContent className="space-y-3">{tasks.slice(0,5).map(task=>(<div key={task.id}className="flex items-center justify-between p-2 rounded-lg bg-muted/50"><div><p className="font-medium">{task.titulo}</p><p className="text-xs text-muted-foreground">{task.modulo}</p></div><div className="flex items-center gap-2"><Badge className={prioridadeColors[task.prioridade]}>{task.prioridade}</Badge><Button variant="ghost"size="icon"onClick={()=>navigate(task.path)}><ArrowRight className="w-4 h-4"/></Button></div></div>))}</CardContent></Card>);}
