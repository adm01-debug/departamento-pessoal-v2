import React from'react';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{Button}from'@/components/ui/button';import{Plus,UserPlus,Calendar,FileText,Clock}from'lucide-react';import{useNavigate}from'react-router-dom';interface Action{label:string;icon:React.ElementType;href?:string;onClick?:()=>void;}const defaultActions:Action[]=[{label:'Nova Admissão',icon:UserPlus,href:'/admissoes/nova'},{label:'Programar Férias',icon:Calendar,href:'/ferias/nova'},{label:'Registrar Ponto',icon:Clock,href:'/ponto'},{label:'Gerar Relatório',icon:FileText,href:'/relatorios'}];interface Props{actions?:Action[];title?:string;}export function QuickActions({actions=defaultActions,title='Ações Rápidas'}:Props){const navigate=useNavigate();const handleClick=(action:Action)=>{if(action.onClick){action.onClick();}else if(action.href){navigate(action.href);}};return(<Card><CardHeader><CardTitle className="text-lg">{title}</CardTitle></CardHeader><CardContent><div className="grid grid-cols-2 gap-2">{actions.map((action,i)=>(<Button key={i}variant="outline"className="h-auto py-3 flex flex-col gap-1"onClick={()=>handleClick(action)}><action.icon className="w-5 h-5"/><span className="text-xs">{action.label}</span></Button>))}</div></CardContent></Card>);}
