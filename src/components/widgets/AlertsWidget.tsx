import React from'react';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{Alert,AlertDescription}from'@/components/ui/alert';import{AlertTriangle,AlertCircle,Info,CheckCircle}from'lucide-react';interface AlertItem{id:string;tipo:'erro'|'aviso'|'info'|'sucesso';mensagem:string;acao?:{label:string;onClick:()=>void;};}interface Props{alerts:AlertItem[];title?:string;}const tipoConfig={erro:{icon:AlertCircle,variant:'destructive'as const},aviso:{icon:AlertTriangle,variant:'default'as const},info:{icon:Info,variant:'default'as const},sucesso:{icon:CheckCircle,variant:'default'as const}};export function AlertsWidget({alerts,title='Alertas'}:Props){if(alerts.length===0)return null;return(<Card><CardHeader><CardTitle className="text-lg">{title}</CardTitle></CardHeader><CardContent className="space-y-2">{alerts.map(a=>{const config=tipoConfig[a.tipo];const Icon=config.icon;return(<Alert key={a.id}variant={config.variant}><Icon className="h-4 w-4"/><AlertDescription className="flex items-center justify-between"><span>{a.mensagem}</span>{a.acao&&<button className="text-sm underline ml-2"onClick={a.acao.onClick}>{a.acao.label}</button>}</AlertDescription></Alert>);})}</CardContent></Card>);}
