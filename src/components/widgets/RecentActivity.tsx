import React from'react';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{ScrollArea}from'@/components/ui/scroll-area';import{formatDateTime}from'@/utils/formatters';interface Activity{id:string;tipo:string;descricao:string;usuario:string;createdAt:string;}interface Props{activities:Activity[];title?:string;maxHeight?:number;}const tipoIcons:Record<string,string>={admissao:'👤',demissao:'🚪',ferias:'🏖️',folha:'💰',ponto:'⏰',documento:'📄'};export function RecentActivity({activities,title='Atividades Recentes',maxHeight=400}:Props){return(<Card><CardHeader><CardTitle className="text-lg">{title}</CardTitle></CardHeader><CardContent><ScrollArea className="pr-4"style={{maxHeight}}><div className="space-y-4">{activities.length===0?<p className="text-muted-foreground text-center py-4">Nenhuma atividade recente</p>:activities.map(a=>(<div key={a.id}className="flex gap-3 pb-4 border-b last:border-0"><span className="text-xl">{tipoIcons[a.tipo]||'📋'}</span><div className="flex-1"><p className="text-sm">{a.descricao}</p><p className="text-xs text-muted-foreground">{a.usuario} • {formatDateTime(a.createdAt)}</p></div></div>))}</div></ScrollArea></CardContent></Card>);}
