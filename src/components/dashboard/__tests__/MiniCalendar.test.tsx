import { describe, it, expect } from 'vitest';
import { MiniCalendar } from '../MiniCalendar';
describe('MiniCalendar', () => { it('should be defined', () => { expect(MiniCalendar).toBeDefined(); }); });
