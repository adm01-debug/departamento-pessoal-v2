import { describe, it, expect } from 'vitest';
import { PayrollSummaryCard } from '../PayrollSummaryCard';
describe('PayrollSummaryCard', () => { it('defined', () => { expect(PayrollSummaryCard).toBeDefined(); }); });
