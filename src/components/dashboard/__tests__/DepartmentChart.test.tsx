import { describe, it, expect } from 'vitest';
import { DepartmentChart } from '../DepartmentChart';
describe('DepartmentChart', () => { it('defined', () => { expect(DepartmentChart).toBeDefined(); }); });
