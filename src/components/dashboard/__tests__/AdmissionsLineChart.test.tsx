import { describe, it, expect } from 'vitest';
import { AdmissionsLineChart } from '../AdmissionsLineChart';
describe('AdmissionsLineChart', () => { it('defined', () => { expect(AdmissionsLineChart).toBeDefined(); }); });
