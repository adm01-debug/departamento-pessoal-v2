import { describe, it, expect } from 'vitest';
import { AbsenteismChart } from '../AbsenteismChart';
describe('AbsenteismChart', () => { it('should be defined', () => { expect(AbsenteismChart).toBeDefined(); }); });
