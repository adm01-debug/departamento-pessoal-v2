import { describe, it, expect } from 'vitest';
import { DashboardCustomizer } from '../DashboardCustomizer';
describe('DashboardCustomizer', () => { it('defined', () => { expect(DashboardCustomizer).toBeDefined(); }); });
