import { describe, it, expect } from 'vitest';
import { AlertHistoryModal } from '../AlertHistoryModal';
describe('AlertHistoryModal', () => { it('defined', () => { expect(AlertHistoryModal).toBeDefined(); }); });
