import React from'react';import{Input}from'@/components/ui/input';import{Select,SelectContent,SelectItem,SelectTrigger,SelectValue}from'@/components/ui/select';import{Button}from'@/components/ui/button';import{Search,X}from'lucide-react';import{STATUS_COLABORADOR,STATUS_COLABORADOR_LABELS}from'@/constants/colaboradores';interface Filters{search:string;status:string;departamentoId:string;}interface Props{filters:Filters;departamentos:{id:string;nome:string}[];onChange:(filters:Filters)=>void;onClear:()=>void;}export function ColaboradorFilter({filters,departamentos,onChange,onClear}:Props){return(<div className="flex flex-wrap gap-4 items-end"><div className="flex-1 min-w-[200px]"><div className="relative"><Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-muted-foreground"/><Input placeholder="Buscar por nome, CPF ou email..."value={filters.search}onChange={e=>onChange({...filters,search:e.target.value})}className="pl-10"/></div></div><Select value={filters.status}onValueChange={v=>onChange({...filters,status:v})}><SelectTrigger className="w-40"><SelectValue placeholder="Status"/></SelectTrigger><SelectContent><SelectItem value="">Todos</SelectItem>{Object.entries(STATUS_COLABORADOR).map(([k,v])=>(<SelectItem key={v}value={v}>{STATUS_COLABORADOR_LABELS[v as keyof typeof STATUS_COLABORADOR_LABELS]}</SelectItem>))}</SelectContent></Select><Select value={filters.departamentoId}onValueChange={v=>onChange({...filters,departamentoId:v})}><SelectTrigger className="w-48"><SelectValue placeholder="Departamento"/></SelectTrigger><SelectContent><SelectItem value="">Todos</SelectItem>{departamentos.map(d=>(<SelectItem key={d.id}value={d.id}>{d.nome}</SelectItem>))}</SelectContent></Select>{(filters.search||filters.status||filters.departamentoId)&&<Button variant="ghost"size="icon"onClick={onClear}><X className="w-4 h-4"/></Button>}</div>);}
