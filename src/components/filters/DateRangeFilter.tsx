import React from'react';import{Input}from'@/components/ui/input';import{Label}from'@/components/ui/label';import{Button}from'@/components/ui/button';import{Select,SelectContent,SelectItem,SelectTrigger,SelectValue}from'@/components/ui/select';import{Calendar,X}from'lucide-react';interface DateRange{inicio:string;fim:string;}interface Props{value:DateRange;onChange:(value:DateRange)=>void;onClear?:()=>void;presets?:boolean;}const presetOptions=[{label:'Hoje',getValue:()=>{const today=new Date().toISOString().split('T')[0];return{inicio:today,fim:today};}},{label:'Esta semana',getValue:()=>{const now=new Date();const start=new Date(now.setDate(now.getDate()-now.getDay()));const end=new Date(now.setDate(now.getDate()+6));return{inicio:start.toISOString().split('T')[0],fim:end.toISOString().split('T')[0]};}},{label:'Este mês',getValue:()=>{const now=new Date();const start=new Date(now.getFullYear(),now.getMonth(),1);const end=new Date(now.getFullYear(),now.getMonth()+1,0);return{inicio:start.toISOString().split('T')[0],fim:end.toISOString().split('T')[0]};}}];export function DateRangeFilter({value,onChange,onClear,presets=true}:Props){return(<div className="flex flex-wrap gap-4 items-end">{presets&&<Select onValueChange={v=>{const preset=presetOptions.find(p=>p.label===v);if(preset)onChange(preset.getValue());}}><SelectTrigger className="w-36"><SelectValue placeholder="Período"/></SelectTrigger><SelectContent>{presetOptions.map(p=>(<SelectItem key={p.label}value={p.label}>{p.label}</SelectItem>))}</SelectContent></Select>}<div><Label className="text-xs">Início</Label><Input type="date"value={value.inicio}onChange={e=>onChange({...value,inicio:e.target.value})}className="w-40"/></div><div><Label className="text-xs">Fim</Label><Input type="date"value={value.fim}onChange={e=>onChange({...value,fim:e.target.value})}className="w-40"/></div>{onClear&&(value.inicio||value.fim)&&<Button variant="ghost"size="icon"onClick={onClear}><X className="w-4 h-4"/></Button>}</div>);}
