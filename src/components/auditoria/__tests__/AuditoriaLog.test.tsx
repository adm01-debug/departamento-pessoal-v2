import { describe, it, expect } from 'vitest';
import { AuditoriaLog } from '../AuditoriaLog';
describe('AuditoriaLog', () => { it('should be defined', () => { expect(AuditoriaLog).toBeDefined(); }); });
