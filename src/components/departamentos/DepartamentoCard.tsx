import React from'react';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{Badge}from'@/components/ui/badge';import{Button}from'@/components/ui/button';import{Avatar}from'@/components/common/Avatar';import{Edit,Trash2,Users}from'lucide-react';interface Props{departamento:{id:string;nome:string;sigla:string;gestor?:{nome:string};totalColaboradores:number;ativo:boolean};onEdit?:(id:string)=>void;onDelete?:(id:string)=>void;}export function DepartamentoCard({departamento,onEdit,onDelete}:Props){return(<Card><CardHeader className="pb-2"><div className="flex justify-between items-start"><div><CardTitle className="text-lg">{departamento.nome}</CardTitle><p className="text-sm text-muted-foreground">{departamento.sigla}</p></div><Badge variant={departamento.ativo?'default':'secondary'}>{departamento.ativo?'Ativo':'Inativo'}</Badge></div></CardHeader><CardContent>{departamento.gestor&&<div className="flex items-center gap-2 mb-4"><Avatar name={departamento.gestor.nome}size="sm"/><div><p className="text-xs text-muted-foreground">Gestor</p><p className="text-sm font-medium">{departamento.gestor.nome}</p></div></div>}<div className="flex items-center justify-between"><div className="flex items-center gap-1"><Users className="w-4 h-4"/><span className="text-sm">{departamento.totalColaboradores} colaboradores</span></div><div className="flex gap-2">{onEdit&&<Button size="sm"variant="ghost"onClick={()=>onEdit(departamento.id)}><Edit className="w-4 h-4"/></Button>}{onDelete&&<Button size="sm"variant="ghost"onClick={()=>onDelete(departamento.id)}><Trash2 className="w-4 h-4 text-red-500"/></Button>}</div></div></CardContent></Card>);}
