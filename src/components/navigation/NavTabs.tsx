import React from'react';import{Link,useLocation}from'react-router-dom';import{cn}from'@/lib/utils';interface Tab{label:string;href:string;}interface Props{tabs:Tab[];}export function NavTabs({tabs}:Props){const{pathname}=useLocation();return(<nav className="flex border-b"><div className="flex space-x-4">{tabs.map(tab=>(<Link key={tab.href}to={tab.href}className={cn('px-4 py-2 border-b-2 -mb-px transition-colors',pathname===tab.href?'border-primary text-primary':'border-transparent text-muted-foreground hover:text-foreground')}>{tab.label}</Link>))}</div></nav>);}
