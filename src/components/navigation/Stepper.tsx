import React from'react';import{cn}from'@/lib/utils';import{Check}from'lucide-react';interface Step{label:string;description?:string;}interface Props{steps:Step[];currentStep:number;}export function Stepper({steps,currentStep}:Props){return(<div className="flex justify-between"><ol className="flex items-center w-full">{steps.map((step,i)=>(<li key={i}className={cn('flex items-center','w-full after:content-[\'\'] after:w-full after:h-1 after:border-b after:border-4 after:inline-block',i===steps.length-1&&'after:hidden',i<currentStep&&'after:border-primary')}><span className={cn('flex items-center justify-center w-10 h-10 rounded-full shrink-0',i<currentStep&&'bg-primary text-white',i===currentStep&&'bg-primary/20 border-2 border-primary',i>currentStep&&'bg-gray-200')}>{i<currentStep?<Check className="w-5 h-5"/>:i+1}</span></li>))}</ol></div>);}
