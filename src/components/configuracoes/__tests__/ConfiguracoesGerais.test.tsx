import { describe, it, expect } from 'vitest';
import { ConfiguracoesGerais } from '../ConfiguracoesGerais';
describe('ConfiguracoesGerais', () => { it('should be defined', () => { expect(ConfiguracoesGerais).toBeDefined(); }); });
