import React from'react';import{DollarSign,Users,Calendar,FileText}from'lucide-react';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{Badge}from'@/components/ui/badge';import{Button}from'@/components/ui/button';import{formatCurrency,formatMonth}from'@/utils/formatters';interface Props{competencia:string;tipo:string;status:string;totalBruto:number;totalLiquido:number;colaboradores:number;onClick?:()=>void;onExport?:()=>void;}const statusConfig:Record<string,{label:string;color:string}>={aberta:{label:'Aberta',color:'bg-blue-100 text-blue-800'},calculada:{label:'Calculada',color:'bg-yellow-100 text-yellow-800'},fechada:{label:'Fechada',color:'bg-green-100 text-green-800'},paga:{label:'Paga',color:'bg-gray-100 text-gray-800'}};const tipoLabels:Record<string,string>={mensal:'Mensal','13_primeira':'13º 1ª','13_segunda':'13º 2ª',ferias:'Férias',rescisao:'Rescisão'};export function FolhaCard({competencia,tipo,status,totalBruto,totalLiquido,colaboradores,onClick,onExport}:Props){const s=statusConfig[status]||statusConfig.aberta;return(<Card className="hover:shadow-md transition-shadow cursor-pointer"onClick={onClick}><CardHeader className="pb-2"><div className="flex justify-between items-start"><div><CardTitle className="text-lg">{formatMonth(competencia)}</CardTitle><Badge variant="outline">{tipoLabels[tipo]||tipo}</Badge></div><Badge className={s.color}>{s.label}</Badge></div></CardHeader><CardContent><div className="space-y-2"><div className="flex justify-between"><span className="text-muted-foreground">Bruto</span><span>{formatCurrency(totalBruto)}</span></div><div className="flex justify-between font-medium"><span>Líquido</span><span className="text-green-600">{formatCurrency(totalLiquido)}</span></div><div className="flex justify-between text-sm"><span className="text-muted-foreground">Colaboradores</span><span>{colaboradores}</span></div>{onExport&&<Button variant="outline"size="sm"className="w-full mt-2"onClick={e=>{e.stopPropagation();onExport();}}><FileText className="w-4 h-4 mr-1"/>Exportar</Button>}</div></CardContent></Card>);}
