import { describe, it, expect } from 'vitest';
import { ResetPasswordForm } from '../ResetPasswordForm';
describe('ResetPasswordForm', () => { it('defined', () => { expect(ResetPasswordForm).toBeDefined(); }); });
