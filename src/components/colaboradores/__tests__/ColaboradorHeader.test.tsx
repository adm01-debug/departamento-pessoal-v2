import { describe, it, expect } from 'vitest';
import { ColaboradorHeader } from '../ColaboradorHeader';
describe('ColaboradorHeader', () => { it('defined', () => { expect(ColaboradorHeader).toBeDefined(); }); });
