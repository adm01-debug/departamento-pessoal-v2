import { describe, it, expect } from 'vitest';
import { ColaboradorCard } from '../ColaboradorCard';
describe('ColaboradorCard', () => { it('should be defined', () => { expect(ColaboradorCard).toBeDefined(); }); });
