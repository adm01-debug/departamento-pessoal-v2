import { describe, it, expect } from 'vitest';
import { ColaboradorForm } from '../ColaboradorForm';
describe('ColaboradorForm', () => { it('should be defined', () => { expect(ColaboradorForm).toBeDefined(); }); });
