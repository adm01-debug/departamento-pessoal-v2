import { describe, it, expect } from 'vitest';
import { ColaboradorList } from '../ColaboradorList';
describe('ColaboradorList', () => { it('defined', () => { expect(ColaboradorList).toBeDefined(); }); });
