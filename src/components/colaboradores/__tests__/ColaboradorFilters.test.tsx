import { describe, it, expect } from 'vitest';
import { ColaboradorFilters } from '../ColaboradorFilters';
describe('ColaboradorFilters', () => { it('defined', () => { expect(ColaboradorFilters).toBeDefined(); }); });
