import { describe, it, expect } from 'vitest';
import { ColaboradorContactInfo } from '../ColaboradorContactInfo';
describe('ColaboradorContactInfo', () => { it('defined', () => { expect(ColaboradorContactInfo).toBeDefined(); }); });
