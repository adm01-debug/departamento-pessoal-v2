import React from'react';import{Badge}from'@/components/ui/badge';import{cn}from'@/lib/utils';type Status='ativo'|'inativo'|'ferias'|'afastado'|'desligado';const statusConfig:Record<Status,{label:string;variant:'default'|'secondary'|'destructive'|'outline'}>={ativo:{label:'Ativo',variant:'default'},inativo:{label:'Inativo',variant:'secondary'},ferias:{label:'Férias',variant:'outline'},afastado:{label:'Afastado',variant:'outline'},desligado:{label:'Desligado',variant:'destructive'}};interface Props{status:Status;className?:string;}export function ColaboradorStatus({status,className}:Props){const config=statusConfig[status]||statusConfig.ativo;return(<Badge variant={config.variant}className={cn(className)}>{config.label}</Badge>);}
