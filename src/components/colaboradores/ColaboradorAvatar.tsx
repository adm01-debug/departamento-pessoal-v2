import React from'react';import{Avatar,AvatarFallback,AvatarImage}from'@/components/ui/avatar';import{cn}from'@/lib/utils';interface Props{nome:string;foto?:string;size?:'sm'|'md'|'lg';className?:string;}const sizes={sm:'h-8 w-8',md:'h-10 w-10',lg:'h-16 w-16'};export function ColaboradorAvatar({nome,foto,size='md',className}:Props){const initials=nome.split(' ').map(n=>n[0]).join('').slice(0,2).toUpperCase();return(<Avatar className={cn(sizes[size],className)}>{foto&&<AvatarImage src={foto}alt={nome}/>}<AvatarFallback>{initials}</AvatarFallback></Avatar>);}
