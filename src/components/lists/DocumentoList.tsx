import React from'react';import{Badge}from'@/components/ui/badge';import{Button}from'@/components/ui/button';import{Table,TableBody,TableCell,TableHead,TableHeader,TableRow}from'@/components/ui/table';import{Eye,Download,FileText,AlertTriangle}from'lucide-react';import{formatDate}from'@/utils/formatters';interface Documento{id:string;nome:string;tipo:string;colaborador:string;vencimento?:string;status:string;url:string;}interface Props{documentos:Documento[];onView?:(d:Documento)=>void;onDownload?:(d:Documento)=>void;}const statusColors={valido:'bg-green-100 text-green-800',vencido:'bg-red-100 text-red-800',pendente:'bg-yellow-100 text-yellow-800'};export function DocumentoList({documentos,onView,onDownload}:Props){if(!documentos.length)return<div className="py-8 text-center text-muted-foreground"><FileText className="w-12 h-12 mx-auto mb-2 opacity-50"/><p>Nenhum documento</p></div>;return(<Table><TableHeader><TableRow><TableHead>Documento</TableHead><TableHead>Tipo</TableHead><TableHead>Colaborador</TableHead><TableHead>Vencimento</TableHead><TableHead>Status</TableHead><TableHead></TableHead></TableRow></TableHeader><TableBody>{documentos.map(d=>(<TableRow key={d.id}><TableCell className="font-medium">{d.nome}</TableCell><TableCell>{d.tipo}</TableCell><TableCell>{d.colaborador}</TableCell><TableCell>{d.vencimento?<span className={d.status==='vencido'?'text-red-600 flex items-center gap-1':''}>{d.status==='vencido'&&<AlertTriangle className="w-4 h-4"/>}{formatDate(d.vencimento)}</span>:'-'}</TableCell><TableCell><Badge className={statusColors[d.status as keyof typeof statusColors]||''}>{d.status}</Badge></TableCell><TableCell><div className="flex gap-1">{onView&&<Button variant="ghost"size="sm"onClick={()=>onView(d)}><Eye className="w-4 h-4"/></Button>}{onDownload&&<Button variant="ghost"size="sm"onClick={()=>onDownload(d)}><Download className="w-4 h-4"/></Button>}</div></TableCell></TableRow>))}</TableBody></Table>);}
