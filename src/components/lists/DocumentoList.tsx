import React from'react';import{FileText,Eye,Download,Trash2,Clock,CheckCircle}from'lucide-react';import{Button}from'@/components/ui/button';import{Badge}from'@/components/ui/badge';import{formatDate,formatBytes}from'@/utils/formatters';interface Documento{id:string;nome:string;tipo:string;tamanho:number;dataUpload:string;validade?:string;status:'ativo'|'vencido'|'pendente';}interface Props{documentos:Documento[];onView?:(id:string)=>void;onDownload?:(id:string)=>void;onDelete?:(id:string)=>void;}const statusConfig={ativo:{label:'Ativo',color:'bg-green-100 text-green-800',icon:CheckCircle},vencido:{label:'Vencido',color:'bg-red-100 text-red-800',icon:Clock},pendente:{label:'Pendente',color:'bg-yellow-100 text-yellow-800',icon:Clock}};export function DocumentoList({documentos,onView,onDownload,onDelete}:Props){if(documentos.length===0)return<p className="text-center py-8 text-muted-foreground">Nenhum documento encontrado</p>;return(<div className="space-y-3">{documentos.map(d=>{const status=statusConfig[d.status];const Icon=status.icon;return(<div key={d.id}className="flex items-center justify-between p-4 border rounded-lg"><div className="flex items-center gap-4"><FileText className="w-8 h-8 text-blue-600"/><div><p className="font-medium">{d.nome}</p><p className="text-sm text-muted-foreground">{d.tipo} | {formatBytes(d.tamanho)} | Upload: {formatDate(d.dataUpload)}</p></div></div><div className="flex items-center gap-3">{d.validade&&<span className="text-sm text-muted-foreground">Val: {formatDate(d.validade)}</span>}<Badge className={status.color}><Icon className="w-3 h-3 mr-1"/>{status.label}</Badge><div className="flex gap-1">{onView&&<Button variant="ghost"size="sm"onClick={()=>onView(d.id)}><Eye className="w-4 h-4"/></Button>}{onDownload&&<Button variant="ghost"size="sm"onClick={()=>onDownload(d.id)}><Download className="w-4 h-4"/></Button>}{onDelete&&<Button variant="ghost"size="sm"onClick={()=>onDelete(d.id)}><Trash2 className="w-4 h-4 text-red-500"/></Button>}</div></div></div>);})}</div>);}
