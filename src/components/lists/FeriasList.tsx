import React from'react';import{Sun,Calendar,Eye,Edit,Trash2}from'lucide-react';import{Button}from'@/components/ui/button';import{Badge}from'@/components/ui/badge';import{formatDate}from'@/utils/formatters';interface Ferias{id:string;colaborador:string;inicio:string;fim:string;dias:number;status:'programada'|'em_gozo'|'concluida'|'cancelada';abono:boolean;}interface Props{ferias:Ferias[];onView?:(id:string)=>void;onEdit?:(id:string)=>void;onDelete?:(id:string)=>void;}const statusConfig={programada:{label:'Programada',color:'bg-blue-100 text-blue-800'},em_gozo:{label:'Em Gozo',color:'bg-green-100 text-green-800'},concluida:{label:'Concluída',color:'bg-gray-100 text-gray-800'},cancelada:{label:'Cancelada',color:'bg-red-100 text-red-800'}};export function FeriasList({ferias,onView,onEdit,onDelete}:Props){if(ferias.length===0)return<p className="text-center py-8 text-muted-foreground">Nenhum registro de férias</p>;return(<div className="space-y-3">{ferias.map(f=>{const status=statusConfig[f.status];return(<div key={f.id}className="flex items-center justify-between p-4 border rounded-lg"><div className="flex items-center gap-4"><Sun className="w-8 h-8 text-yellow-500"/><div><p className="font-medium">{f.colaborador}</p><p className="text-sm text-muted-foreground">{formatDate(f.inicio)} a {formatDate(f.fim)} ({f.dias} dias)</p></div></div><div className="flex items-center gap-3">{f.abono&&<Badge variant="outline">Abono</Badge>}<Badge className={status.color}>{status.label}</Badge><div className="flex gap-1">{onView&&<Button variant="ghost"size="sm"onClick={()=>onView(f.id)}><Eye className="w-4 h-4"/></Button>}{onEdit&&<Button variant="ghost"size="sm"onClick={()=>onEdit(f.id)}><Edit className="w-4 h-4"/></Button>}{onDelete&&<Button variant="ghost"size="sm"onClick={()=>onDelete(f.id)}><Trash2 className="w-4 h-4 text-red-500"/></Button>}</div></div></div>);})}</div>);}
