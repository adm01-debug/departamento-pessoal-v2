import React from'react';import{FileText,Calendar,Eye,Edit,Trash2}from'lucide-react';import{Button}from'@/components/ui/button';import{Badge}from'@/components/ui/badge';import{formatDate}from'@/utils/formatters';interface Contrato{id:string;colaborador:string;tipo:'clt'|'pj'|'estagio'|'temporario';dataInicio:string;dataFim?:string;status:'ativo'|'encerrado'|'suspenso';}interface Props{contratos:Contrato[];onView?:(id:string)=>void;onEdit?:(id:string)=>void;onDelete?:(id:string)=>void;}const tipoLabel={clt:'CLT',pj:'PJ',estagio:'Estágio',temporario:'Temporário'};const statusConfig={ativo:{label:'Ativo',color:'bg-green-100 text-green-800'},encerrado:{label:'Encerrado',color:'bg-gray-100 text-gray-800'},suspenso:{label:'Suspenso',color:'bg-yellow-100 text-yellow-800'}};export function ContratoList({contratos,onView,onEdit,onDelete}:Props){if(contratos.length===0)return<p className="text-center py-8 text-muted-foreground">Nenhum contrato encontrado</p>;return(<div className="space-y-3">{contratos.map(c=>{const status=statusConfig[c.status];return(<div key={c.id}className="flex items-center justify-between p-4 border rounded-lg"><div className="flex items-center gap-4"><FileText className="w-8 h-8 text-muted-foreground"/><div><p className="font-medium">{c.colaborador}</p><p className="text-sm text-muted-foreground">Início: {formatDate(c.dataInicio)}{c.dataFim&&` | Fim: ${formatDate(c.dataFim)}`}</p></div></div><div className="flex items-center gap-3"><Badge variant="outline">{tipoLabel[c.tipo]}</Badge><Badge className={status.color}>{status.label}</Badge><div className="flex gap-1">{onView&&<Button variant="ghost"size="sm"onClick={()=>onView(c.id)}><Eye className="w-4 h-4"/></Button>}{onEdit&&<Button variant="ghost"size="sm"onClick={()=>onEdit(c.id)}><Edit className="w-4 h-4"/></Button>}{onDelete&&<Button variant="ghost"size="sm"onClick={()=>onDelete(c.id)}><Trash2 className="w-4 h-4 text-red-500"/></Button>}</div></div></div>);})}</div>);}
