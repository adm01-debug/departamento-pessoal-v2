import React from'react';import{format,startOfWeek,addDays,isSameDay}from'date-fns';import{ptBR}from'date-fns/locale';import{cn}from'@/lib/utils';interface Event{id:string;title:string;start:Date;end:Date;type:'ferias'|'feriado'|'reuniao'|'outro';}interface Props{date:Date;events:Event[];onEventClick?:(event:Event)=>void;onDayClick?:(date:Date)=>void;}const typeColors={ferias:'bg-blue-100 border-blue-300',feriado:'bg-red-100 border-red-300',reuniao:'bg-green-100 border-green-300',outro:'bg-gray-100 border-gray-300'};export function WeekView({date,events,onEventClick,onDayClick}:Props){const weekStart=startOfWeek(date,{locale:ptBR});const days=Array.from({length:7},(\_,i)=>addDays(weekStart,i));const hours=Array.from({length:12},(\_,i)=>i+7);return(<div className="flex flex-col h-full"><div className="grid grid-cols-8 border-b"><div className="w-16"/>{days.map(day=>(<div key={day.toISOString()}className={cn('text-center py-2 cursor-pointer hover:bg-muted',isSameDay(day,new Date())&&'bg-primary/10')}onClick={()=>onDayClick?.(day)}><p className="text-sm font-medium">{format(day,'EEE',{locale:ptBR})}</p><p className="text-lg">{format(day,'dd')}</p></div>))}</div><div className="flex-1 overflow-auto"><div className="grid grid-cols-8">{hours.map(hour=>(<React.Fragment key={hour}><div className="w-16 text-right pr-2 text-sm text-muted-foreground border-b py-2">{hour}:00</div>{days.map(day=>{const dayEvents=events.filter(e=>isSameDay(new Date(e.start),day)&&new Date(e.start).getHours()===hour);return(<div key={day.toISOString()+hour}className="border-b border-l min-h-[50px] relative">{dayEvents.map(e=>(<div key={e.id}className={cn('absolute inset-x-1 top-1 p-1 text-xs rounded border cursor-pointer truncate',typeColors[e.type])}onClick={()=>onEventClick?.(e)}>{e.title}</div>))}</div>);})}</React.Fragment>))}</div></div></div>);}
