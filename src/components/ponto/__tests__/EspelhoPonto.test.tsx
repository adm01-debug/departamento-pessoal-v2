import { describe, it, expect } from 'vitest';
import { EspelhoPonto } from '../EspelhoPonto';
describe('EspelhoPonto', () => { it('should be defined', () => { expect(EspelhoPonto).toBeDefined(); }); });
