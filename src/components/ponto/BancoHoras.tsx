import React from'react';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{Progress}from'@/components/ui/progress';import{Clock,TrendingUp,TrendingDown}from'lucide-react';interface Props{saldo:number;limite:number;historico:{mes:string;saldo:number}[];}export function BancoHoras({saldo,limite,historico}:Props){const percentual=Math.min((Math.abs(saldo)/limite)*100,100);const positivo=saldo>=0;return(<Card><CardHeader><CardTitle className="flex items-center gap-2"><Clock className="w-5 h-5"/>Banco de Horas</CardTitle></CardHeader><CardContent><div className="space-y-4"><div className="flex items-center justify-between"><span className="text-sm text-muted-foreground">Saldo Atual</span><span className={`text-2xl font-bold ${positivo?'text-green-600':'text-red-600'}`}>{positivo?'+':''}{Math.floor(saldo/60)}h{Math.abs(saldo%60).toString().padStart(2,'0')}min</span></div><Progress value={percentual}className="h-2"/><p className="text-xs text-muted-foreground text-right">Limite: {limite/60}h</p><div className="mt-4"><h4 className="text-sm font-medium mb-2">Últimos meses</h4>{historico.slice(-3).map((h,i)=>(<div key={i}className="flex justify-between text-sm py-1 border-b last:border-0"><span>{h.mes}</span><span className={h.saldo>=0?'text-green-600':'text-red-600'}>{h.saldo>=0?'+':''}{Math.floor(h.saldo/60)}h</span></div>))}</div></div></CardContent></Card>);}
