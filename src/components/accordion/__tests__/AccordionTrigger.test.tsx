import { describe, it, expect } from 'vitest';
import { AccordionTrigger } from '../AccordionTrigger';
describe('AccordionTrigger', () => { it('defined', () => { expect(AccordionTrigger).toBeDefined(); }); });
