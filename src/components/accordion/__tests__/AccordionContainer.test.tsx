import { describe, it, expect } from 'vitest';
import { AccordionContainer } from '../AccordionContainer';
describe('AccordionContainer', () => { it('defined', () => { expect(AccordionContainer).toBeDefined(); }); });
