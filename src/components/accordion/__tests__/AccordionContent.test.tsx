import { describe, it, expect } from 'vitest';
import { AccordionContent } from '../AccordionContent';
describe('AccordionContent', () => { it('defined', () => { expect(AccordionContent).toBeDefined(); }); });
