import React from'react';import{PieChart as RechartsPie,Pie,Cell,Tooltip,Legend,ResponsiveContainer}from'recharts';interface Props{data:{name:string;value:number;color?:string}[];title?:string;innerRadius?:number;}const COLORS=['#3b82f6','#22c55e','#eab308','#ef4444','#8b5cf6'];export function DonutChart({data,title,innerRadius=60}:Props){const total=data.reduce((sum,d)=>sum+d.value,0);return(<div className="w-full h-[300px]"><ResponsiveContainer><RechartsPie><Pie data={data}cx="50%"cy="50%"innerRadius={innerRadius}outerRadius={80}fill="#8884d8"paddingAngle={2}dataKey="value"label={({name,value})=>`${name}: ${((value/total)*100).toFixed(0)}%`}>{data.map((entry,index)=>(<Cell key={`cell-${index}`}fill={entry.color||COLORS[index%COLORS.length]}/>))}</Pie><Tooltip/><Legend/></RechartsPie></ResponsiveContainer></div>);}
