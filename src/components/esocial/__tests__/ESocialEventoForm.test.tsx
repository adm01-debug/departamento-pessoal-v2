import { describe, it, expect } from 'vitest';
import { ESocialEventoForm } from '../ESocialEventoForm';
describe('ESocialEventoForm', () => { it('should be defined', () => { expect(ESocialEventoForm).toBeDefined(); }); });
