import { describe, it, expect } from 'vitest';
import { ESocialDashboard } from '../ESocialDashboard';
describe('ESocialDashboard', () => { it('defined', () => { expect(ESocialDashboard).toBeDefined(); }); });
