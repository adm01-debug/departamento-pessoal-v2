import React from'react';import{Timeline}from'@/components/data-display/Timeline';import{formatDateTime}from'@/utils/formatters';import{Send,CheckCircle,XCircle,RefreshCw}from'lucide-react';interface Historico{id:string;acao:string;data:string;detalhes?:string;}interface Props{historico:Historico[];}const icons:Record<string,React.ReactNode>={gerado:<Send className="w-3 h-3 text-white"/>,enviado:<Send className="w-3 h-3 text-white"/>,processado:<CheckCircle className="w-3 h-3 text-white"/>,erro:<XCircle className="w-3 h-3 text-white"/>,reenviado:<RefreshCw className="w-3 h-3 text-white"/>};export function EventoTimeline({historico}:Props){const items=historico.map(h=>({id:h.id,title:h.acao,description:h.detalhes,date:formatDateTime(h.data),icon:icons[h.acao.toLowerCase()]||<Send className="w-3 h-3 text-white"/>}));return<Timeline items={items}/>;}
