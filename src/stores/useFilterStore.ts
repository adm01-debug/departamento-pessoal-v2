import{create}from'zustand';interface FilterState{filters:Record<string,any>;setFilter:(key:string,value:any)=>void;clearFilter:(key:string)=>void;clearAllFilters:()=>void;getFilter:<T>(key:string)=>T|undefined;}export const useFilterStore=create<FilterState>((set,get)=>({filters:{},setFilter:(key,value)=>set((s)=>({filters:{...s.filters,[key]:value}})),clearFilter:(key)=>set((s)=>{const{[key]:_,...rest}=s.filters;return{filters:rest};}),clearAllFilters:()=>set({filters:{}}),getFilter:<T>(key:string)=>get().filters[key]as T|undefined}));
