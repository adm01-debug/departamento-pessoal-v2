import { z } from "zod";
export const cpfSchema = z.string().length(11).refine(cpf => { if (/^(\d)\1+$/.test(cpf)) return false; let soma = 0; for (let i = 0; i < 9; i++) soma += parseInt(cpf[i]) * (10 - i); let resto = (soma * 10) % 11; if (resto === 10 || resto === 11) resto = 0; if (resto !== parseInt(cpf[9])) return false; soma = 0; for (let i = 0; i < 10; i++) soma += parseInt(cpf[i]) * (11 - i); resto = (soma * 10) % 11; if (resto === 10 || resto === 11) resto = 0; return resto === parseInt(cpf[10]); }, "CPF inválido");
export const cnpjSchema = z.string().length(14).refine(cnpj => { if (/^(\d)\1+$/.test(cnpj)) return false; let tam = cnpj.length - 2, nums = cnpj.substring(0, tam), digs = cnpj.substring(tam), soma = 0, pos = tam - 7; for (let i = tam; i >= 1; i--) { soma += parseInt(nums.charAt(tam - i)) * pos--; if (pos < 2) pos = 9; } let res = soma % 11 < 2 ? 0 : 11 - (soma % 11); if (res !== parseInt(digs.charAt(0))) return false; tam++; nums = cnpj.substring(0, tam); soma = 0; pos = tam - 7; for (let i = tam; i >= 1; i--) { soma += parseInt(nums.charAt(tam - i)) * pos--; if (pos < 2) pos = 9; } res = soma % 11 < 2 ? 0 : 11 - (soma % 11); return res === parseInt(digs.charAt(1)); }, "CNPJ inválido");
export const pisSchema = z.string().length(11).refine(pis => { const mult = [3,2,9,8,7,6,5,4,3,2]; let soma = 0; for (let i = 0; i < 10; i++) soma += parseInt(pis[i]) * mult[i]; const resto = soma % 11; const dig = resto < 2 ? 0 : 11 - resto; return dig === parseInt(pis[10]); }, "PIS inválido");
export const emailSchema = z.string().email("Email inválido");
export const telefoneSchema = z.string().min(10).max(11).regex(/^\d+$/, "Telefone inválido");
