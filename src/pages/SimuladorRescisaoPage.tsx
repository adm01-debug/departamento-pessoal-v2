import React,{useState}from'react';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{Input}from'@/components/ui/input';import{Label}from'@/components/ui/label';import{Select,SelectContent,SelectItem,SelectTrigger,SelectValue}from'@/components/ui/select';import{PageHeader}from'@/components/common/PageHeader';import{Calculator}from'lucide-react';import{formatCurrency}from'@/utils/formatters';export default function SimuladorRescisaoPage(){const[salario,setSalario]=useState(5000);const[mesesTrabalhados,setMeses]=useState(24);const[tipo,setTipo]=useState('sem_justa_causa');const avos13=Math.ceil((new Date().getMonth()+1)/12*12);const ferias=(salario/12)*mesesTrabalhados;const terco=ferias/3;const decimo=(salario/12)*avos13;const aviso=tipo==='sem_justa_causa'?salario:0;const multa=tipo==='sem_justa_causa'?salario*0.4:0;const total=ferias+terco+decimo+aviso+multa;return(<div className="space-y-6"><PageHeader title="Simulador de Rescisão"icon={Calculator}/><div className="grid md:grid-cols-2 gap-6"><Card><CardHeader><CardTitle>Dados</CardTitle></CardHeader><CardContent className="space-y-4"><div><Label>Salário</Label><Input type="number"value={salario}onChange={e=>setSalario(Number(e.target.value))}/></div><div><Label>Meses Trabalhados</Label><Input type="number"value={mesesTrabalhados}onChange={e=>setMeses(Number(e.target.value))}/></div><div><Label>Tipo de Rescisão</Label><Select value={tipo}onValueChange={setTipo}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="sem_justa_causa">Sem Justa Causa</SelectItem><SelectItem value="pedido_demissao">Pedido de Demissão</SelectItem><SelectItem value="justa_causa">Justa Causa</SelectItem></SelectContent></Select></div></CardContent></Card><Card><CardHeader><CardTitle>Estimativa</CardTitle></CardHeader><CardContent className="space-y-2"><div className="flex justify-between py-1 border-b"><span>Férias Proporcionais</span><span>{formatCurrency(ferias)}</span></div><div className="flex justify-between py-1 border-b"><span>1/3 Férias</span><span>{formatCurrency(terco)}</span></div><div className="flex justify-between py-1 border-b"><span>13º Proporcional</span><span>{formatCurrency(decimo)}</span></div><div className="flex justify-between py-1 border-b"><span>Aviso Prévio</span><span>{formatCurrency(aviso)}</span></div><div className="flex justify-between py-1 border-b"><span>Multa 40% FGTS</span><span>{formatCurrency(multa)}</span></div><div className="flex justify-between py-2 text-lg font-bold"><span>Total Estimado</span><span className="text-green-600">{formatCurrency(total)}</span></div></CardContent></Card></div></div>);}
