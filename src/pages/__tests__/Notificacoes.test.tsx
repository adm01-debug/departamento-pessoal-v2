import { describe, it, expect } from 'vitest';
import Notificacoes from '../Notificacoes';
describe('Notificacoes Page', () => { it('should be defined', () => { expect(Notificacoes).toBeDefined(); }); });
