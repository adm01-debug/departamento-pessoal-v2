import React,{useState}from'react';import{PageLayout}from'@/components/layout/PageLayout';import{PageHeader}from'@/components/common/PageHeader';import{Card,CardContent}from'@/components/ui/card';import{Button}from'@/components/ui/button';import{Badge}from'@/components/ui/badge';import{ChevronLeft,ChevronRight}from'lucide-react';import{format,startOfMonth,endOfMonth,eachDayOfInterval,isSameDay,isToday,addMonths,subMonths}from'date-fns';import{ptBR}from'date-fns/locale';const eventos=[{data:'2024-01-15',tipo:'ferias',titulo:'Férias João'},{data:'2024-01-20',tipo:'aniversario',titulo:'Aniversário Maria'},{data:'2024-01-25',tipo:'feriado',titulo:'Feriado'}];const tipoColors={ferias:'bg-blue-100 text-blue-800',aniversario:'bg-pink-100 text-pink-800',feriado:'bg-green-100 text-green-800'};export default function CalendarioPage(){const[currentDate,setCurrentDate]=useState(new Date());const days=eachDayOfInterval({start:startOfMonth(currentDate),end:endOfMonth(currentDate)});const getEventos=(date:Date)=>eventos.filter(e=>isSameDay(new Date(e.data),date));return(<PageLayout><PageHeader title="Calendário"/><Card><CardContent className="pt-6"><div className="flex items-center justify-between mb-4"><Button variant="outline"size="icon"onClick={()=>setCurrentDate(subMonths(currentDate,1))}><ChevronLeft className="w-4 h-4"/></Button><h2 className="text-lg font-bold">{format(currentDate,'MMMM yyyy',{locale:ptBR})}</h2><Button variant="outline"size="icon"onClick={()=>setCurrentDate(addMonths(currentDate,1))}><ChevronRight className="w-4 h-4"/></Button></div><div className="grid grid-cols-7 gap-1">{['Dom','Seg','Ter','Qua','Qui','Sex','Sáb'].map(d=>(<div key={d}className="p-2 text-center text-sm font-medium text-muted-foreground">{d}</div>))}{days.map(day=>{const dayEventos=getEventos(day);return(<div key={day.toISOString()}className={`min-h-[80px] p-1 border rounded ${isToday(day)?'bg-primary/10 border-primary':''}`}><span className="text-sm">{format(day,'d')}</span>{dayEventos.map((e,i)=>(<Badge key={i}className={`block mt-1 text-xs truncate ${tipoColors[e.tipo as keyof typeof tipoColors]}`}>{e.titulo}</Badge>))}</div>);})}</div></CardContent></Card></PageLayout>);}
