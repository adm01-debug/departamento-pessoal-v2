import React from'react';import{useParams}from'react-router-dom';import{useQuery}from'@tanstack/react-query';import{PageLayout}from'@/components/layout/PageLayout';import{PageHeader}from'@/components/common/PageHeader';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{Button}from'@/components/ui/button';import{LoadingSpinner}from'@/components/common/LoadingSpinner';import{api}from'@/lib/api';import{formatCurrency,formatDate}from'@/utils/formatters';import{Printer,Download}from'lucide-react';export default function TRCTPage(){const{id}=useParams();const{data:trct,isLoading}=useQuery({queryKey:['trct',id],queryFn:async()=>(await api.get(`/demissoes/${id}/trct`)).data,enabled:!!id});if(isLoading)return<LoadingSpinner/>;return(<PageLayout><PageHeader title="TRCT - Termo de Rescisão"actions={<div className="flex gap-2"><Button variant="outline"><Download className="w-4 h-4 mr-2"/>PDF</Button><Button><Printer className="w-4 h-4 mr-2"/>Imprimir</Button></div>}/><Card><CardHeader><CardTitle>Dados do Trabalhador</CardTitle></CardHeader><CardContent className="grid grid-cols-2 gap-4"><div><p className="text-sm text-muted-foreground">Nome</p><p className="font-medium">{trct?.colaborador}</p></div><div><p className="text-sm text-muted-foreground">Data Admissão</p><p>{formatDate(trct?.dataAdmissao)}</p></div><div><p className="text-sm text-muted-foreground">Data Demissão</p><p>{formatDate(trct?.dataDemissao)}</p></div><div><p className="text-sm text-muted-foreground">Tipo</p><p>{trct?.tipo}</p></div></CardContent></Card><Card className="mt-4"><CardHeader><CardTitle>Verbas Rescisórias</CardTitle></CardHeader><CardContent className="space-y-2">{trct?.verbas?.map((v:any,i:number)=>(<div key={i}className="flex justify-between py-1 border-b last:border-0"><span>{v.descricao}</span><span className={v.tipo==='desconto'?'text-red-600':''}>{v.tipo==='desconto'?'-':''}{formatCurrency(v.valor)}</span></div>))}<div className="pt-4 text-xl font-bold flex justify-between"><span>Total Líquido</span><span>{formatCurrency(trct?.totalLiquido)}</span></div></CardContent></Card></PageLayout>);}
