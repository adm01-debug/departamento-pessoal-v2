import React,{useState}from'react';import{PageLayout}from'@/components/layout/PageLayout';import{PageHeader}from'@/components/common/PageHeader';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{Button}from'@/components/ui/button';import{FileUpload}from'@/components/common/FileUpload';import{useImport}from'@/hooks/useImport';import{Download,Upload,FileSpreadsheet,Users,DollarSign}from'lucide-react';const templates=[{id:'colaboradores',nome:'Colaboradores',icon:Users},{id:'folha',nome:'Folha de Pagamento',icon:DollarSign}];export default function ImportacaoPage(){const{importFile,loading,result,clearResult}=useImport();const[selectedTemplate,setSelectedTemplate]=useState('');const handleFileSelect=(files:File[])=>{if(files.length>0&&selectedTemplate){importFile(`/import/${selectedTemplate}`,files[0],['colaboradores']);}};return(<PageLayout><PageHeader title="Importação"description="Importe dados via planilha"/><div className="grid grid-cols-1 md:grid-cols-2 gap-6"><Card><CardHeader><CardTitle>Templates</CardTitle></CardHeader><CardContent className="space-y-4">{templates.map(t=>(<div key={t.id}className="flex items-center justify-between p-4 border rounded-lg"><div className="flex items-center gap-3"><t.icon className="w-6 h-6"/><span className="font-medium">{t.nome}</span></div><div className="flex gap-2"><Button variant="outline"size="sm"onClick={()=>window.open(`/api/templates/${t.id}.xlsx`)}><Download className="w-4 h-4 mr-1"/>Baixar</Button><Button size="sm"onClick={()=>setSelectedTemplate(t.id)}><Upload className="w-4 h-4 mr-1"/>Importar</Button></div></div>))}</CardContent></Card>{selectedTemplate&&<Card><CardHeader><CardTitle>Upload de Arquivo</CardTitle></CardHeader><CardContent><FileUpload accept={['.xlsx','.csv']}onFilesSelected={handleFileSelect}/>{result&&<div className="mt-4 p-4 border rounded"><p>Total: {result.total}</p><p className="text-green-600">Importados: {result.importados}</p>{result.erros.length>0&&<p className="text-red-600">Erros: {result.erros.length}</p>}</div>}</CardContent></Card>}</div></PageLayout>);}
