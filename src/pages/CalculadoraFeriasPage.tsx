import React,{useState}from'react';import{PageLayout}from'@/components/layout/PageLayout';import{PageHeader}from'@/components/common/PageHeader';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{Button}from'@/components/ui/button';import{Input}from'@/components/ui/input';import{Label}from'@/components/ui/label';import{Switch}from'@/components/ui/switch';import{calcularValorFerias}from'@/utils/ferias';import{calcularINSS,calcularIRRF}from'@/utils/calculations';import{formatCurrency}from'@/utils/formatters';import{Calculator}from'lucide-react';export default function CalculadoraFeriasPage(){const[salario,setSalario]=useState<number>(0);const[dias,setDias]=useState<number>(30);const[abono,setAbono]=useState<number>(0);const[dependentes,setDependentes]=useState<number>(0);const[resultado,setResultado]=useState<any>(null);const calcular=()=>{const bruto=calcularValorFerias(salario,dias,abono);const inss=calcularINSS(bruto);const irrf=calcularIRRF(bruto-inss,dependentes);setResultado({bruto,inss,irrf,liquido:bruto-inss-irrf});};return(<PageLayout><PageHeader title="Calculadora de Férias"icon={Calculator}/><Card className="max-w-md"><CardHeader><CardTitle>Simular Férias</CardTitle></CardHeader><CardContent className="space-y-4"><div><Label>Salário</Label><Input type="number"value={salario||''}onChange={e=>setSalario(Number(e.target.value))}/></div><div><Label>Dias de Férias</Label><Input type="number"min={5}max={30}value={dias}onChange={e=>setDias(Number(e.target.value))}/></div><div><Label>Dias de Abono (venda)</Label><Input type="number"min={0}max={10}value={abono}onChange={e=>setAbono(Number(e.target.value))}/></div><div><Label>Dependentes IR</Label><Input type="number"min={0}value={dependentes}onChange={e=>setDependentes(Number(e.target.value))}/></div><Button onClick={calcular}className="w-full">Calcular</Button>{resultado&&<div className="p-4 bg-muted rounded-lg space-y-2"><div className="flex justify-between"><span>Férias + 1/3:</span><span>{formatCurrency(resultado.bruto)}</span></div><div className="flex justify-between"><span>INSS:</span><span className="text-red-600">-{formatCurrency(resultado.inss)}</span></div><div className="flex justify-between"><span>IRRF:</span><span className="text-red-600">-{formatCurrency(resultado.irrf)}</span></div><div className="flex justify-between border-t pt-2"><span>Líquido:</span><span className="font-bold text-lg">{formatCurrency(resultado.liquido)}</span></div></div>}</CardContent></Card></PageLayout>);}
