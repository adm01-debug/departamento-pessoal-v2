import React,{useState}from'react';import{useUsuarios,useResetSenha}from'@/hooks/useUsuario';import{PageLayout}from'@/components/layout/PageLayout';import{PageHeader}from'@/components/common/PageHeader';import{Card,CardContent}from'@/components/ui/card';import{Button}from'@/components/ui/button';import{Badge}from'@/components/ui/badge';import{DataTable}from'@/components/ui/data-table';import{Avatar}from'@/components/common/Avatar';import{UsuarioModal}from'@/components/modals/UsuarioModal';import{LoadingSpinner}from'@/components/common/LoadingSpinner';import{Plus,Edit,Key}from'lucide-react';export default function UsuariosPage(){const{data:usuarios,isLoading}=useUsuarios();const resetSenhaMutation=useResetSenha();const[modalOpen,setModalOpen]=useState(false);const[selectedUser,setSelectedUser]=useState<any>(null);const handleEdit=(u:any)=>{setSelectedUser(u);setModalOpen(true);};const handleResetSenha=(id:string)=>resetSenhaMutation.mutateAsync(id);const columns=[{accessorKey:'nome',header:'Usuário',cell:({row}:any)=><div className="flex items-center gap-3"><Avatar name={row.original.nome}size="sm"/><div><p className="font-medium">{row.original.nome}</p><p className="text-sm text-muted-foreground">{row.original.email}</p></div></div>},{accessorKey:'perfilNome',header:'Perfil'},{accessorKey:'ativo',header:'Status',cell:({row}:any)=><Badge className={row.original.ativo?'bg-green-100 text-green-800':'bg-red-100 text-red-800'}>{row.original.ativo?'Ativo':'Inativo'}</Badge>},{id:'actions',cell:({row}:any)=><div className="flex gap-1"><Button variant="ghost"size="sm"onClick={()=>handleEdit(row.original)}><Edit className="w-4 h-4"/></Button><Button variant="ghost"size="sm"onClick={()=>handleResetSenha(row.original.id)}><Key className="w-4 h-4"/></Button></div>}];if(isLoading)return<LoadingSpinner/>;return(<PageLayout><PageHeader title="Usuários"description="Gerencie os usuários do sistema"actions={<Button onClick={()=>{setSelectedUser(null);setModalOpen(true);}}><Plus className="w-4 h-4 mr-2"/>Novo Usuário</Button>}/><Card><CardContent className="p-0"><DataTable columns={columns}data={usuarios||[]}/></CardContent></Card><UsuarioModal open={modalOpen}onClose={()=>setModalOpen(false)}usuario={selectedUser}/></PageLayout>);}
