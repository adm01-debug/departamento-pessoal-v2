import React,{useState}from'react';import{useBeneficios,useDeleteBeneficio}from'@/hooks/useBeneficio';import{PageLayout}from'@/components/layout/PageLayout';import{PageHeader}from'@/components/common/PageHeader';import{Card,CardContent}from'@/components/ui/card';import{Button}from'@/components/ui/button';import{BeneficioList}from'@/components/lists/BeneficioList';import{BeneficioModal}from'@/components/modals/BeneficioModal';import{ConfirmDialog}from'@/components/common/ConfirmDialog';import{LoadingSpinner}from'@/components/common/LoadingSpinner';import{Plus}from'lucide-react';export default function BeneficiosPage(){const{data:beneficios,isLoading}=useBeneficios();const deleteMutation=useDeleteBeneficio();const[modalOpen,setModalOpen]=useState(false);const[selectedBeneficio,setSelectedBeneficio]=useState<any>(null);const[deleteId,setDeleteId]=useState<string|null>(null);const handleEdit=(b:any)=>{setSelectedBeneficio(b);setModalOpen(true);};const handleDelete=async()=>{if(deleteId){await deleteMutation.mutateAsync(deleteId);setDeleteId(null);}};if(isLoading)return<LoadingSpinner/>;return(<PageLayout><PageHeader title="Benefícios"description="Gerencie os benefícios dos colaboradores"actions={<Button onClick={()=>{setSelectedBeneficio(null);setModalOpen(true);}}><Plus className="w-4 h-4 mr-2"/>Novo Benefício</Button>}/><Card><CardContent className="p-0"><BeneficioList beneficios={beneficios||[]}onEdit={handleEdit}onDelete={id=>setDeleteId(id)}/></CardContent></Card><BeneficioModal open={modalOpen}onClose={()=>setModalOpen(false)}beneficio={selectedBeneficio}/><ConfirmDialog open={!!deleteId}onOpenChange={()=>setDeleteId(null)}title="Excluir Benefício"description="Tem certeza que deseja excluir este benefício?"onConfirm={handleDelete}variant="destructive"/></PageLayout>);}
