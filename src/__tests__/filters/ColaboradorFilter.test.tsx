import{describe,it,expect,vi}from'vitest';import{render,screen,fireEvent}from'@/test/utils';import{ColaboradorFilter}from'@/components/filters/ColaboradorFilter';describe('ColaboradorFilter',()=>{it.skip('should call onChange when search changes',()=>{const onChange=vi.fn();render(<ColaboradorFilter filtros={{busca:'',status:'',departamento:''}}departamentos={[]}onChange={onChange}onClear={vi.fn()}/>);fireEvent.change(screen.getByPlaceholderText(/buscar/i),{target:{value:'João'}});expect(onChange).toHaveBeenCalled();});});
