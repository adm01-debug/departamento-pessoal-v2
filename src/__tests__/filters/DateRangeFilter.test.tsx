import{describe,it,expect,vi}from'vitest';import{render,screen,fireEvent}from'@/test/utils';import{DateRangeFilter}from'@/components/filters/DateRangeFilter';describe('DateRangeFilter',()=>{it.skip('should call onChange when date changes',()=>{const onChange=vi.fn();render(<DateRangeFilter dataInicio=""dataFim=""onChange={onChange}onClear={vi.fn()}/>);const inputs=screen.getAllByRole('textbox');expect(inputs.length).toBeGreaterThan(0);});});
