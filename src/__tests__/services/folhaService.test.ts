import{describe,it,expect,vi,beforeEach}from'vitest';import{folhaService}from'@/services/folhaService';import{api}from'@/lib/api';vi.mock('@/lib/api');describe('folhaService',()=>{beforeEach(()=>{vi.clearAllMocks();});it('should calcular folha',async()=>{const mockData={totalProventos:5000};vi.mocked(api.post).mockResolvedValue({data:mockData});const result=await folhaService.calcular('2024-01');expect(api.post).toHaveBeenCalledWith('/folha/calcular',{competencia:'2024-01'});expect(result.totalProventos).toBe(5000);});it('should fechar folha',async()=>{vi.mocked(api.post).mockResolvedValue({data:{status:'fechada'}});const result=await folhaService.fechar('2024-01');expect(result.status).toBe('fechada');});});
