import{describe,it,expect,vi,beforeEach}from'vitest';import{folhaService}from'@/services/folhaService';import{api}from'@/lib/api';vi.mock('@/lib/api');describe('folhaService',()=>{beforeEach(()=>{vi.clearAllMocks();});it('should list folha by competencia',async()=>{const mockData=[{id:'1',competencia:'2024-01'}];vi.mocked(api.get).mockResolvedValue({data:mockData});const result=await folhaService.listar('2024-01');expect(api.get).toHaveBeenCalledWith('/folha',{params:{competencia:'2024-01'}});expect(result).toEqual(mockData);});it('should calculate folha',async()=>{vi.mocked(api.post).mockResolvedValue({data:{success:true}});const result=await folhaService.calcular('2024-01');expect(api.post).toHaveBeenCalledWith('/folha/calcular',{competencia:'2024-01'});expect(result.success).toBe(true);});});
