import{describe,it,expect,vi,beforeEach}from'vitest';import{folhaService}from'@/services/folhaService';import{api}from'@/lib/api';vi.mock('@/lib/api');describe('folhaService',()=>{beforeEach(()=>{vi.clearAllMocks();});it('should list folhas',async()=>{const mockData=[{id:'1',competencia:'2024-01'}];vi.mocked(api.get).mockResolvedValue({data:mockData});const result=await folhaService.listar();expect(api.get).toHaveBeenCalledWith('/folha');expect(result).toEqual(mockData);});it('should calculate folha',async()=>{vi.mocked(api.post).mockResolvedValue({data:{status:'calculada'}});const result=await folhaService.calcular('1');expect(api.post).toHaveBeenCalledWith('/folha/1/calcular');expect(result.status).toBe('calculada');});});
