import{describe,it,expect,vi,beforeEach}from'vitest';import{colaboradorService}from'@/services/colaboradorService';import{api}from'@/lib/api';vi.mock('@/lib/api');describe('colaboradorService',()=>{beforeEach(()=>{vi.clearAllMocks();});it('should list colaboradores',async()=>{const mockData=[{id:'1',nome:'Test'}];vi.mocked(api.get).mockResolvedValue({data:mockData});const result=await colaboradorService.listar();expect(api.get).toHaveBeenCalledWith('/colaboradores',{params:undefined});expect(result).toEqual(mockData);});it('should create colaborador',async()=>{const newData={nome:'Novo'};vi.mocked(api.post).mockResolvedValue({data:{id:'1',...newData}});const result=await colaboradorService.criar(newData);expect(api.post).toHaveBeenCalledWith('/colaboradores',newData);expect(result.nome).toBe('Novo');});});
