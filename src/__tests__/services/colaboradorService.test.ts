import{describe,it,expect,vi,beforeEach}from'vitest';import{colaboradorService}from'@/services/colaboradorService';import{api}from'@/lib/api';vi.mock('@/lib/api');describe('colaboradorService',()=>{beforeEach(()=>{vi.clearAllMocks();});it('should list colaboradores',async()=>{const mockData=[{id:'1',nome:'João'}];(api.get as any).mockResolvedValue({data:{data:mockData,total:1}});const result=await colaboradorService.listar();expect(result.data).toHaveLength(1);});});
