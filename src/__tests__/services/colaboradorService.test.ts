import{describe,it,expect,vi,beforeEach}from'vitest';import{colaboradorService}from'@/services/colaboradorService';import{api}from'@/lib/api';vi.mock('@/lib/api');describe('colaboradorService',()=>{beforeEach(()=>{vi.clearAllMocks();});it('should list colaboradores',async()=>{const mockData=[{id:'1',nome:'João'}];vi.mocked(api.get).mockResolvedValue({data:mockData});const result=await colaboradorService.listar();expect(api.get).toHaveBeenCalledWith('/colaboradores',expect.any(Object));expect(result).toEqual(mockData);});it('should create colaborador',async()=>{const newColaborador={nome:'Maria',cpf:'123'};vi.mocked(api.post).mockResolvedValue({data:{id:'2',...newColaborador}});const result=await colaboradorService.criar(newColaborador);expect(api.post).toHaveBeenCalledWith('/colaboradores',newColaborador);expect(result.nome).toBe('Maria');});});
