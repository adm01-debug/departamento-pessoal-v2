import{describe,it,expect,vi,beforeEach}from'vitest';import{authService}from'@/services/authService';import{api}from'@/lib/api';vi.mock('@/lib/api');describe('authService',()=>{beforeEach(()=>{vi.clearAllMocks();});it('should login successfully',async()=>{const mockResponse={token:'abc123',user:{id:'1',nome:'Test'}};vi.mocked(api.post).mockResolvedValue({data:mockResponse});const result=await authService.login({email:'test@test.com',senha:'123456'});expect(api.post).toHaveBeenCalledWith('/auth/login',{email:'test@test.com',senha:'123456'});expect(result.token).toBe('abc123');});it('should logout',async()=>{vi.mocked(api.post).mockResolvedValue({});await authService.logout();expect(api.post).toHaveBeenCalledWith('/auth/logout');});});
