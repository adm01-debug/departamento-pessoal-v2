import{describe,it,expect,vi,beforeEach}from'vitest';import{feriasService}from'@/services/feriasService';import{api}from'@/lib/api';vi.mock('@/lib/api');describe('feriasService',()=>{beforeEach(()=>{vi.clearAllMocks();});it('should list ferias',async()=>{const mockData=[{id:'1',colaboradorId:'1',dias:30}];vi.mocked(api.get).mockResolvedValue({data:mockData});const result=await feriasService.listar();expect(api.get).toHaveBeenCalledWith('/ferias',expect.any(Object));expect(result).toEqual(mockData);});it('should approve ferias',async()=>{vi.mocked(api.post).mockResolvedValue({data:{status:'aprovada'}});const result=await feriasService.aprovar('1');expect(api.post).toHaveBeenCalledWith('/ferias/1/aprovar');expect(result.status).toBe('aprovada');});});
