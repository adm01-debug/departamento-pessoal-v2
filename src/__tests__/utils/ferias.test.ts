import{describe,it,expect}from'vitest';import{calcularValorFerias}from'@/utils/ferias';describe('utils/ferias',()=>{it('should calculate ferias 30 dias',()=>{const result=calcularValorFerias({salario:3000,dias:30,abonoPecuniario:0,mediaAdicionais:0,dependentes:0});expect(result.valorFerias).toBe(3000);expect(result.tercoFerias).toBe(1000);expect(result.totalBruto).toBe(4000);});it('should calculate with abono',()=>{const result=calcularValorFerias({salario:3000,dias:20,abonoPecuniario:10,mediaAdicionais:0,dependentes:0});expect(result.valorAbono).toBe(1000);expect(result.tercoAbono).toBeCloseTo(333.33,1);});});
