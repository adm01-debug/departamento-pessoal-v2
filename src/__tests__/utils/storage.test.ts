import{describe,it,expect,beforeEach}from'vitest';import{storage}from'@/utils/storage';describe('utils/storage',()=>{beforeEach(()=>{localStorage.clear();});it('should set and get value',()=>{storage.set('key','value');expect(storage.get('key','')).toBe('value');});it('should return default if not found',()=>{expect(storage.get('notfound','default')).toBe('default');});it('should remove value',()=>{storage.set('key','value');storage.remove('key');expect(storage.get('key','default')).toBe('default');});});
