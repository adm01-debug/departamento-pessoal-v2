import{describe,it,expect}from'vitest';import{calcularINSS,calcularIRRF,calcularFGTS,calcularHorasExtras}from'@/utils/calculations';describe('calculations',()=>{describe('calcularINSS',()=>{it('should calculate INSS correctly',()=>{expect(calcularINSS(1500)).toBeGreaterThan(0);expect(calcularINSS(5000)).toBeGreaterThan(0);});});describe('calcularIRRF',()=>{it('should calculate IRRF correctly',()=>{const inss=calcularINSS(5000);expect(calcularIRRF(5000,inss,0)).toBeGreaterThanOrEqual(0);});it('should return 0 for low salary',()=>{expect(calcularIRRF(2000,150,0)).toBe(0);});});describe('calcularFGTS',()=>{it('should calculate FGTS as 8%',()=>{expect(calcularFGTS(1000)).toBe(80);expect(calcularFGTS(5000)).toBe(400);});});describe('calcularHorasExtras',()=>{it('should calculate 50% overtime correctly',()=>{expect(calcularHorasExtras(50,2,50)).toBe(150);});it('should calculate 100% overtime correctly',()=>{expect(calcularHorasExtras(50,2,100)).toBe(200);});});});
