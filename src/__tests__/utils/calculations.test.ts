import{describe,it,expect}from'vitest';import{calcularINSS,calcularIRRF}from'@/utils/calculations';describe('calculations',()=>{describe('calcularINSS',()=>{it('should calculate INSS for first bracket',()=>{const result=calcularINSS(1400);expect(result).toBeCloseTo(105,1);});it('should cap at ceiling',()=>{const result=calcularINSS(10000);expect(result).toBeLessThanOrEqual(900);});});describe('calcularIRRF',()=>{it('should return 0 for exempt range',()=>{expect(calcularIRRF(2000,0)).toBe(0);});it('should calculate IRRF with dependents',()=>{const result=calcularIRRF(5000,2);expect(result).toBeGreaterThan(0);});});});
