import{describe,it,expect}from'vitest';import{calcularINSS,calcularIRRF}from'@/utils/calculations';describe('calculations',()=>{describe('calcularINSS',()=>{it('should calculate INSS for first range',()=>{const result=calcularINSS(1500);expect(result).toBeCloseTo(112.5,2);});it('should calculate INSS for higher salary',()=>{const result=calcularINSS(5000);expect(result).toBeGreaterThan(0);});});describe('calcularIRRF',()=>{it('should return 0 for exempt range',()=>{const result=calcularIRRF(1500,0);expect(result).toBe(0);});it('should calculate IRRF correctly',()=>{const result=calcularIRRF(5000,0);expect(result).toBeGreaterThan(0);});});});
