import{describe,it,expect}from'vitest';import{calcularRescisao}from'@/utils/rescisao';describe('utils/rescisao',()=>{it('should calculate rescisao sem justa causa',()=>{const result=calcularRescisao({salario:3000,dataAdmissao:'2023-01-01',dataDemissao:'2024-06-15',tipo:'sem_justa_causa',avisoPrevio:'indenizado',saldoFGTS:5000,feriasVencidas:0,diasFerias:0,mediaHorasExtras:0});expect(result.total).toBeGreaterThan(0);expect(result.multaFGTS).toBe(2000);});it('should calculate rescisao justa causa',()=>{const result=calcularRescisao({salario:3000,dataAdmissao:'2023-01-01',dataDemissao:'2024-06-15',tipo:'justa_causa',avisoPrevio:'dispensado',saldoFGTS:5000,feriasVencidas:0,diasFerias:0,mediaHorasExtras:0});expect(result.multaFGTS).toBe(0);});});
