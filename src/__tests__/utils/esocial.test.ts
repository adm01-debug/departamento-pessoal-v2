import{describe,it,expect}from'vitest';import{EVENTOS_ESOCIAL,gerarXMLEvento,validarEvento}from'@/utils/esocial';describe('utils/esocial',()=>{it('should have eventos defined',()=>{expect(EVENTOS_ESOCIAL.S2200).toBeDefined();});it('should generate XML',()=>{const xml=gerarXMLEvento('S-2200',{});expect(xml).toContain('<?xml');expect(xml).toContain('eSocial');});it('should validate evento',()=>{const{valido,erros}=validarEvento('S-2200',null);expect(valido).toBe(false);expect(erros).toContain('Dados obrigatórios');});});
