import{describe,it,expect}from'vitest';import{handleApiError}from'@/utils/errorHandler';import{AxiosError}from'axios';describe('utils/errorHandler',()=>{it('should handle 401',()=>{const error=new AxiosError();error.response={status:401} as any;const result=handleApiError(error);expect(result.code).toBe('UNAUTHORIZED');});it('should handle 404',()=>{const error=new AxiosError();error.response={status:404} as any;const result=handleApiError(error);expect(result.code).toBe('NOT_FOUND');});it('should handle unknown error',()=>{const result=handleApiError('unknown');expect(result.message).toBe('Erro desconhecido.');});});
