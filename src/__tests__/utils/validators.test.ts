import{describe,it,expect}from'vitest';import{validarCPF,validarCNPJ,validarEmail}from'@/utils/validators';describe('validators',()=>{describe('validarCPF',()=>{it('should validate valid CPF',()=>{expect(validarCPF('529.982.247-25')).toBe(true);});it('should reject invalid CPF',()=>{expect(validarCPF('111.111.111-11')).toBe(false);});});describe('validarCNPJ',()=>{it('should validate valid CNPJ',()=>{expect(validarCNPJ('11.222.333/0001-81')).toBe(true);});});describe('validarEmail',()=>{it('should validate valid email',()=>{expect(validarEmail('test@example.com')).toBe(true);});it('should reject invalid email',()=>{expect(validarEmail('invalid')).toBe(false);});});});
