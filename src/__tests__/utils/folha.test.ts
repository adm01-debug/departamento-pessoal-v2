import{describe,it,expect}from'vitest';import{calcularFolha}from'@/utils/folha';describe('utils/folha',()=>{it('should calculate folha',()=>{const result=calcularFolha({salarioBase:3000,horasExtras50:10,horasExtras100:0,adicionalNoturno:0,comissoes:0,dependentes:0,valeTransporte:200,valeAlimentacao:0,planoSaude:100,outrosDescontos:0});expect(result.totalProventos).toBeGreaterThan(3000);expect(result.inss).toBeGreaterThan(0);expect(result.salarioLiquido).toBeGreaterThan(0);});it('should calculate VT discount',()=>{const result=calcularFolha({salarioBase:3000,horasExtras50:0,horasExtras100:0,adicionalNoturno:0,comissoes:0,dependentes:0,valeTransporte:300,valeAlimentacao:0,planoSaude:0,outrosDescontos:0});expect(result.vtDesconto).toBe(180);});});
