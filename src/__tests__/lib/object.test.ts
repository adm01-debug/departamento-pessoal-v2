import{describe,it,expect}from'vitest';import{pick,omit,isEmpty,deepClone,deepMerge}from'@/lib/object';describe('lib/object',()=>{describe('pick',()=>{it('should pick keys',()=>{const obj={a:1,b:2,c:3};expect(pick(obj,['a','c'])).toEqual({a:1,c:3});});});describe('omit',()=>{it('should omit keys',()=>{const obj={a:1,b:2,c:3};expect(omit(obj,['b'])).toEqual({a:1,c:3});});});describe('isEmpty',()=>{it('should check empty',()=>{expect(isEmpty({})).toBe(true);expect(isEmpty({a:1})).toBe(false);});});describe('deepClone',()=>{it('should clone deeply',()=>{const obj={a:{b:1}};const clone=deepClone(obj);clone.a.b=2;expect(obj.a.b).toBe(1);});});});
