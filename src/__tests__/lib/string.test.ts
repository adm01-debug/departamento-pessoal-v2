import{describe,it,expect}from'vitest';import{capitalize,capitalizeWords,slugify,truncate,removeAccents,onlyNumbers}from'@/lib/string';describe('lib/string',()=>{describe('capitalize',()=>{it('should capitalize first letter',()=>{expect(capitalize('hello')).toBe('Hello');});});describe('slugify',()=>{it('should create slug',()=>{expect(slugify('Hello World')).toBe('hello-world');});});describe('truncate',()=>{it('should truncate long strings',()=>{expect(truncate('Hello World',5)).toBe('Hello...');});});describe('onlyNumbers',()=>{it('should keep only numbers',()=>{expect(onlyNumbers('abc123def456')).toBe('123456');});});});
