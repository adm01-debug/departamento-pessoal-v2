import{describe,it,expect}from'vitest';import{formatDate,formatDateTime,calcularIdade,calcularTempoEmpresa}from'@/lib/date';describe('lib/date',()=>{describe('formatDate',()=>{it('should format date',()=>{expect(formatDate('2024-01-15')).toBe('15/01/2024');});it('should return - for null',()=>{expect(formatDate(null)).toBe('-');});});describe('calcularIdade',()=>{it('should calculate age',()=>{const age=calcularIdade('1990-01-01');expect(age).toBeGreaterThanOrEqual(33);});});});
