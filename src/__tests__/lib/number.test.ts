import{describe,it,expect}from'vitest';import{formatCurrency,formatPercentage,formatNumber,parseCurrency,roundTo,clamp}from'@/lib/number';describe('lib/number',()=>{describe('formatCurrency',()=>{it('should format currency',()=>{const result=formatCurrency(1234.56);expect(result).toContain('1.234,56');});});describe('formatPercentage',()=>{it('should format percentage',()=>{expect(formatPercentage(75.5)).toBe('75.50%');});});describe('clamp',()=>{it('should clamp value',()=>{expect(clamp(5,0,10)).toBe(5);expect(clamp(-5,0,10)).toBe(0);expect(clamp(15,0,10)).toBe(10);});});});
