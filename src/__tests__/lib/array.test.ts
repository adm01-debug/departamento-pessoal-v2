import{describe,it,expect}from'vitest';import{groupBy,sortBy,unique,uniqueBy,chunk,sum,average}from'@/lib/array';describe('lib/array',()=>{describe('groupBy',()=>{it('should group by key',()=>{const data=[{type:'a',val:1},{type:'b',val:2},{type:'a',val:3}];const result=groupBy(data,'type');expect(result['a']).toHaveLength(2);expect(result['b']).toHaveLength(1);});});describe('unique',()=>{it('should remove duplicates',()=>{expect(unique([1,2,2,3,3,3])).toEqual([1,2,3]);});});describe('sum',()=>{it('should sum numbers',()=>{expect(sum([1,2,3,4,5])).toBe(15);});});describe('average',()=>{it('should calculate average',()=>{expect(average([1,2,3,4,5])).toBe(3);});});});
