import{describe,it,expect,vi}from'vitest';import{render,screen,fireEvent}from'@testing-library/react';import{EmptyState}from'@/components/common/EmptyState';describe('EmptyState',()=>{it('should render title',()=>{render(<EmptyState title="Nenhum item"/>);expect(screen.getByText('Nenhum item')).toBeInTheDocument();});it('should render description',()=>{render(<EmptyState title="Test"description="Descrição teste"/>);expect(screen.getByText('Descrição teste')).toBeInTheDocument();});it('should call action onClick',()=>{const onClick=vi.fn();render(<EmptyState title="Test"action={{label:'Adicionar',onClick}}/>);fireEvent.click(screen.getByText('Adicionar'));expect(onClick).toHaveBeenCalled();});});
