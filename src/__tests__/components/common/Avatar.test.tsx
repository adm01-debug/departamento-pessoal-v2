import{describe,it,expect}from'vitest';import{render,screen}from'@testing-library/react';import{Avatar}from'@/components/common/Avatar';describe('Avatar',()=>{it('should render initials',()=>{render(<Avatar name="João Silva"/>);expect(screen.getByText('JS')).toBeInTheDocument();});it('should render single initial',()=>{render(<Avatar name="João"/>);expect(screen.getByText('JO')).toBeInTheDocument();});});
