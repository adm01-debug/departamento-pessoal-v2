import{describe,it,expect}from'vitest';import{render,screen}from'@testing-library/react';import{StatusBadge}from'@/components/common/StatusBadge';describe('StatusBadge',()=>{it('should render status label',()=>{render(<StatusBadge status="ativo"/>);expect(screen.getByText('Ativo')).toBeInTheDocument();});it('should apply correct color class',()=>{const{container}=render(<StatusBadge status="ativo"/>);expect(container.firstChild).toHaveClass('bg-green-100');});it('should use custom label',()=>{render(<StatusBadge status="ativo"customLabel="Funcionando"/>);expect(screen.getByText('Funcionando')).toBeInTheDocument();});});
