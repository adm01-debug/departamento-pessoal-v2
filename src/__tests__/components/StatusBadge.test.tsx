import{describe,it,expect}from'vitest';import{render,screen}from'@testing-library/react';import{StatusBadge}from'@/components/common/StatusBadge';describe('StatusBadge',()=>{it('should render default label',()=>{render(<StatusBadge status="ativo"/>);expect(screen.getByText('Ativo')).toBeInTheDocument();});it('should render custom label',()=>{render(<StatusBadge status="ativo"customLabel="Online"/>);expect(screen.getByText('Online')).toBeInTheDocument();});it('should apply correct color class',()=>{const{container}=render(<StatusBadge status="inativo"/>);expect(container.querySelector('.bg-gray-100')).toBeInTheDocument();});});
