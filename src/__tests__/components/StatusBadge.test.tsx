import{describe,it,expect}from'vitest';import{render,screen}from'@testing-library/react';import{StatusBadge}from'@/components/common/StatusBadge';describe('StatusBadge',()=>{it('should render ativo status',()=>{render(<StatusBadge status="ativo"/>);expect(screen.getByText('Ativo')).toBeInTheDocument();});it('should render custom label',()=>{render(<StatusBadge status="ativo"customLabel="Custom Label"/>);expect(screen.getByText('Custom Label')).toBeInTheDocument();});it('should apply correct color classes',()=>{const{container}=render(<StatusBadge status="ativo"/>);expect(container.querySelector('.bg-green-100')).toBeInTheDocument();});});
