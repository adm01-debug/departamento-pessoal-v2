import{describe,it,expect}from'vitest';import{render,screen}from'@testing-library/react';import{Avatar}from'@/components/common/Avatar';describe('Avatar',()=>{it('should render initials',()=>{render(<Avatar name="João Silva"/>);expect(screen.getByText('JS')).toBeInTheDocument();});it('should render image when provided',()=>{render(<Avatar name="João"image="/avatar.jpg"/>);const img=screen.getByRole('img');expect(img).toHaveAttribute('src','/avatar.jpg');});it('should apply size classes',()=>{const{container}=render(<Avatar name="Test"size="lg"/>);expect(container.firstChild).toHaveClass('w-12','h-12');});});
