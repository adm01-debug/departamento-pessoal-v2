import{describe,it,expect,vi}from'vitest';import{render,screen,fireEvent}from'@testing-library/react';import{ColaboradorForm}from'@/components/forms/ColaboradorForm';describe('ColaboradorForm',()=>{const mockCargos=[{id:'1',nome:'Desenvolvedor'}];const mockDepts=[{id:'1',nome:'TI'}];const mockSubmit=vi.fn();const mockCancel=vi.fn();it('should render form fields',()=>{render(<ColaboradorForm cargos={mockCargos}departamentos={mockDepts}onSubmit={mockSubmit}onCancel={mockCancel}/>);expect(screen.getByPlaceholderText('Nome completo')).toBeInTheDocument();});it('should call onCancel',()=>{render(<ColaboradorForm cargos={mockCargos}departamentos={mockDepts}onSubmit={mockSubmit}onCancel={mockCancel}/>);fireEvent.click(screen.getByText('Cancelar'));expect(mockCancel).toHaveBeenCalled();});});
