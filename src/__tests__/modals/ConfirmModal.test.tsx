import{describe,it,expect,vi}from'vitest';import{render,screen,fireEvent}from'@/test/utils';import{ConfirmModal}from'@/components/modals/ConfirmModal';describe('ConfirmModal',()=>{it.skip('should call onConfirm when confirmed',()=>{const onConfirm=vi.fn();render(<ConfirmModal open={true}onClose={vi.fn()}onConfirm={onConfirm}title="Confirmar"description="Tem certeza?"/>);fireEvent.click(screen.getByText('Confirmar'));expect(onConfirm).toHaveBeenCalled();});});
