import{describe,it,expect}from'vitest';import{render}from'@/test/utils';import{FolhaTable}from'@/components/tables/FolhaTable';describe('FolhaTable',()=>{it.skip('should render table with data',()=>{const itens=[{id:'1',colaborador:'João',cargo:'Dev',salarioBase:5000,proventos:5500,descontos:1000,liquido:4500,status:'calculado'}];const{container}=render(<FolhaTable itens={itens}/>);expect(container.querySelector('table')).toBeInTheDocument();});});
