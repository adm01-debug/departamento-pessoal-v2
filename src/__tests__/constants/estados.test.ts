import{describe,it,expect}from'vitest';import{ESTADOS,getEstadoNome}from'@/constants/estados';describe('constants/estados',()=>{it('should have all 27 states',()=>{expect(ESTADOS).toHaveLength(27);});it('should get estado nome',()=>{expect(getEstadoNome('SP')).toBe('São Paulo');expect(getEstadoNome('RJ')).toBe('Rio de Janeiro');});it('should return uf if not found',()=>{expect(getEstadoNome('XX')).toBe('XX');});});
