import{describe,it,expect}from'vitest';import{TABELA_INSS_2024,TABELA_IRRF_2024,SALARIO_MINIMO}from'@/constants/tabelas';describe('constants/tabelas',()=>{it('should have INSS table',()=>{expect(TABELA_INSS_2024).toHaveLength(4);expect(TABELA_INSS_2024[0].aliquota).toBe(7.5);});it('should have IRRF table',()=>{expect(TABELA_IRRF_2024).toHaveLength(5);expect(TABELA_IRRF_2024[0].aliquota).toBe(0);});it('should have correct minimum wage',()=>{expect(SALARIO_MINIMO).toBe(1412);});});
