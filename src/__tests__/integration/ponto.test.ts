import{describe,it,expect,vi,beforeEach}from'vitest';import{pontoService}from'@/services/pontoService';import{api}from'@/lib/api';vi.mock('@/lib/api');describe('Ponto Integration',()=>{beforeEach(()=>{vi.clearAllMocks();});it('should register ponto',async()=>{const mockPonto={id:'1',colaboradorId:'c1',data:'2024-01-15',entrada:'08:00',status:'regular'};vi.mocked(api.post).mockResolvedValue({data:mockPonto});const registered=await pontoService.registrar('c1','entrada');expect(registered.entrada).toBe('08:00');});it('should get espelho',async()=>{vi.mocked(api.get).mockResolvedValue({data:{registros:[],resumo:{diasTrabalhados:22}}});const espelho=await pontoService.getEspelho('c1','01','2024');expect(espelho.resumo.diasTrabalhados).toBe(22);});});
