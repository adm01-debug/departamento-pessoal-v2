import{describe,it,expect,beforeAll,afterAll}from'vitest';import{server}from'@/mocks/server';import{colaboradorService}from'@/services/colaboradorService';describe('Colaborador Integration',()=>{beforeAll(()=>server.listen());afterAll(()=>server.close());it('should list colaboradores',async()=>{const result=await colaboradorService.listar();expect(result.data).toBeDefined();expect(Array.isArray(result.data)).toBe(true);});});
