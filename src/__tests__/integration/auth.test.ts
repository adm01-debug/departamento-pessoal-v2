import{describe,it,expect,beforeAll,afterAll}from'vitest';import{server}from'@/mocks/server';import{authService}from'@/services/authService';describe('Auth Integration',()=>{beforeAll(()=>server.listen());afterAll(()=>server.close());it('should login and receive token',async()=>{const result=await authService.login('admin@test.com','password');expect(result.token).toBeDefined();expect(result.user).toBeDefined();});});
