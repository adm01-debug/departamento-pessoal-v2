import{describe,it,expect}from'vitest';import{pontoSchema}from'@/schemas/ponto';describe('pontoSchema',()=>{it('should validate valid ponto',()=>{const valid={colaboradorId:'c1',data:'2024-01-15',entrada:'08:00',saida:'17:00',tipo:'normal'};const result=pontoSchema.safeParse(valid);expect(result.success).toBe(true);});it('should reject invalid time format',()=>{const invalid={colaboradorId:'c1',data:'2024-01-15',entrada:'8:00',tipo:'normal'};const result=pontoSchema.safeParse(invalid);expect(result.success).toBe(false);});it('should require justificativa for ajuste',()=>{const invalid={colaboradorId:'c1',data:'2024-01-15',entrada:'08:00',tipo:'ajuste'};const result=pontoSchema.safeParse(invalid);expect(result.success).toBe(false);});});
