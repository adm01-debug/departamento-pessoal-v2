import{describe,it,expect}from'vitest';import{usuarioSchema,loginSchema}from'@/schemas/usuario';describe('usuarioSchema',()=>{it('should validate valid user',()=>{const data={nome:'Admin',email:'admin@test.com',perfil:'admin',ativo:true};const result=usuarioSchema.safeParse(data);expect(result.success).toBe(true);});});describe('loginSchema',()=>{it('should validate login data',()=>{const data={email:'test@test.com',senha:'password'};const result=loginSchema.safeParse(data);expect(result.success).toBe(true);});it('should reject invalid email',()=>{const data={email:'invalid',senha:'password'};const result=loginSchema.safeParse(data);expect(result.success).toBe(false);});});
