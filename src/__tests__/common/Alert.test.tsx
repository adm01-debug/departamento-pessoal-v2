import{describe,it,expect,vi}from'vitest';import{render,screen,fireEvent}from'@/test/utils';import{Alert}from'@/components/common/Alert';describe('Alert',()=>{it('should render alert with title',()=>{render(<Alert type="info"title="Info Alert"/>);expect(screen.getByText('Info Alert')).toBeInTheDocument();});it('should call onClose',()=>{const onClose=vi.fn();render(<Alert type="info"title="Alert"onClose={onClose}/>);fireEvent.click(screen.getByRole('button'));expect(onClose).toHaveBeenCalled();});});
