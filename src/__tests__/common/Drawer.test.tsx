import{describe,it,expect,vi}from'vitest';import{render,screen}from'@/test/utils';import{Drawer}from'@/components/common/Drawer';describe('Drawer',()=>{it('should render drawer when open',()=>{render(<Drawer open={true}onClose={vi.fn()}title="Test Drawer"><p>Content</p></Drawer>);expect(screen.getByText('Test Drawer')).toBeInTheDocument();expect(screen.getByText('Content')).toBeInTheDocument();});});
