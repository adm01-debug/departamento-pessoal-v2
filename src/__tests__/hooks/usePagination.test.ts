import{describe,it,expect}from'vitest';import{renderHook,act}from'@testing-library/react';import{usePagination}from'@/hooks/usePagination';describe('usePagination',()=>{const data=Array.from({length:25},(_,i)=>({id:i+1}));it('should return first page',()=>{const{result}=renderHook(()=>usePagination({data,pageSize:10}));expect(result.current.currentPage).toBe(1);expect(result.current.paginatedData).toHaveLength(10);expect(result.current.totalPages).toBe(3);});it('should navigate to next page',()=>{const{result}=renderHook(()=>usePagination({data,pageSize:10}));act(()=>{result.current.nextPage();});expect(result.current.currentPage).toBe(2);});it('should change page size',()=>{const{result}=renderHook(()=>usePagination({data,pageSize:10}));act(()=>{result.current.setPageSize(25);});expect(result.current.paginatedData).toHaveLength(25);expect(result.current.totalPages).toBe(1);});});
