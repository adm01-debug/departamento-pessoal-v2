import{describe,it,expect,vi}from'vitest';import{render,screen,waitFor}from'@testing-library/react';import{AuthProvider,useAuth}from'@/contexts/AuthContext';import{BrowserRouter}from'react-router-dom';function TestComponent(){const{signed,user}=useAuth();return<div>{signed?user?.nome:'Not signed'}</div>;}describe('AuthContext',()=>{it('should start unsigned',()=>{render(<BrowserRouter><AuthProvider><TestComponent/></AuthProvider></BrowserRouter>);expect(screen.getByText('Not signed')).toBeInTheDocument();});});
