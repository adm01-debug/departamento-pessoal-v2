import dayjs from'dayjs';import'dayjs/locale/pt-br';import customParseFormat from'dayjs/plugin/customParseFormat';import relativeTime from'dayjs/plugin/relativeTime';import isBetween from'dayjs/plugin/isBetween';dayjs.locale('pt-br');dayjs.extend(customParseFormat);dayjs.extend(relativeTime);dayjs.extend(isBetween);export{dayjs};export function formatDate(date:string|Date,format='DD/MM/YYYY'):string{return dayjs(date).format(format);}export function formatDateTime(date:string|Date):string{return dayjs(date).format('DD/MM/YYYY HH:mm');}export function fromNow(date:string|Date):string{return dayjs(date).fromNow();}export function isBetweenDates(date:string|Date,start:string|Date,end:string|Date):boolean{return dayjs(date).isBetween(start,end,'day','[]');}
