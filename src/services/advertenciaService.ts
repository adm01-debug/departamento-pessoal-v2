import { supabase } from '@/integrations/supabase/client';

export const advertenciaService = {
  async getAll() {
    const { data, error } = await supabase.from('advertencia').select('*');
    if (error) throw error;
    return data || [];
  },
  async getById(id: string) {
    const { data, error } = await supabase.from('advertencia').select('*').eq('id', id).single();
    if (error) throw error;
    return data;
  },
  async create(item: any) {
    const { data, error } = await supabase.from('advertencia').insert(item).select().single();
    if (error) throw error;
    return data;
  },
  async update(id: string, item: any) {
    const { data, error } = await supabase.from('advertencia').update(item).eq('id', id).select().single();
    if (error) throw error;
    return data;
  },
  async delete(id: string) {
    const { error } = await supabase.from('advertencia').delete().eq('id', id);
    if (error) throw error;
  },
};
export default advertenciaService;
