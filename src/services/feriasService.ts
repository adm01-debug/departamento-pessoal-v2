import{api}from'@/lib/api';import{ENDPOINTS}from'@/api/endpoints';import{Ferias}from'@/types/ferias';interface ListParams{colaboradorId?:string;status?:string;ano?:number;}export const feriasService={async listar(params?:ListParams):Promise<Ferias[]>{const{data}=await api.get(ENDPOINTS.FERIAS.BASE,{params});return data;},async buscar(id:string):Promise<Ferias>{const{data}=await api.get(ENDPOINTS.FERIAS.BY_ID(id));return data;},async criar(dados:Partial<Ferias>):Promise<Ferias>{const{data}=await api.post(ENDPOINTS.FERIAS.BASE,dados);return data;},async aprovar(id:string):Promise<Ferias>{const{data}=await api.post(ENDPOINTS.FERIAS.APROVAR(id));return data;},async rejeitar(id:string,motivo?:string):Promise<Ferias>{const{data}=await api.post(ENDPOINTS.FERIAS.REJEITAR(id),{motivo});return data;}};
