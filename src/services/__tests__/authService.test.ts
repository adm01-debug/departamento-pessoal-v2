import{describe,it,expect,vi}from'vitest';import{authService}from'../authService';import{server}from'@/test/mocks/server';import{http,HttpResponse}from'msw';describe('authService',()=>{it('login with valid credentials',async()=>{const result=await authService.login('admin@test.com','password');expect(result.user).toBeDefined();expect(result.token).toBeDefined();});it('login with invalid credentials throws',async()=>{server.use(http.post('*/auth/login',()=>HttpResponse.json({message:'Invalid'},{status:401})));await expect(authService.login('wrong@test.com','wrong')).rejects.toThrow();});});
