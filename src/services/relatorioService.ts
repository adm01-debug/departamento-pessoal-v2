import{api}from'@/lib/api';export const relatorioService={async gerar(tipo:string,params:any){const r=await api.get(`/relatorios/${tipo}`,{params,responseType:'blob'});return r.data;},async listarAgendados(){const r=await api.get('/relatorios/agendados');return r.data;},async agendar(data:any){const r=await api.post('/relatorios/agendar',data);return r.data;},async cancelarAgendamento(id:string){await api.delete(`/relatorios/agendados/${id}`);},async folhaPagamento(competencia:string,formato:'pdf'|'xlsx'){return this.gerar('folha',{competencia,formato});},async colaboradores(filtros:any,formato:'pdf'|'xlsx'){return this.gerar('colaboradores',{...filtros,formato});},async ferias(periodo:any,formato:'pdf'|'xlsx'){return this.gerar('ferias',{...periodo,formato});},async ponto(mes:string,formato:'pdf'|'xlsx'){return this.gerar('ponto',{mes,formato});}};
