import{api}from'@/lib/api';export const uploadService={async upload(file:File,folder?:string){const formData=new FormData();formData.append('file',file);if(folder)formData.append('folder',folder);const r=await api.post('/uploads',formData,{headers:{'Content-Type':'multipart/form-data'}});return r.data;},async uploadMultiple(files:File[],folder?:string){const formData=new FormData();files.forEach(f=>formData.append('files',f));if(folder)formData.append('folder',folder);const r=await api.post('/uploads/multiple',formData,{headers:{'Content-Type':'multipart/form-data'}});return r.data;},async delete(fileId:string){await api.delete(`/uploads/${fileId}`);},async getUrl(fileId:string){const r=await api.get(`/uploads/${fileId}/url`);return r.data.url;}};
