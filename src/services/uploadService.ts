import{api}from'@/lib/api';export interface UploadResult{id:string;nome:string;url:string;tipo:string;tamanho:number;}export const uploadService={async upload(file:File,folder?:string):Promise<UploadResult>{const formData=new FormData();formData.append('file',file);if(folder)formData.append('folder',folder);const r=await api.post('/uploads',formData,{headers:{'Content-Type':'multipart/form-data'}});return r.data;},async uploadMultiple(files:File[],folder?:string):Promise<UploadResult[]>{const formData=new FormData();files.forEach(f=>formData.append('files',f));if(folder)formData.append('folder',folder);const r=await api.post('/uploads/multiple',formData,{headers:{'Content-Type':'multipart/form-data'}});return r.data;},async delete(id:string){await api.delete(`/uploads/${id}`);},async getUrl(id:string){const r=await api.get(`/uploads/${id}/url`);return r.data.url;}};
