import{api}from'@/lib/api';export const authService={async login(email:string,senha:string){const r=await api.post('/auth/login',{email,senha});return r.data;},async logout(){await api.post('/auth/logout');},async refreshToken(){const r=await api.post('/auth/refresh');return r.data;},async forgotPassword(email:string){const r=await api.post('/auth/forgot-password',{email});return r.data;},async resetPassword(token:string,senha:string){const r=await api.post('/auth/reset-password',{token,senha});return r.data;},async changePassword(senhaAtual:string,novaSenha:string){const r=await api.post('/auth/change-password',{senhaAtual,novaSenha});return r.data;},async me(){const r=await api.get('/auth/me');return r.data;}};
