import{api}from'@/lib/api';export const auditService={async listar(params?:{entidade?:string;usuarioId?:string;dataInicio?:string;dataFim?:string;page?:number;limit?:number}){const r=await api.get('/audit-logs',{params});return r.data;},async buscar(id:string){const r=await api.get(`/audit-logs/${id}`);return r.data;},async registrar(data:{acao:string;entidade:string;entidadeId:string;dados?:any}){const r=await api.post('/audit-logs',data);return r.data;},async exportar(params:any){const r=await api.get('/audit-logs/export',{params,responseType:'blob'});return r.data;}};
