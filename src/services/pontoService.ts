import{api}from'@/lib/api';import{Ponto}from'@/types/ponto';export const pontoService={listar:async(params?:any):Promise<Ponto[]>=>{const{data}=await api.get('/ponto',{params});return data;},registrar:async(tipo:string):Promise<Ponto>=>{const{data}=await api.post('/ponto/registrar',{tipo,dataHora:new Date().toISOString()});return data;},ajustar:async(id:string,ajustes:Partial<Ponto>):Promise<Ponto>=>{const{data}=await api.put(`/ponto/${id}`,ajustes);return data;},getEspelho:async(colaboradorId:string,mes:string,ano:string):Promise<Ponto[]>=>{const{data}=await api.get('/ponto/espelho',{params:{colaboradorId,mes,ano}});return data;}};
