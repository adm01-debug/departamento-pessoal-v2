import{api}from'@/lib/api';import{ENDPOINTS}from'@/api/endpoints';import{Beneficio,BeneficioColaborador}from'@/types/beneficio';export const beneficioService={async listar():Promise<Beneficio[]>{const{data}=await api.get(ENDPOINTS.BENEFICIOS.BASE);return data;},async buscar(id:string):Promise<Beneficio>{const{data}=await api.get(`${ENDPOINTS.BENEFICIOS.BASE}/${id}`);return data;},async criar(dados:Partial<Beneficio>):Promise<Beneficio>{const{data}=await api.post(ENDPOINTS.BENEFICIOS.BASE,dados);return data;},async atualizar(id:string,dados:Partial<Beneficio>):Promise<Beneficio>{const{data}=await api.put(`${ENDPOINTS.BENEFICIOS.BASE}/${id}`,dados);return data;},async excluir(id:string):Promise<void>{await api.delete(`${ENDPOINTS.BENEFICIOS.BASE}/${id}`);},async atribuirColaborador(dados:Partial<BeneficioColaborador>):Promise<BeneficioColaborador>{const{data}=await api.post(`${ENDPOINTS.BENEFICIOS.BASE}/colaborador`,dados);return data;}};
