import { describe, it, expect } from 'vitest';
import { useCopyToClipboard } from '../useCopyToClipboard';
describe('useCopyToClipboard', () => { it('should be defined', () => { expect(useCopyToClipboard).toBeDefined(); }); });
