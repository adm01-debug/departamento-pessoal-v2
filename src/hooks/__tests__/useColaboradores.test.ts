import { describe, it, expect } from 'vitest';
import { useColaboradores } from '../useColaboradores';
describe('useColaboradores', () => { it('defined', () => { expect(useColaboradores).toBeDefined(); }); });
