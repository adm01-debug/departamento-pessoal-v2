import { describe, it, expect } from 'vitest';
import { useTableFilter } from '../useTableFilter';
describe('useTableFilter', () => { it('defined', () => { expect(useTableFilter).toBeDefined(); }); });
