import { describe, it, expect } from 'vitest';
import { useDashboardConfig } from '../useDashboardConfig';
describe('useDashboardConfig', () => { it('defined', () => { expect(useDashboardConfig).toBeDefined(); }); });
