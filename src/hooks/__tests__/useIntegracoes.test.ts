import { describe, it, expect } from 'vitest';
import { useIntegracoes } from '../useIntegracoes';
describe('useIntegracoes', () => { it('defined', () => { expect(useIntegracoes).toBeDefined(); }); });
