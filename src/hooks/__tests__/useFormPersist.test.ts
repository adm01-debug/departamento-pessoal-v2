import { describe, it, expect } from 'vitest';
import { useFormPersist } from '../useFormPersist';
describe('useFormPersist', () => { it('should be defined', () => { expect(useFormPersist).toBeDefined(); }); });
