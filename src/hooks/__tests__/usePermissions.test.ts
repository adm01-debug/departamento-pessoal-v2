import { describe, it, expect } from 'vitest';
import { usePermissions } from '../usePermissions';
describe('usePermissions', () => { it('defined', () => { expect(usePermissions).toBeDefined(); }); });
