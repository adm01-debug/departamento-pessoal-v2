import { describe, it, expect } from 'vitest';
import { useCalculoFolha } from '../useCalculoFolha';
describe('useCalculoFolha', () => { it('defined', () => { expect(useCalculoFolha).toBeDefined(); }); });
