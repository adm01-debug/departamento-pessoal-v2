import { describe, it, expect } from 'vitest';
import { useAuditoriaWrapper } from '../useAuditoriaWrapper';
describe('useAuditoriaWrapper', () => { it('defined', () => { expect(useAuditoriaWrapper).toBeDefined(); }); });
