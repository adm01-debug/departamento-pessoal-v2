import { describe, it, expect } from 'vitest';
import { useBitrix24Sync } from '../useBitrix24Sync';
describe('useBitrix24Sync', () => { it('defined', () => { expect(useBitrix24Sync).toBeDefined(); }); });
