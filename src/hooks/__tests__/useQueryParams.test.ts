import { describe, it, expect } from 'vitest';
import { useQueryParams } from '../useQueryParams';
describe('useQueryParams', () => { it('defined', () => { expect(useQueryParams).toBeDefined(); }); });
