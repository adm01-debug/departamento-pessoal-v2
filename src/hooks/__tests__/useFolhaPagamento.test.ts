import { describe, it, expect } from 'vitest';
import { useFolhaPagamento } from '../useFolhaPagamento';
describe('useFolhaPagamento', () => { it('defined', () => { expect(useFolhaPagamento).toBeDefined(); }); });
