import { describe, it, expect } from 'vitest';
import { useDocumentTitle } from '../useDocumentTitle';
describe('useDocumentTitle', () => { it('should be defined', () => { expect(useDocumentTitle).toBeDefined(); }); });
