import { describe, it, expect } from 'vitest';
import { useLazyComponent } from '../useLazyComponent';
describe('useLazyComponent', () => { it('should be defined', () => { expect(useLazyComponent).toBeDefined(); }); });
