import { describe, it, expect } from 'vitest';
import { useAfastamentos } from '../useAfastamentos';
describe('useAfastamentos', () => { it('defined', () => { expect(useAfastamentos).toBeDefined(); }); });
