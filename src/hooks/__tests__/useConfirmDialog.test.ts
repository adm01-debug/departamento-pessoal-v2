import { describe, it, expect } from 'vitest';
import { useConfirmDialog } from '../useConfirmDialog';
describe('useConfirmDialog', () => { it('defined', () => { expect(useConfirmDialog).toBeDefined(); }); });
