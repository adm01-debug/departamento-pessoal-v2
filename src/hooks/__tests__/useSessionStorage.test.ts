import { describe, it, expect } from 'vitest';
import { useSessionStorage } from '../useSessionStorage';
describe('useSessionStorage', () => { it('defined', () => { expect(useSessionStorage).toBeDefined(); }); });
