import { describe, it, expect } from 'vitest';
import { useEventListener } from '../useEventListener';
describe('useEventListener', () => { it('defined', () => { expect(useEventListener).toBeDefined(); }); });
