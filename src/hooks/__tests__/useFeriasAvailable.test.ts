import { describe, it, expect } from 'vitest';
import { useFeriasAvailable } from '../useFeriasAvailable';
describe('useFeriasAvailable', () => { it('defined', () => { expect(useFeriasAvailable).toBeDefined(); }); });
