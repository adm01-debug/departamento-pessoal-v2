import{useState,useCallback}from'react';import{useToast}from'./useToast';export function useClipboard(){const[copied,setCopied]=useState(false);const{toast}=useToast();const copy=useCallback(async(text:string)=>{try{await navigator.clipboard.writeText(text);setCopied(true);toast({title:'Copiado!'});setTimeout(()=>setCopied(false),2000);return true;}catch{toast({title:'Erro ao copiar',variant:'destructive'});return false;}},[toast]);return{copy,copied};}
