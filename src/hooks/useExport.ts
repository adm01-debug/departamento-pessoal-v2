// @ts-nocheck
import{useState}from'react';import{api}from'@/lib/api';import{useToast}from'./useToast';type ExportFormat='pdf'|'xlsx'|'csv';export function useExport(){const[loading,setLoading]=useState(false);const{toast}=useToast();const exportData=async(endpoint:string,filename:string,format:ExportFormat='pdf',params?:any)=>{try{setLoading(true);const r=await api.get(endpoint,{params:{...params,format},responseType:'blob'});const blob=r.data;const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`${filename}.${format}`;a.click();URL.revokeObjectURL(url);toast({title:'Exportação concluída'});}catch(e){toast({title:'Erro na exportação',variant:'destructive'});}finally{setLoading(false);}};return{exportData,loading};}