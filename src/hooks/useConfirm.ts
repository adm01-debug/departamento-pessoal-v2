import{useState,useCallback}from'react';interface ConfirmState{isOpen:boolean;title:string;message:string;onConfirm:()=>void;}const initialState:ConfirmState={isOpen:false,title:'',message:'',onConfirm:()=>{}};export function useConfirm(){const[state,setState]=useState(initialState);const confirm=useCallback((title:string,message:string):Promise<boolean>=>{return new Promise(resolve=>{setState({isOpen:true,title,message,onConfirm:()=>{setState(initialState);resolve(true);}});});},[]);const cancel=useCallback(()=>{setState(initialState);},[]);return{...state,confirm,cancel};}
