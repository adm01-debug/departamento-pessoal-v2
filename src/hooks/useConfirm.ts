import{useState,useCallback}from'react';interface ConfirmState{isOpen:boolean;title:string;message:string;onConfirm:()=>void;}export function useConfirm(){const[state,setState]=useState<ConfirmState>({isOpen:false,title:'',message:'',onConfirm:()=>{}});const confirm=useCallback((title:string,message:string):Promise<boolean>=>{return new Promise(resolve=>{setState({isOpen:true,title,message,onConfirm:()=>{resolve(true);setState(s=>({...s,isOpen:false}));}});});},[]);const cancel=useCallback(()=>{setState(s=>({...s,isOpen:false}));},[]);return{...state,confirm,cancel};}
