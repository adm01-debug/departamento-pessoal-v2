import{useQuery,useMutation,useQueryClient}from'@tanstack/react-query';import{api}from'@/lib/api';import{useToast}from'./useToast';export interface Folha{id:string;competencia:string;tipo:string;status:string;totalBruto:number;totalDescontos:number;totalLiquido:number;colaboradores:number;}export interface FolhaItem{id:string;colaboradorId:string;colaborador:string;salarioBruto:number;descontos:number;liquido:number;}export function useFolhas(){return useQuery<Folha[]>({queryKey:['folhas'],queryFn:async()=>{const r=await api.get('/folha');return r.data;}});}export function useFolhaDetalhes(id:string){return useQuery<{folha:Folha;itens:FolhaItem[]}>({queryKey:['folha',id],queryFn:async()=>{const r=await api.get(`/folha/${id}`);return r.data;},enabled:!!id});}export function useCalcularFolha(){const qc=useQueryClient();const{toast}=useToast();return useMutation({mutationFn:async(folhaId:string)=>{const r=await api.post(`/folha/${folhaId}/calcular`);return r.data;},onSuccess:()=>{qc.invalidateQueries({queryKey:['folha']});toast({title:'Folha calculada'});}});}export function useFecharFolha(){const qc=useQueryClient();const{toast}=useToast();return useMutation({mutationFn:async(folhaId:string)=>{const r=await api.post(`/folha/${folhaId}/fechar`);return r.data;},onSuccess:()=>{qc.invalidateQueries({queryKey:['folha']});toast({title:'Folha fechada'});}});}
