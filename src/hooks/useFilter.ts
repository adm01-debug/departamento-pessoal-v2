import{useState,useMemo,useCallback}from'react';interface UseFilterProps<T>{data:T[];searchFields?:(keyof T)[];}interface UseFilterReturn<T>{filteredData:T[];search:string;setSearch:(value:string)=>void;filters:Record<string,string>;setFilter:(key:string,value:string)=>void;clearFilters:()=>void;}export function useFilter<T extends Record<string,any>>({data,searchFields=[]}:UseFilterProps<T>):UseFilterReturn<T>{const[search,setSearch]=useState('');const[filters,setFilters]=useState<Record<string,string>>({});const filteredData=useMemo(()=>{return data.filter(item=>{if(search&&searchFields.length>0){const searchLower=search.toLowerCase();const matchesSearch=searchFields.some(field=>{const value=item[field];return value&&String(value).toLowerCase().includes(searchLower);});if(!matchesSearch)return false;}for(const[key,value]of Object.entries(filters)){if(value&&item[key]!==value)return false;}return true;});},[data,search,searchFields,filters]);const setFilter=useCallback((key:string,value:string)=>{setFilters(prev=>({...prev,[key]:value}));},[]);const clearFilters=useCallback(()=>{setSearch('');setFilters({});},[]);return{filteredData,search,setSearch,filters,setFilter,clearFilters};}
