import{useState,useMemo,useCallback}from'react';export function useFilter<T extends Record<string,any>>(data:T[]){const[search,setSearch]=useState('');const[fieldFilters,setFieldFilters]=useState<Record<string,any>>({});const setFieldFilter=useCallback((field:string,value:any)=>{setFieldFilters(prev=>({...prev,[field]:value}));},[]);const clearFilters=useCallback(()=>{setSearch('');setFieldFilters({});},[]);const filteredData=useMemo(()=>{return data.filter(item=>{if(search){const searchLower=search.toLowerCase();const matches=Object.values(item).some(v=>String(v).toLowerCase().includes(searchLower));if(!matches)return false;}for(const[field,value]of Object.entries(fieldFilters)){if(value&&item[field]!==value)return false;}return true;});},[data,search,fieldFilters]);return{filteredData,search,setSearch,fieldFilters,setFieldFilter,clearFilters};}
