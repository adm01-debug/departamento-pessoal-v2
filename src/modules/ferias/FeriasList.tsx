import React from'react';import{useQuery}from'@tanstack/react-query';import{feriasService}from'@/services/feriasService';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{Badge}from'@/components/ui/badge';import{formatDate}from'@/utils/formatters';export function FeriasList(){const{data:ferias,isLoading}=useQuery({queryKey:['ferias'],queryFn:()=>feriasService.listar()});if(isLoading)return<p>Carregando...</p>;return(<div className="space-y-4">{ferias?.map((f:any)=>(<Card key={f.id}><CardHeader><CardTitle className="flex justify-between items-center"><span>{f.colaborador?.nome}</span><Badge>{f.status}</Badge></CardTitle></CardHeader><CardContent><p>Período: {formatDate(f.dataInicio)} a {formatDate(f.dataFim)}</p><p>Dias: {f.diasGozo}</p></CardContent></Card>))}</div>);}
