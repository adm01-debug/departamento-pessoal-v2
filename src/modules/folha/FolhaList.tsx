import React from'react';import{DataTable}from'@/components/common/DataTable';import{formatCurrency}from'@/utils/formatters';import{Badge}from'@/components/ui/badge';const columns=[{accessorKey:'colaborador.nome',header:'Colaborador'},{accessorKey:'salarioBase',header:'Salário Base',cell:({row}:any)=>formatCurrency(row.original.salarioBase)},{accessorKey:'totalProventos',header:'Proventos',cell:({row}:any)=>formatCurrency(row.original.totalProventos)},{accessorKey:'totalDescontos',header:'Descontos',cell:({row}:any)=>formatCurrency(row.original.totalDescontos)},{accessorKey:'salarioLiquido',header:'Líquido',cell:({row}:any)=>formatCurrency(row.original.salarioLiquido)},{accessorKey:'status',header:'Status',cell:({row}:any)=>(<Badge>{row.original.status}</Badge>)}];interface Props{folhas:any[];loading?:boolean;}export function FolhaList({folhas,loading}:Props){return(<DataTable columns={columns}data={folhas}loading={loading}/>);}
