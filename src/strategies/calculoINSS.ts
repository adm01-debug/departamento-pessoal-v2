export interface INSSStrategy{calcular(salario:number):number;}export class INSSProgressivo implements INSSStrategy{private faixas=[{ate:1412.00,aliquota:0.075},{ate:2666.68,aliquota:0.09},{ate:4000.03,aliquota:0.12},{ate:7786.02,aliquota:0.14}];calcular(salario:number):number{let inss=0;let salarioRestante=salario;let faixaAnterior=0;for(const faixa of this.faixas){if(salarioRestante<=0)break;const baseCalculo=Math.min(salarioRestante,faixa.ate-faixaAnterior);inss+=baseCalculo*faixa.aliquota;salarioRestante-=baseCalculo;faixaAnterior=faixa.ate;}return Math.round(inss*100)/100;}}
