import{z}from'zod';import{validarCPF}from'@/utils/validators';export const colaboradorSchema=z.object({nome:z.string().min(3,'Nome deve ter no mínimo 3 caracteres').max(100),cpf:z.string().refine(validarCPF,'CPF inválido'),rg:z.string().optional(),dataNascimento:z.string().min(1,'Data de nascimento obrigatória'),email:z.string().email('E-mail inválido').optional().or(z.literal('')),telefone:z.string().optional(),dataAdmissao:z.string().min(1,'Data de admissão obrigatória'),cargoId:z.string().min(1,'Cargo obrigatório'),departamentoId:z.string().min(1,'Departamento obrigatório'),salario:z.number().min(1412,'Salário deve ser maior que o mínimo'),pis:z.string().optional(),ctps:z.string().optional(),banco:z.string().optional(),agencia:z.string().optional(),conta:z.string().optional()});export type ColaboradorFormData=z.infer<typeof colaboradorSchema>;
