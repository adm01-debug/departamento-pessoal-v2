import{z}from'zod';const timeRegex=/^([01]\d|2[0-3]):([0-5]\d)$/;export const registroPontoSchema=z.object({tipo:z.enum(['entrada','saida_almoco','retorno_almoco','saida']),dataHora:z.string().optional(),observacao:z.string().max(200).optional()});export const ajustePontoSchema=z.object({colaboradorId:z.string().min(1),data:z.string().min(1),entrada:z.string().regex(timeRegex,'Hora inválida').optional(),saidaAlmoco:z.string().regex(timeRegex,'Hora inválida').optional(),retornoAlmoco:z.string().regex(timeRegex,'Hora inválida').optional(),saida:z.string().regex(timeRegex,'Hora inválida').optional(),motivo:z.string().min(10,'Descreva o motivo do ajuste')});export type RegistroPontoFormData=z.infer<typeof registroPontoSchema>;export type AjustePontoFormData=z.infer<typeof ajustePontoSchema>;
