import{z}from'zod';export const usuarioSchema=z.object({nome:z.string().min(3,'Nome deve ter no mínimo 3 caracteres'),email:z.string().email('Email inválido'),perfilId:z.string().min(1,'Selecione o perfil'),senha:z.string().min(6,'Senha deve ter no mínimo 6 caracteres').optional(),ativo:z.boolean().default(true),});export const alterarSenhaSchema=z.object({senhaAtual:z.string().min(1,'Senha atual obrigatória'),novaSenha:z.string().min(6,'Nova senha deve ter no mínimo 6 caracteres'),confirmarSenha:z.string().min(1,'Confirmação obrigatória'),}).refine(data=>data.novaSenha===data.confirmarSenha,{message:'Senhas não conferem',path:['confirmarSenha'],});export const loginSchema=z.object({email:z.string().email('Email inválido'),senha:z.string().min(1,'Senha obrigatória'),});export type UsuarioFormData=z.infer<typeof usuarioSchema>;export type AlterarSenhaFormData=z.infer<typeof alterarSenhaSchema>;export type LoginFormData=z.infer<typeof loginSchema>;
