import{z}from'zod';export const departamentoSchema=z.object({nome:z.string().min(2,'Nome deve ter pelo menos 2 caracteres'),sigla:z.string().min(2,'Sigla deve ter pelo menos 2 caracteres').max(10,'Sigla deve ter no máximo 10 caracteres'),descricao:z.string().optional(),gestorId:z.string().optional(),centroCusto:z.string().optional(),ativo:z.boolean()});export type DepartamentoFormData=z.infer<typeof departamentoSchema>;
