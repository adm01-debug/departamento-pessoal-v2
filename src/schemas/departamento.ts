import{z}from'zod';export const departamentoSchema=z.object({nome:z.string().min(1,'Nome obrigatório'),codigo:z.string().min(1,'Código obrigatório').regex(/^[A-Z0-9]+$/,'Código deve conter apenas letras maiúsculas e números'),gestorId:z.string().optional(),centroCusto:z.string().optional(),email:z.string().email('Email inválido').optional().or(z.literal('')),telefone:z.string().optional(),ativo:z.boolean().default(true),});export type DepartamentoFormData=z.infer<typeof departamentoSchema>;
