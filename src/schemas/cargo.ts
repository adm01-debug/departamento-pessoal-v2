import{z}from'zod';export const cargoSchema=z.object({nome:z.string().min(1,'Nome obrigatório'),cbo:z.string().min(1,'CBO obrigatório').regex(/^\d{4}-\d{2}$/,'Formato CBO inválido (0000-00)'),nivel:z.enum(['estagiario','junior','pleno','senior','especialista','coordenador','gerente','diretor','ceo'],{required_error:'Selecione o nível'}),departamentoId:z.string().min(1,'Selecione o departamento'),salarioBase:z.number().min(0,'Salário base inválido'),salarioTeto:z.number().min(0).optional(),descricao:z.string().optional(),requisitos:z.array(z.string()).optional(),ativo:z.boolean().default(true),});export type CargoFormData=z.infer<typeof cargoSchema>;
