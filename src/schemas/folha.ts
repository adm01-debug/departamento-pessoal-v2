import{z}from'zod';export const folhaItemSchema=z.object({colaboradorId:z.string().min(1),rubricaId:z.string().min(1),tipo:z.enum(['provento','desconto']),valor:z.number().min(0),quantidade:z.number().min(0).optional(),referencia:z.string().optional()});export const folhaSchema=z.object({competencia:z.string().regex(/^\d{2}\/\d{4}$/,'Competência inválida (MM/YYYY)'),status:z.enum(['aberta','calculada','fechada']),itens:z.array(folhaItemSchema)});export type FolhaFormData=z.infer<typeof folhaSchema>;export type FolhaItemFormData=z.infer<typeof folhaItemSchema>;
