import{Ferias}from'@/types/ferias';let idCounter=1;export function createFerias(overrides:Partial<Ferias>={}):Ferias{const id=String(idCounter++);return{id,colaboradorId:'1',dataInicio:'2024-01-01',dataFim:'2024-01-30',dias:30,abonoPecuniario:false,diasAbono:0,status:'programada',createdAt:new Date().toISOString(),updatedAt:new Date().toISOString(),...overrides};}export function createFeriasList(count:number):Ferias[]{return Array.from({length:count},()=>createFerias());}
