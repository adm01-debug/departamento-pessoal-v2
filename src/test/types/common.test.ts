import { describe, it, expect } from 'vitest';
describe('common types', () => {
  it('should be importable', () => { expect(true).toBe(true); });
  it('should have correct structure', () => { expect(true).toBe(true); });
});
