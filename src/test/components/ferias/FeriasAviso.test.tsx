import { render } from '@testing-library/react';
import { describe, it } from 'vitest';
import { FeriasAviso } from '@/components/ferias/FeriasAviso';
describe('FeriasAviso', () => { it('renders', () => { render(<FeriasAviso />); }); });
