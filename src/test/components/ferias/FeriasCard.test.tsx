import { render } from '@testing-library/react';
import { describe, it } from 'vitest';
import { FeriasCard } from '@/components/ferias/FeriasCard';
describe('FeriasCard', () => { it('renders', () => { render(<FeriasCard />); }); });
