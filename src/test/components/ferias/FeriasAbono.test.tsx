import { render } from '@testing-library/react';
import { describe, it } from 'vitest';
import { FeriasAbono } from '@/components/ferias/FeriasAbono';
describe('FeriasAbono', () => { it('renders', () => { render(<FeriasAbono />); }); });
