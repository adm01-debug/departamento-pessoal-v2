import { render } from '@testing-library/react';
import { describe, it } from 'vitest';
import { EscalaTrabalho } from '@/components/ponto/EscalaTrabalho';
describe('EscalaTrabalho', () => { it('renders', () => { render(<EscalaTrabalho />); }); });
