import { render } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
import { FolhaHistorico } from '@/components/folha/FolhaHistorico';
describe('FolhaHistorico', () => { it('renders', () => { render(<FolhaHistorico />); }); });
