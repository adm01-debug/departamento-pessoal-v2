import { render } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
import { FolhaDetalhes } from '@/components/folha/FolhaDetalhes';
describe('FolhaDetalhes', () => { it('renders', () => { render(<FolhaDetalhes />); }); });
