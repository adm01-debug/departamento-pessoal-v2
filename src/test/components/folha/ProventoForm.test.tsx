import { render } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
import { ProventoForm } from '@/components/folha/ProventoForm';
describe('ProventoForm', () => { it('renders', () => { render(<ProventoForm />); }); });
