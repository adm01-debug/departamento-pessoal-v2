import { render } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
import { ContrachequeModal } from '@/components/folha/ContrachequeModal';
describe('ContrachequeModal', () => { it('renders', () => { render(<ContrachequeModal />); }); });
