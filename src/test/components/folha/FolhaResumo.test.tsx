import { render } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
import { FolhaResumo } from '@/components/folha/FolhaResumo';
describe('FolhaResumo', () => { it('renders', () => { render(<FolhaResumo />); }); });
