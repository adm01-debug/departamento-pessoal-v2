import { render } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
import { FolhaComparativo } from '@/components/folha/FolhaComparativo';
describe('FolhaComparativo', () => { it('renders', () => { render(<FolhaComparativo />); }); });
