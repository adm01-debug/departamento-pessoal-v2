import { render } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
import { DescontoForm } from '@/components/folha/DescontoForm';
describe('DescontoForm', () => { it('renders', () => { render(<DescontoForm />); }); });
