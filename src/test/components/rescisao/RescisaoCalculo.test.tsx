import { render } from '@testing-library/react';
import { describe, it } from 'vitest';
import { RescisaoCalculo } from '@/components/rescisao/RescisaoCalculo';
describe('RescisaoCalculo', () => { it('renders', () => { render(<RescisaoCalculo />); }); });
