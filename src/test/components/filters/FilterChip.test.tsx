import { render } from '@testing-library/react';
import { describe, it } from 'vitest';
import { FilterChip } from '@/components/filters/FilterChip';
describe('FilterChip', () => { it('renders', () => { render(<FilterChip />); }); });
