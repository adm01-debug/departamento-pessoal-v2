import { render } from '@testing-library/react';
import { describe, it } from 'vitest';
import { MultiSelect } from '@/components/filters/MultiSelect';
describe('MultiSelect', () => { it('renders', () => { render(<MultiSelect />); }); });
