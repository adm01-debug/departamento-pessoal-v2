import { render } from '@testing-library/react';
import { describe, it } from 'vitest';
import { TrendIndicator } from '@/components/data/TrendIndicator';
describe('TrendIndicator', () => { it('renders', () => { render(<TrendIndicator />); }); });
