import { render } from '@testing-library/react';
import { describe, it } from 'vitest';
import { DataList } from '@/components/data/DataList';
describe('DataList', () => { it('renders', () => { render(<DataList />); }); });
