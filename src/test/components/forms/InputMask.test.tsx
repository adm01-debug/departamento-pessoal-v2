import { render } from '@testing-library/react';
import { describe, it } from 'vitest';
import { InputMask } from '@/components/forms/InputMask';
describe('InputMask', () => { it('renders', () => { render(<InputMask />); }); });
