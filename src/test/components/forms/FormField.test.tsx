import { render } from '@testing-library/react';
import { describe, it } from 'vitest';
import { FormField } from '@/components/forms/FormField';
describe('FormField', () => { it('renders', () => { render(<FormField />); }); });
