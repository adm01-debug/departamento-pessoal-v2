import { render } from '@testing-library/react';
import { describe, it } from 'vitest';
import { FormValidation } from '@/components/forms/FormValidation';
describe('FormValidation', () => { it('renders', () => { render(<FormValidation />); }); });
