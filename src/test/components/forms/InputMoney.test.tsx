import { render } from '@testing-library/react';
import { describe, it } from 'vitest';
import { InputMoney } from '@/components/forms/InputMoney';
describe('InputMoney', () => { it('renders', () => { render(<InputMoney />); }); });
