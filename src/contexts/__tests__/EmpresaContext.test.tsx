import { describe, it, expect } from 'vitest';
import { EmpresaContext } from '../EmpresaContext';
describe('EmpresaContext', () => { it('defined', () => { expect(EmpresaContext).toBeDefined(); }); });
