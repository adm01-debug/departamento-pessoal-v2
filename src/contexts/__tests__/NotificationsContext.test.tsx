import { describe, it, expect } from 'vitest';
import { NotificationsContext } from '../NotificationsContext';
describe('NotificationsContext', () => { it('defined', () => { expect(NotificationsContext).toBeDefined(); }); });
