import{api}from'@/lib/api';export type CNABLayout='240'|'400';export interface CNABConfig{layout:CNABLayout;banco:string;empresa:{nome:string;cnpj:string;};convenio:string;}export interface CNABRegistro{tipo:'detalhe'|'header'|'trailer';dados:Record<string,any>;}export interface CNABArquivo{nome:string;conteudo:string;registros:number;valorTotal:number;}export class CNABIntegration{private config:CNABConfig;constructor(config:CNABConfig){this.config=config;}async gerarRemessa(registros:CNABRegistro[]):Promise<CNABArquivo>{const response=await api.post('/integrations/cnab/remessa',{registros,config:this.config});return response.data;}async lerRetorno(arquivo:File):Promise<{registros:CNABRegistro[];resumo:{liquidados:number;rejeitados:number;valorTotal:number}}>{const formData=new FormData();formData.append('arquivo',arquivo);formData.append('layout',this.config.layout);const response=await api.post('/integrations/cnab/retorno',formData);return response.data;}async validarArquivo(conteudo:string):Promise<{valido:boolean;erros:string[]}>{const response=await api.post('/integrations/cnab/validar',{conteudo,layout:this.config.layout});return response.data;}}export const createCNABIntegration=(config:CNABConfig)=>new CNABIntegration(config);
