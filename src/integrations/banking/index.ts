import{api}from'@/lib/api';export interface BankConfig{banco:string;agencia:string;conta:string;convenio:string;certificado?:string;}export interface Remessa{id:string;tipo:'pagamento'|'cobranca';registros:RemessaRegistro[];geradoEm:string;}export interface RemessaRegistro{sequencial:number;valor:number;dataVencimento:string;sacado:{nome:string;cpfCnpj:string;endereco:string;};}export interface Retorno{id:string;arquivo:string;registros:RetornoRegistro[];processadoEm:string;}export interface RetornoRegistro{nossoNumero:string;valorPago:number;dataPagamento:string;status:'liquidado'|'baixado'|'rejeitado';}export class BankingIntegration{private config:BankConfig;constructor(config:BankConfig){this.config=config;}async gerarRemessa(tipo:'pagamento'|'cobranca',registros:RemessaRegistro[]):Promise<{arquivo:string;conteudo:string}>{const response=await api.post('/integrations/banking/remessa',{tipo,registros,config:this.config});return response.data;}async processarRetorno(arquivo:File):Promise<Retorno>{const formData=new FormData();formData.append('arquivo',arquivo);const response=await api.post('/integrations/banking/retorno',formData);return response.data;}async consultarSaldo():Promise<{saldo:number;dataConsulta:string}>{const response=await api.get('/integrations/banking/saldo',{params:this.config});return response.data;}}export const createBankingIntegration=(config:BankConfig)=>new BankingIntegration(config);
