import{api}from'@/lib/api';export interface INSSConfig{certificado?:string;cnpj:string;}export interface GPSData{competencia:string;codigoPagamento:string;valorInss:number;valorOutrasEntidades:number;valorAtualizacao:number;}export interface DARFData{competencia:string;codigoReceita:string;valor:number;multa?:number;juros?:number;}export class INSSIntegration{private config:INSSConfig;constructor(config:INSSConfig){this.config=config;}async gerarGPS(data:GPSData):Promise<{codigoBarras:string;linhaDigitavel:string;valor:number}>{const response=await api.post('/integrations/inss/gps',{...data,config:this.config});return response.data;}async gerarDARF(data:DARFData):Promise<{codigoBarras:string;valor:number}>{const response=await api.post('/integrations/inss/darf',{...data,config:this.config});return response.data;}async calcularContribuicao(salario:number,categoria:'empregado'|'contribuinte_individual'|'domestico'):Promise<{aliquota:number;valor:number;teto:number}>{const response=await api.post('/integrations/inss/calcular',{salario,categoria});return response.data;}async consultarCND():Promise<{situacao:'regular'|'pendente';validade?:string;pendencias?:string[]}>{const response=await api.get('/integrations/inss/cnd',{params:{cnpj:this.config.cnpj}});return response.data;}}export const createINSSIntegration=(config:INSSConfig)=>new INSSIntegration(config);
