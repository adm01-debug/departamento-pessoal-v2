import{api}from'@/lib/api';export interface N8NConfig{baseUrl:string;apiKey?:string;}export interface Workflow{id:string;name:string;active:boolean;nodes:number;}export interface WebhookTrigger{event:string;url:string;method:'GET'|'POST';headers?:Record<string,string>;}export interface ExecutionResult{id:string;finished:boolean;mode:string;startedAt:string;stoppedAt?:string;data?:any;}export class N8NIntegration{private config:N8NConfig;constructor(config:N8NConfig){this.config=config;}async triggerWebhook(webhookPath:string,data:any):Promise<ExecutionResult>{const response=await api.post(`${this.config.baseUrl}/webhook/${webhookPath}`,data);return response.data;}async listWorkflows():Promise<Workflow[]>{const response=await api.get('/integrations/n8n/workflows',{params:{baseUrl:this.config.baseUrl,apiKey:this.config.apiKey}});return response.data;}async executeWorkflow(workflowId:string,data?:any):Promise<ExecutionResult>{const response=await api.post('/integrations/n8n/execute',{workflowId,data,config:this.config});return response.data;}async getExecution(executionId:string):Promise<ExecutionResult>{const response=await api.get(`/integrations/n8n/executions/${executionId}`);return response.data;}async registrarWebhook(trigger:WebhookTrigger):Promise<{id:string;webhookUrl:string}>{const response=await api.post('/integrations/n8n/webhooks',{...trigger,config:this.config});return response.data;}}export const createN8NIntegration=(config:N8NConfig)=>new N8NIntegration(config);
