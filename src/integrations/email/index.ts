import{api}from'@/lib/api';export interface EmailConfig{provider:'smtp'|'sendgrid'|'ses'|'mailgun';host?:string;port?:number;user?:string;password?:string;apiKey?:string;from:string;fromName:string;}export interface EmailMessage{to:string|string[];subject:string;html?:string;text?:string;template?:string;templateData?:Record<string,any>;attachments?:{filename:string;content:string;contentType:string;}[];}export interface EmailResult{id:string;status:'sent'|'queued'|'failed';error?:string;}export class EmailIntegration{private config:EmailConfig;constructor(config:EmailConfig){this.config=config;}async send(message:EmailMessage):Promise<EmailResult>{const response=await api.post('/integrations/email/send',{...message,config:this.config});return response.data;}async sendTemplate(templateId:string,to:string|string[],data:Record<string,any>):Promise<EmailResult>{return this.send({to,subject:'',template:templateId,templateData:data});}async sendBulk(messages:EmailMessage[]):Promise<EmailResult[]>{const response=await api.post('/integrations/email/bulk',{messages,config:this.config});return response.data;}async verificarEmail(email:string):Promise<{valido:boolean;motivo?:string}>{const response=await api.post('/integrations/email/verificar',{email});return response.data;}}export const createEmailIntegration=(config:EmailConfig)=>new EmailIntegration(config);
