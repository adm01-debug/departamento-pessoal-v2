import{api}from'@/lib/api';export interface PixConfig{psp:'banco_brasil'|'itau'|'bradesco'|'santander'|'sicoob'|'gerencianet';clientId:string;clientSecret:string;certificado:string;chave:string;}export interface PixCobranca{valor:number;chave:string;txid?:string;infoAdicionais?:{nome:string;valor:string}[];expiracao?:number;}export interface PixCriado{txid:string;location:string;qrcode:string;imagemQrcode:string;copiaECola:string;}export class PixIntegration{private config:PixConfig;constructor(config:PixConfig){this.config=config;}async criarCobranca(cobranca:PixCobranca):Promise<PixCriado>{const response=await api.post('/integrations/pix/cobranca',{...cobranca,config:this.config});return response.data;}async consultarCobranca(txid:string):Promise<{status:'ATIVA'|'CONCLUIDA'|'REMOVIDA_PELO_USUARIO_RECEBEDOR';valor:number;horario?:string}>{const response=await api.get(`/integrations/pix/cobranca/${txid}`);return response.data;}async listarRecebimentos(inicio:string,fim:string):Promise<{pix:{txid:string;valor:number;horario:string;pagador:any}[]}>{const response=await api.get('/integrations/pix/recebimentos',{params:{inicio,fim}});return response.data;}async devolverPix(e2eid:string,valor:number):Promise<{id:string;status:string}>{const response=await api.post('/integrations/pix/devolucao',{e2eid,valor,config:this.config});return response.data;}}export const createPixIntegration=(config:PixConfig)=>new PixIntegration(config);
