import{api}from'@/lib/api';export interface GoogleConfig{clientId:string;clientSecret:string;redirectUri:string;scopes:string[];}export interface GoogleUser{id:string;email:string;name:string;picture?:string;}export interface CalendarEvent{id?:string;summary:string;description?:string;start:{dateTime:string;timeZone:string};end:{dateTime:string;timeZone:string};attendees?:{email:string}[];}export class GoogleIntegration{private config:GoogleConfig;constructor(config:GoogleConfig){this.config=config;}getAuthUrl():string{const params=new URLSearchParams({client_id:this.config.clientId,redirect_uri:this.config.redirectUri,scope:this.config.scopes.join(' '),response_type:'code',access_type:'offline'});return`https://accounts.google.com/o/oauth2/v2/auth?${params}`;}async getTokens(code:string):Promise<{accessToken:string;refreshToken:string}>{const response=await api.post('/integrations/google/tokens',{code,config:this.config});return response.data;}async getUserInfo(accessToken:string):Promise<GoogleUser>{const response=await api.get('/integrations/google/userinfo',{headers:{Authorization:`Bearer ${accessToken}`}});return response.data;}async createCalendarEvent(accessToken:string,event:CalendarEvent):Promise<CalendarEvent>{const response=await api.post('/integrations/google/calendar/events',{event},{headers:{Authorization:`Bearer ${accessToken}`}});return response.data;}}export const createGoogleIntegration=(config:GoogleConfig)=>new GoogleIntegration(config);
