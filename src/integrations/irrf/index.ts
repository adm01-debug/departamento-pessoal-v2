import{api}from'@/lib/api';export interface IRRFConfig{cnpj:string;ano:number;}export interface DIRFData{anoCalendario:number;beneficiarios:{cpf:string;nome:string;rendimentos:number;irRetido:number;previdencia:number;dependentes:number;}[];}export interface DARFData{competencia:string;codigoReceita:string;valor:number;}export class IRRFIntegration{private config:IRRFConfig;constructor(config:IRRFConfig){this.config=config;}async calcular(salarioBruto:number,deducoes:{dependentes:number;pensao:number;previdencia:number;outrasDeducoes:number}):Promise<{baseCalculo:number;aliquota:number;parcelaDeduzir:number;valorIRRF:number}>{const response=await api.post('/integrations/irrf/calcular',{salarioBruto,deducoes});return response.data;}async gerarDARF(data:DARFData):Promise<{codigoBarras:string;valor:number}>{const response=await api.post('/integrations/irrf/darf',{...data,config:this.config});return response.data;}async gerarDIRF():Promise<{arquivo:string;totalBeneficiarios:number}>{const response=await api.post('/integrations/irrf/dirf',{config:this.config});return response.data;}async consultarTabelaProgressiva():Promise<{faixas:{de:number;ate:number;aliquota:number;parcela:number}[];deducaoDependente:number}>{const response=await api.get('/integrations/irrf/tabela');return response.data;}}export const createIRRFIntegration=(config:IRRFConfig)=>new IRRFIntegration(config);
