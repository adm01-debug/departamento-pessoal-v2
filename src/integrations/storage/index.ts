import{api}from'@/lib/api';export interface StorageConfig{provider:'s3'|'gcs'|'azure'|'local';bucket?:string;region?:string;accessKey?:string;secretKey?:string;basePath?:string;}export interface UploadResult{key:string;url:string;size:number;contentType:string;}export interface FileInfo{key:string;size:number;lastModified:Date;contentType:string;url:string;}export class StorageIntegration{private config:StorageConfig;constructor(config:StorageConfig){this.config=config;}async upload(file:File,path?:string):Promise<UploadResult>{const formData=new FormData();formData.append('file',file);if(path)formData.append('path',path);const response=await api.post('/integrations/storage/upload',formData,{headers:{'Content-Type':'multipart/form-data'}});return response.data;}async download(key:string):Promise<Blob>{const response=await api.get(`/integrations/storage/download/${encodeURIComponent(key)}`,{responseType:'blob'});return response.data;}async delete(key:string):Promise<void>{await api.delete(`/integrations/storage/${encodeURIComponent(key)}`);}async list(prefix?:string):Promise<FileInfo[]>{const response=await api.get('/integrations/storage/list',{params:{prefix}});return response.data;}async getSignedUrl(key:string,expiresIn:number=3600):Promise<string>{const response=await api.post('/integrations/storage/signed-url',{key,expiresIn});return response.data.url;}}export const createStorageIntegration=(config:StorageConfig)=>new StorageIntegration(config);
