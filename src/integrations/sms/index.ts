import{api}from'@/lib/api';export interface SMSConfig{provider:'twilio'|'zenvia'|'totalvoice'|'smsdev';accountSid?:string;authToken?:string;apiKey?:string;from:string;}export interface SMSMessage{to:string;message:string;schedule?:string;}export interface SMSResult{id:string;status:'sent'|'queued'|'failed'|'delivered';error?:string;}export class SMSIntegration{private config:SMSConfig;constructor(config:SMSConfig){this.config=config;}async send(message:SMSMessage):Promise<SMSResult>{const response=await api.post('/integrations/sms/send',{...message,config:this.config});return response.data;}async sendBulk(messages:SMSMessage[]):Promise<SMSResult[]>{const response=await api.post('/integrations/sms/bulk',{messages,config:this.config});return response.data;}async getStatus(messageId:string):Promise<SMSResult>{const response=await api.get(`/integrations/sms/status/${messageId}`);return response.data;}async getSaldo():Promise<{saldo:number;unidade:string}>{const response=await api.get('/integrations/sms/saldo',{params:{provider:this.config.provider}});return response.data;}}export const createSMSIntegration=(config:SMSConfig)=>new SMSIntegration(config);
