import{api}from'@/lib/api';import{ENDPOINTS}from'@/api/endpoints';import{Ferias}from'@/types/ferias';export const feriasApi={listar:async(params?:any):Promise<Ferias[]>=>{const{data}=await api.get(ENDPOINTS.FERIAS.BASE,{params});return data;},buscar:async(id:string):Promise<Ferias>=>{const{data}=await api.get(ENDPOINTS.FERIAS.BY_ID(id));return data;},criar:async(dados:Partial<Ferias>):Promise<Ferias>=>{const{data}=await api.post(ENDPOINTS.FERIAS.BASE,dados);return data;},aprovar:async(id:string):Promise<Ferias>=>{const{data}=await api.post(ENDPOINTS.FERIAS.APROVAR(id));return data;},rejeitar:async(id:string,motivo?:string):Promise<Ferias>=>{const{data}=await api.post(ENDPOINTS.FERIAS.REJEITAR(id),{motivo});return data;}};
