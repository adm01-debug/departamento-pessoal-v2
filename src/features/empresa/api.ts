import{api}from'@/lib/api';export const empresaApi={async buscar(){const{data}=await api.get('/empresa');return data;},async atualizar(dados:any){const{data}=await api.put('/empresa',dados);return data;},async uploadLogo(file:File){const formData=new FormData();formData.append('logo',file);const{data}=await api.post('/empresa/logo',formData,{headers:{'Content-Type':'multipart/form-data'}});return data;}};
