import{api}from'@/lib/api';export const esocialApi={async listarEventos(status?:string){const{data}=await api.get('/esocial/eventos',{params:{status}});return data;},async buscarEvento(id:string){const{data}=await api.get(`/esocial/eventos/${id}`);return data;},async enviarEvento(id:string){const{data}=await api.post(`/esocial/eventos/${id}/enviar`);return data;},async reenviarEvento(id:string){const{data}=await api.post(`/esocial/eventos/${id}/reenviar`);return data;},async consultarRetorno(protocolo:string){const{data}=await api.get(`/esocial/retorno/${protocolo}`);return data;}};
