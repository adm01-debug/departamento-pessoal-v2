import{useQuery,useMutation,useQueryClient}from'@tanstack/react-query';import{demissaoApi}from'./api';export function useCalcularVerbas(id:string){return useQuery({queryKey:['demissao',id,'verbas'],queryFn:()=>demissaoApi.calcularVerbas(id),enabled:!!id});}export function useIniciarDemissao(){const qc=useQueryClient();return useMutation({mutationFn:({colaboradorId,motivo,data}:{colaboradorId:string;motivo:string;data:string})=>demissaoApi.iniciar(colaboradorId,motivo,data),onSuccess:()=>{qc.invalidateQueries({queryKey:['demissoes']});}});}export function useFinalizarDemissao(){return useMutation({mutationFn:demissaoApi.finalizar});}
