import{api}from'@/lib/api';export const demissaoApi={async iniciar(colaboradorId:string,motivo:string,data:string){const{data:resp}=await api.post('/demissoes',{colaboradorId,motivo,data});return resp;},async calcularVerbas(id:string){const{data}=await api.get(`/demissoes/${id}/verbas`);return data;},async gerarDocumentos(id:string){const{data}=await api.get(`/demissoes/${id}/documentos`,{responseType:'blob'});return data;},async finalizar(id:string){const{data}=await api.post(`/demissoes/${id}/finalizar`);return data;}};
