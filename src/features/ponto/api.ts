import{api}from'@/lib/api';import{ENDPOINTS}from'@/api/endpoints';import{Ponto}from'@/types/ponto';export const pontoApi={listar:async(params?:any):Promise<Ponto[]>=>{const{data}=await api.get(ENDPOINTS.PONTO.BASE,{params});return data;},registrar:async(tipo:string):Promise<Ponto>=>{const{data}=await api.post(ENDPOINTS.PONTO.REGISTRAR,{tipo,dataHora:new Date().toISOString()});return data;},getEspelho:async(colaboradorId:string,mes:string,ano:string):Promise<Ponto[]>=>{const{data}=await api.get(ENDPOINTS.PONTO.ESPELHO,{params:{colaboradorId,mes,ano}});return data;}};
