// V15-433
export interface FolhaPagamento { id: string; empresa_id: string; competencia: string; tipo: 'mensal' | 'adiantamento' | 'ferias' | '13_primeira' | '13_segunda' | 'rescisao' | 'plr'; status: 'rascunho' | 'calculada' | 'conferida' | 'fechada' | 'paga'; total_proventos: number; total_descontos: number; total_liquido: number; total_fgts: number; data_calculo?: string; data_fechamento?: string; data_pagamento?: string; created_at: string; }
export interface ItemFolha { id: string; folha_id: string; colaborador_id: string; colaborador_nome: string; salario_base: number; total_proventos: number; total_descontos: number; valor_liquido: number; inss: number; irrf: number; fgts: number; }
export interface FolhaFilters { competencia?: string; tipo?: FolhaPagamento['tipo']; status?: FolhaPagamento['status']; }
