export interface RequestConfig{headers?:Record<string,string>;params?:Record<string,any>;timeout?:number;}export interface QueryParams{page?:number;limit?:number;search?:string;sortBy?:string;sortOrder?:'asc'|'desc';[key:string]:any;}export interface MutationResult<T>{data?:T;error?:string;success:boolean;}export interface PaginatedParams{page:number;pageSize:number;filters?:Record<string,any>;sort?:{field:string;order:'asc'|'desc'};}
