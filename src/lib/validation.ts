export function isValidCPF(cpf:string):boolean{const clean=cpf.replace(/\D/g,'');if(clean.length!==11||/^(\d)+$/.test(clean))return false;let sum=0;for(let i=0;i<9;i++)sum+=(10-i)*parseInt(clean[i]);let d1=(sum*10)%11;if(d1===10)d1=0;if(d1!==parseInt(clean[9]))return false;sum=0;for(let i=0;i<10;i++)sum+=(11-i)*parseInt(clean[i]);let d2=(sum*10)%11;if(d2===10)d2=0;return d2===parseInt(clean[10]);}export function isValidCNPJ(cnpj:string):boolean{const clean=cnpj.replace(/\D/g,'');if(clean.length!==14)return false;const calc=(size:number)=>{let sum=0,pos=size-7;for(let i=size;i>=1;i--){sum+=parseInt(clean[size-i])*pos--;if(pos<2)pos=9;}return sum%11<2?0:11-sum%11;};const d1=calc(12);const d2=calc(13);return d1===parseInt(clean[12])&&d2===parseInt(clean[13]);}export function isValidEmail(email:string):boolean{return/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);}export function isValidPhone(phone:string):boolean{const clean=phone.replace(/\D/g,'');return clean.length>=10&&clean.length<=11;}
