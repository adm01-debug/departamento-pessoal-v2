import { describe, it, expect } from 'vitest';
import * as schemasDesligamento from '../schemasDesligamento';
describe('schemasDesligamento', () => { it('should be defined', () => { expect(schemasDesligamento).toBeDefined(); }); });
