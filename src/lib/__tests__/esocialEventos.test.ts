import { describe, it, expect } from 'vitest';
import * as esocialEventos from '../esocialEventos';
describe('esocialEventos', () => { it('should export events', () => { expect(esocialEventos).toBeDefined(); }); });
