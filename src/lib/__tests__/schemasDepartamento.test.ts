import { describe, it, expect } from 'vitest';
import * as schemasDepartamento from '../schemasDepartamento';
describe('schemasDepartamento', () => { it('should be defined', () => { expect(schemasDepartamento).toBeDefined(); }); });
