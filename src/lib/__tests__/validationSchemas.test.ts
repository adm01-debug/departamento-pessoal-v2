import { describe, it, expect } from 'vitest';
import * as validationSchemas from '../validationSchemas';
describe('validationSchemas', () => { it('should export schemas', () => { expect(validationSchemas).toBeDefined(); }); });
