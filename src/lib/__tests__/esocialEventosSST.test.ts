import { describe, it, expect } from 'vitest';
import * as esocialEventosSST from '../esocialEventosSST';
describe('esocialEventosSST', () => { it('should export events', () => { expect(esocialEventosSST).toBeDefined(); }); });
