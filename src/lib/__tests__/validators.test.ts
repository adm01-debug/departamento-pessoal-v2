import { describe, it, expect } from 'vitest';
import * as validators from '../validators';
describe('validators', () => { it('should export functions', () => { expect(validators).toBeDefined(); }); });
