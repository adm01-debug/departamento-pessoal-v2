import { describe, it, expect } from 'vitest';
import * as objectHelpers from '../objectHelpers';
describe('objectHelpers', () => { it('should export functions', () => { expect(objectHelpers).toBeDefined(); }); });
