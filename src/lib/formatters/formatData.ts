export function formatData(data: string | Date, formato: string = "dd/MM/yyyy"): string { const d=typeof data==="string"?new Date(data):data; const pad=(n:number)=>n.toString().padStart(2,"0"); const map:{[k:string]:string}={dd:pad(d.getDate()),MM:pad(d.getMonth()+1),yyyy:d.getFullYear().toString(),yy:d.getFullYear().toString().slice(-2),HH:pad(d.getHours()),mm:pad(d.getMinutes()),ss:pad(d.getSeconds())}; return formato.replace(/dd|MM|yyyy|yy|HH|mm|ss/g,m=>map[m]); }
export function formatDataInput(valor: string): string { const n=valor.replace(/\D/g,"").slice(0,8); if(n.length<=2)return n; if(n.length<=4)return n.slice(0,2)+"/"+n.slice(2); return n.slice(0,2)+"/"+n.slice(2,4)+"/"+n.slice(4); }
export function parseDataBR(data: string): Date | null { const p=data.split("/"); if(p.length!==3)return null; return new Date(parseInt(p[2]),parseInt(p[1])-1,parseInt(p[0])); }
export function formatDataRelativa(data: string | Date): string { const d=typeof data==="string"?new Date(data):data; const diff=Date.now()-d.getTime(); const dias=Math.floor(diff/(1000*60*60*24)); if(dias===0)return "Hoje"; if(dias===1)return "Ontem"; if(dias<7)return `${dias} dias atrás`; if(dias<30)return `${Math.floor(dias/7)} semanas atrás`; if(dias<365)return `${Math.floor(dias/30)} meses atrás`; return `${Math.floor(dias/365)} anos atrás`; }
export function formatCompetencia(data: string | Date): string { const d=typeof data==="string"?new Date(data):data; const meses=["Jan","Fev","Mar","Abr","Mai","Jun","Jul","Ago","Set","Out","Nov","Dez"]; return meses[d.getMonth()]+"/"+d.getFullYear(); }
export function isDataValida(data: string): boolean { const d=parseDataBR(data); return d!==null&&!isNaN(d.getTime()); }
export default { formatData, formatDataInput, parseDataBR, formatDataRelativa, formatCompetencia, isDataValida };
