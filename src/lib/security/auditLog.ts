export interface AuditEntry{id:string;timestamp:string;userId:string;action:string;resource:string;resourceId?:string;details?:Record<string,any>;ip?:string;userAgent?:string;}export function createAuditEntry(userId:string,action:string,resource:string,details?:Record<string,any>):AuditEntry{return{id:crypto.randomUUID(),timestamp:new Date().toISOString(),userId,action,resource,details};}export function formatAuditLog(entries:AuditEntry[]):string{return entries.map(e=>`[${e.timestamp}] ${e.userId}: ${e.action} on ${e.resource}`).join("
");}export default{createAuditEntry,formatAuditLog};
